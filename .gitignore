node_modules/
.next/
out/
build/
dist/
src/generated/
*.tsbuildinfo
next-env.d.ts

.env
.env.local
.env.*.local
!.env.example

.DS_Store
*.log
npm-debug.log*
coverage/
.vitest/
.idea/
.vscode/

# Токен, которым Prometheus читает /api/metrics (решение 137) — тот же секрет,
# что METRICS_TOKEN. Пример без значения — deploy/monitoring/metrics-token.example.
deploy/monitoring/metrics-token

# Рабочие заметки и одноразовые скрипты сессии (решение 141) — не часть репозитория.
.demo141-todo.md
scripts/_tmp_*.ts
