'use client'

import { ReportTablePage } from '../ReportTablePage'

/**
 * Каталог по ТЗ (решение 145, п.8 функц. требований): «Название вуза, Вендор,
 * ПО, Номер договора, Подписание лицензии, Срок действия лицензии (год),
 * Статус по передаче, ФИО менеджера, Ответственные от вуза, Комментарий» —
 * дословно и в этом порядке. Данные и колонки строит сервер (`GET
 * /api/reports/catalog`), экран — превью и ссылки на выгрузку (решение 150).
 */
export default function CatalogReportPage() {
  return (
    <ReportTablePage
      title="Каталог по ТЗ"
      breadcrumbLabel="Каталог по ТЗ"
      description="Реквизиты договора и лицензии, статус передачи ПО и ответственные — по каждой связке, колонки дословно из ТЗ."
      endpoint="/api/reports/catalog"
    />
  )
}
