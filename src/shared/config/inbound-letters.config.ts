/**
 * Нормативы разбора писем вузов (решение 170). Как и остальные — TEMP: подбор
 * значений — вопрос эксплуатации после первых недель на реальных письмах.
 */

/** Ограничение размера загружаемого `.eml` (multipart-поле `file`). */
export const MAX_EML_SIZE_BYTES = 5 * 1024 * 1024 // TEMP, 5 МБ — с запасом на подпись и цитаты

/** Сколько похожих размеченных писем брать в подсказку модели (few-shot). */
export const SIMILAR_EXAMPLES_LIMIT = 3 // TEMP

/** Сколько прошлых размеченных писем просматривать в поиске похожих — не безгранично. */
export const SIMILAR_EXAMPLES_POOL_LIMIT = 200 // TEMP

/**
 * Учитывать похожим только письмо с ненулевым пересечением слов: случайное
 * совпадение по стоп-словам не в счёт (стоп-слова уже отфильтрованы токенизацией).
 */
export const SIMILARITY_MIN_SCORE = 0.01

/** Точность разбора по группе (решение 170, тот же принцип, что решение 119). */
export const INBOUND_LETTER_LEARNING = {
  /** Период полураспада: решение давностью столько дней весит вдвое меньше свежего. */
  halfLifeDays: 30, // TEMP, совпадает с решением 119 — те же формулы (`recommendations.learning.ts`)
} as const

/** Длина превью письма в списке (`InboundLetterListItemDto.bodyPreview`). */
export const LETTER_PREVIEW_LENGTH = 160

/** Уверенность разбора правилами без единого совпадения ключевого слова — группа «Прочее». */
export const RULES_FALLBACK_CONFIDENCE = 0.2 // TEMP
/** Уверенность правил при совпадении по ключевым словам: растёт с числом совпадений, не выше этого. */
export const RULES_MAX_CONFIDENCE = 0.6 // TEMP

/** Сколько цитат-оснований отдавать в разборе — и от правил, и от модели. */
export const MAX_QUOTES = 3

/** Длина одной цитаты-основания и превью в подсказке модели. */
export const QUOTE_MAX_LENGTH = 220
