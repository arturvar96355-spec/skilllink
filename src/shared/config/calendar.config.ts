/**
 * Подписка на календарь сроков и встреч (.ics, решение 105).
 */
export const CALENDAR_FEED = {
  /**
   * Больше событий в ленте не бывает. У сотрудника их десятки; предел нужен,
   * чтобы ответ оставался небольшим, если назначить одного человека на всё:
   * лента собирается при каждом обновлении календаря (приложения спрашивают её
   * сами, раз в несколько часов), и выгружать тысячи строк на каждый запрос
   * сервер на 2 ГБ не должен. При переполнении остаются события, ближайшие
   * к сегодняшнему дню, — дальние прошлые и дальние будущие уходят первыми.
   */
  maxEvents: 500, // TEMP
  /**
   * Прошедшие встречи — только за последние N дней: календарю нужна история
   * недавней работы, а не архив за годы. Сроки этапов не ограничены: незакрытый
   * этап с прошедшим сроком — это просрочка, её прятать нельзя.
   */
  pastMeetingsDays: 60, // TEMP
  /**
   * Длительность встречи в календаре. У встречи в системе есть только начало;
   * без длительности календарь покажет её точкой нулевой длины.
   */
  meetingDurationMinutes: 60, // TEMP
  /** Подсказка приложению, как часто перечитывать ленту (REFRESH-INTERVAL). */
  refreshIntervalHours: 1, // TEMP
} as const
