/**
 * За сколько дней назад в ленту попадают изменения, сделанные другими людьми.
 * Дольше — лента превращается в архив, короче — после выходных человек
 * не увидит, что случилось в пятницу.
 */
export const NOTIFICATION_WINDOW_DAYS = 14 // TEMP

/** Больше одной ленты за раз фронту не нужно: это выпадающий список, не страница. */
export const NOTIFICATION_MAX_LIMIT = 50
export const NOTIFICATION_DEFAULT_LIMIT = 20
