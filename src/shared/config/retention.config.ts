/**
 * Сроки хранения данных журнала — docs/PRIVACY.md, раздел «Сроки хранения».
 *
 * Применяет их `npm run db:retention` (scripts/retention.ts): без флага только
 * показывает, что будет сделано, с `--apply` — делает. Запуск по расписанию
 * настраивает владелец сервера (docs/DEPLOY.md). Значения — TEMP: срок утверждает
 * оператор персональных данных приказом, а не код.
 */
export const RETENTION = {
  /** Записи журнала действий старше этого срока удаляются целиком, дней. */
  auditLogDays: 365, // TEMP
  /**
   * Адрес клиента (IP) в записях журнала старше этого срока стирается, дней.
   * IP-адрес — персональные данные; для разбора инцидентов он нужен недолго,
   * а сама запись о действии остаётся до `auditLogDays`.
   */
  clientAddressDays: 90, // TEMP
  /** Сколько записей обрабатывается за один проход — чтобы не держать всё в памяти. */
  batchSize: 500,
} as const
