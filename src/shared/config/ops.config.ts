/**
 * Нормативы эксплуатации: оповещения владельцу из приложения (решение 118).
 */

export const OWNER_ALERT = {
  /** Одно и то же событие с тем же ключом — не чаще раза в столько. */
  dedupeMs: 15 * 60_000,
  /** Дольше отправка не ждёт: оповещение не должно держать ни запрос, ни процесс. */
  timeoutMs: 5_000,
  /** Сколько пар «событие + ключ» помнить: память процесса не резиновая. */
  maxRemembered: 1000,
} as const

/**
 * Пороги «массовых» событий: одиночное — норма, всплеск — повод посмотреть.
 * Окно скользящее, счёт в памяти процесса.
 */
export const OWNER_ALERT_BURSTS = {
  /** Проверка «не робот» потребовалась столько раз за окно по всем учётным записям. */
  captcha: { threshold: 20, windowMs: 10 * 60_000 }, // TEMP
  /** Один пользователь выгрузил столько файлов за окно. */
  exportsPerUser: { threshold: 10, windowMs: 10 * 60_000 }, // TEMP
} as const
