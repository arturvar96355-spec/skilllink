/**
 * Параметр адреса входа: «cookie сессии есть, но сервер её не принял».
 *
 * Middleware видит только наличие cookie и отправляет вошедшего со страницы
 * входа на главную. Когда cookie остался, а пользователя за ним нет — после
 * перезаливки демо-данных пользователи создаются заново, — главная получала
 * 401 и уводила на вход, вход возвращал на главную, и экран оставался пустым.
 * Войти можно было, только почистив cookie. С этим параметром middleware
 * страницу входа пропускает (решение 48).
 *
 * Отдельный модуль без зависимостей: его читают и middleware в edge-среде,
 * и клиентский код.
 */
export const REAUTH_PARAM = 'reauth'
