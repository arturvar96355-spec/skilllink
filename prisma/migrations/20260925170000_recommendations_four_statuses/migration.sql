-- Рекомендации — четыре статуса: Новая → В работе → Выполнена / Отклонена (решение 98).
-- «Принята» больше не используется: принятые рекомендации снова открыты — «В работе».
-- Значение перечисления в базе остаётся (удаление значения enum в PostgreSQL —
-- пересоздание типа); появиться снова ему не даёт таблица переходов и db:verify.
UPDATE "recommendations"
   SET "status" = 'IN_PROGRESS',
       "resolved_at" = NULL,
       "resolved_by_id" = NULL
 WHERE "status" = 'ACCEPTED';
