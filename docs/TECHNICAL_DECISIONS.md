# TECHNICAL_DECISIONS.md — принятые технические решения

Журнал решений. Каждая запись: что решено, почему, какие были альтернативы, что это ограничивает.
Решения из раздела «Принятые решения» в `CLAUDE.md` здесь не пересказываются — только то,
что появилось в ходе реализации.

---

## 1. ТЗ Tech Lead отсутствует

**Дата:** 21.09.2026

`CLAUDE.md` называет `docs/tz/tech-lead-tz.md` одним из источников требований.
Этого файла нет ни в репозитории, ни среди переданных материалов.

**Решение.** Работаем по ТЗ продукта (`docs/tz/skilllink-tz.md`), концепции и разделу
«Принятые решения» в `CLAUDE.md` — там прямо сказано, что этот раздел главнее остальных
источников.

**Что это ограничивает.** Структура документов `PROJECT_AUDIT.md` и `DESIGN_INTEGRATION.md`
восстановлена по смыслу, а не по разделам отсутствующего ТЗ. Если файл появится, документы
нужно сверить с ним.

---

## 2. Проект создан с нуля

**Дата:** 21.09.2026

Раздел «Расхождения стартового каркаса с ТЗ» в `CLAUDE.md` описывает существующий код: модель
`Program`, поле `parallelStreams`, сущности `Vacancy` и `VacancySkill`, каталог `src/lib`,
эндпоинты `/api/analytics/market-demand` и `/api/analytics/skill-gap`. Этого кода передано
не было.

**Решение.** Проект написан с нуля сразу по ТЗ. Все перечисленные расхождения учтены как
требования, а не как миграция: `EducationalProgram`, `groupCount`, `MarketDemand`,
`src/modules` + `src/shared`, `/api/skills/demand`, `/api/skills/gaps`, код ошибки в ответе.

**Что это ограничивает.** На этом шаге фронт существовал только в виде макетов
дизайн-системы; страницы в `src/app` были заглушкой (интерфейс собран позже, решение 41).

---

## 3. Prisma 7 с драйверным адаптером вместо Prisma 6

**Дата:** 21.09.2026

В Prisma 7 свойство `url` в блоке `datasource` больше не поддерживается: строка подключения
задаётся в `prisma.config.ts`, а клиент работает через драйверный адаптер.

**Решение.** Взята Prisma 7.10 с `@prisma/adapter-pg`. Строка подключения — в `prisma.config.ts`
из `DATABASE_URL`, клиент создаётся в единственном месте (`src/shared/db/prisma.ts`).

**Альтернатива.** Остаться на Prisma 6 с привычным синтаксисом. Отклонено: начинать новый
проект на предыдущем мажоре означает запланировать миграцию на ближайшие месяцы.

**Новые зависимости и зачем.** `@prisma/adapter-pg` и `pg` — требование Prisma 7, без них
клиент не подключится. Типы `@types/pg` отдельно не ставятся: их приносит
`@prisma/adapter-pg`, а сам проект `pg` напрямую не импортирует.

---

## 4. Next.js зафиксирован на версии 15

**Дата:** 21.09.2026

`npm install next` ставит 16-ю версию. Целевой стек концепции и `CLAUDE.md` — Next.js 15
(App Router).

**Решение.** Версия зафиксирована точно: `next@15.5.25`. Менять мажор без согласования
с фронтом нельзя — это их среда тоже.

---

## 5. Zod 4 с русской локалью

**Дата:** 21.09.2026

Контракт требует тексты ошибок на русском. Стандартные сообщения Zod — английские.

**Решение.** В `src/shared/zod.ts` один раз вызывается `z.config(ru())` из `zod/locales`.
Все модули импортируют `z` оттуда, а не напрямую из `zod`. Сообщения для ключевых полей
дополнительно заданы вручную по-русски.

---

## 6. Контактное лицо вынесено в отдельную таблицу

**Дата:** 21.09.2026

В разделе 11 ТЗ у сущности `University` контактов нет, в разделе 7.3 контактное лицо
перечислено как поля карточки.

**Решение.** Заведена таблица `contacts` со ссылкой на вуз и флагом `is_primary`. В карточке
вуза отдаётся `primaryContact` плюс полный список.

**Почему.** Контактов у вуза бывает несколько, и на них ссылаются участники встреч
(`meeting_participants`). Держать их полями вуза означало бы дублировать данные.

**Согласовать с Тиграном.**

---

## 7. Отмена этапа требует комментария на любом этапе

**Дата:** 21.09.2026

Решение 4 в `CLAUDE.md` говорит про этап 5: «можно отменить с комментарием "не требуется"».
Про остальные этапы условие не задано.

**Решение.** Комментарий требуется при отмене **любого** этапа.

**Почему.** Отмена — необратимое для менеджера действие (переоткрыть может только ADMIN),
и оно попадает в историю. История без основания бесполезна.

**Что это ограничивает.** Фронт обязан запрашивать комментарий в диалоге отмены, иначе
получит `VALIDATION_ERROR` 422.

---

## 8. Контрольный этап 14 исключён из процента выполнения

**Дата:** 21.09.2026

Решение 2 в `CLAUDE.md` описывает автоматический пересчёт этапа 14, но не говорит, входит ли
он в прогресс.

**Решение.** `progress.totalStages` равен 13. Этап 14 в знаменатель не входит.

**Почему.** Этап 14 — производная от остальных. Включив его, мы считали бы одно и то же
дважды: прогресс никогда не достигал бы 100 %, пока система сама не закроет контроль.

---

## 9. Название этапа хранится копией

**Дата:** 21.09.2026

**Решение.** `workflow_stages.title` заполняется из `WORKFLOW_STAGES` в момент создания связки
и дальше живёт в базе.

**Почему.** Переименование этапа в конфиге не должно задним числом менять историю уже
пройденных связок. Номер этапа остаётся ключом, название — снимком на момент создания.

---

## 10. Рейтинг считается в приложении, а не в СУБД

**Дата:** 21.09.2026

Концепция обещает «аналитические показатели рассчитываются представлениями и агрегирующими
запросами на стороне СУБД».

**Решение.** На MVP рейтинг и skill gap считаются в `src/modules/analytics/rating.ts` и
`src/modules/skills/skills.rules.ts` — чистыми функциями, покрытыми тестами.

**Почему.** Формулы ещё не утверждены (все веса помечены TEMP). Менять коэффициент в конфиге
дешевле, чем переписывать представление и миграцию. Чистые функции проверяются тестами
без поднятия базы.

**Когда пересмотреть.** При росте объёма данных расчёт переносится в представления PostgreSQL
без изменения контракта API. Обсудить с Тиграном.

---

## 11. Нормирование внутри выборки

**Дата:** 21.09.2026

**Решение.** Рейтинг нормируется внутри выборки ответа; спрос на навыки — по всей выборке
периода, а не по отфильтрованной странице.

**Почему.** Для спроса иначе масштаб зависел бы от фильтров и значения нельзя было бы
сравнивать между запросами.

**Что это ограничивает.** Баллы рейтинга сравнимы только внутри одного ответа. Это написано
в поле `explanation` каждой записи, фронт обязан это показывать.

---

## 12. Мягкое удаление вместо физического

**Дата:** 21.09.2026

**Решение.** Вузы и программы архивируются (`archived_at`), связи на них — `RESTRICT`.
Архивировать вуз с незакрытыми связками нельзя — `CONFLICT` 409.

**Почему.** Сотрудничество длится годами, история — основная ценность системы.

---

## 13. TypeScript: strict без noUncheckedIndexedAccess

**Дата:** 21.09.2026

**Решение.** `strict: true`. Флаг `noUncheckedIndexedAccess` не включён.

**Почему.** `strict` покрывает требование `CLAUDE.md`. `noUncheckedIndexedAccess` на этом
объёме кода даёт много шума в местах, где индекс заведомо корректен.

**Когда пересмотреть.** Можно включить перед сдачей, если будет время на правки.

---

## 14. Postgres 16 вместо Postgres 17

**Дата:** 21.09.2026

На машине разработчика уже работал `postgresql@16`, занимая порт 5432.

**Решение.** Проект использует уже запущенный Postgres 16. В `docker-compose.yml` также
`postgres:16-alpine`, чтобы локальная и контейнерная среда совпадали.

**Почему.** Ничего из используемого не требует 17-й версии, а два сервера на одном порту —
источник путаницы.

---

## 15. Пустое тело PATCH отклоняется

**Дата:** 21.09.2026

При реализации обнаружено: `.partial()` в Zod **не снимает** `.default()`. Схема изменения,
построенная из схемы создания, на пустом теле возвращала `{ status: "NEW" }` и молча меняла
статус записи.

**Решение.** Для вузов, программ и связок заведены отдельные наборы полей без значений
по умолчанию. Схема создания добавляет `.default()` поверх, схема изменения строится из чистой
базы. Поведение закреплено тестами в `universities.test.ts`, `programs.test.ts`,
`cooperation.test.ts`.

---

## 16. Сортировка с `nulls: 'last'` только для nullable-полей

**Дата:** 21.09.2026

При прогоне сквозного сценария выяснилось: Prisma отклоняет `orderBy: { field: { sort, nulls } }`
для полей, которые не могут быть NULL. Список связок падал с 500.

**Решение.** В `shared/http/pagination.ts` добавлен `buildOrderBy(sort, nullableFields)`.
Каждый репозиторий перечисляет свои nullable-поля: показатели набора у программ, контрольные
даты у связок. В сквозной сценарий добавлены проверки сортировки по обоим видам полей.

---

## 17. Интеграционный слой пока не создан

**Дата:** 21.09.2026

**Решение.** Каталог `src/integrations` не заведён: по приоритетам это P1, а P0 закрывается
первым. Переменные окружения (`MARKET_DATA_PROVIDER`, `LMS_*`, `SITE_*`) уже описаны в
`.env.example` и зарезервированы.

**Что это ограничивает.** Пока рыночные данные приходят только из таблицы `market_demand`,
заполняемой seed-скриптом.

---

## 18. Добавлен модуль `portal`

**Дата:** 21.09.2026

В списке модулей `CLAUDE.md` кабинета представителя вуза нет, хотя решения 9 и 10 его
описывают.

**Решение.** Заведён модуль `src/modules/portal` с собственными схемами, правилами,
репозиторием и сервисом.

**Почему.** Кабинет — другой пользовательский сценарий: другой состав полей, другие права,
другой набор действий. Размазав его по `universities` и `programs`, мы получили бы сервисы,
которые обслуживают двух разных пользователей и в каждой функции проверяют, кто пришёл.

**Согласовать с Артуром** как расширение списка модулей.

---

## 19. Комментарий сотрудника к рекомендации хранится отдельно

**Дата:** 21.09.2026

При реализации обнаружено: если писать комментарий сотрудника в поле `justification`,
обоснование системы затирается и причина появления рекомендации теряется навсегда.

**Решение.** Добавлено поле `recommendations.resolution_comment`. `justification` —
обоснование системы, `resolution_comment` — решение человека.

**Изменение схемы:** миграция `20260921074512_recommendation_resolution_comment`.
**Согласовать с Тиграном.**

---

## 20. Застой связки считается по любому незакрытому этапу

**Дата:** 21.09.2026

Первая версия правила `cooperation.stalled` срабатывала только на этапе в статусе
`NOT_STARTED`. На демонстрационных данных оно не сработало ни разу: связка, которая месяц
висит на начатом этапе, под правило не попадала.

**Решение.** Правило смотрит на любой незакрытый текущий этап и подбирает формулировку под
его статус. Застой на заблокированном этапе получает повышенный приоритет: блокировка,
которую месяц никто не снимает, — не рабочее состояние.

---

## 21. Просрочка и застой по одной связке не дублируются

**Дата:** 21.09.2026

**Решение.** Если по связке уже сформирована рекомендация о просроченном этапе, рекомендация
о застое не создаётся.

**Почему.** Это две карточки об одной проблеме. Список приоритетных действий должен отвечать
на вопрос «чем заняться», а не показывать одну связку дважды.

---

## 22. Демонстрационные заявки согласованы с показателем

**Дата:** 21.09.2026

При проверке кабинета вуза выяснилось: `applicationCount` в демо-данных проставлялся вручную
и не совпадал с суммой записей в `applications`. Первая же поданная заявка запускала пересчёт,
и показатель на глазах менялся с 420 на 120.

**Решение.** Seed создаёт заявки ровно на то количество, которое стоит в показателе, и в конце
сверяет сумму. Расхождение роняет загрузку данных с явной ошибкой.

**Почему.** Решение 9 объявляет заявки источником истины для `applicationCount`. Набор данных,
который этому противоречит, ломает демонстрацию в самый неудобный момент.

---

## 23. `next typegen` перед проверкой типов

**Дата:** 21.09.2026

Next.js генерирует типы маршрутов в `.next/types`, и `tsc` их проверяет. Пока сгенерированные
типы не обновлены, новые маршруты дают ложные ошибки, а ошибки в старых — не видны.

**Решение.** `npm run typecheck` выполняет `next typegen && tsc --noEmit`.

---

## 24. Пустое тело запроса допустимо там, где все поля необязательны

**Дата:** 21.09.2026

**Решение.** Добавлен `parseOptionalBody`. Используется в загрузке рыночных данных и
подтверждении получения материалов.

**Почему.** Требовать от клиента присылать `{}` ради формальности — лишняя придирка,
которая всплывёт на стороне фронта как непонятная ошибка 422.

---

## 25. Добавлен модуль `audit`

**Дата:** 21.09.2026

`CLAUDE.md` перечисляет `AuditLog` среди сущностей, но модуля для него не задаёт.

**Решение.** Заведён `src/modules/audit` с двумя разными ответами на два разных вопроса:
`GET /api/audit` — технический журнал для администратора, `GET /api/universities/:id/events` —
лента событий вуза.

**Почему лента строится не из журнала.** Пункт 7.3 ТЗ требует «Последние события» —
это рассказ о работе с вузом. Журнал хранит действия системы (`stage.status.change`,
идентификаторы, служебный payload), и показывать его пользователю бессмысленно. Лента
собирается из доменных данных: истории этапов, истории документов, встреч и заявок.

**Что это ограничивает.** Лента показывает только то, что оставляет след в доменных
таблицах. Действия без такого следа (например, правка описания вуза) в неё не попадут.

---

## 26. NextAuth.js версии 5 (beta) вместо стабильной четвёртой

**Дата:** 21.09.2026

Концепция обещает «аутентификация и сессии через NextAuth.js на токенах JWT». Стабильная
линия next-auth 4 рассчитана на Pages Router; с App Router работает версия 5, которая пока
в статусе beta.

**Решение.** Взята `next-auth@5.0.0-beta.32`, версия зафиксирована точно — без диапазона.

**Почему это приемлемо.** Весь доступ к пользователю идёт через единственную функцию
`getCurrentUser()`. Если бета подведёт, её реализация меняется в одном файле, а модули
не трогаются — ровно то, ради чего решение 9 и вводило эту функцию.

**Новые зависимости и зачем.** `next-auth` — обещано концепцией. `bcryptjs` — хеширование
паролей; выбран вместо `bcrypt`, потому что не требует нативной сборки и одинаково ставится
на машинах команды.

---

## 27. Демо-режим входа остаётся, но выключается переменной

**Дата:** 21.09.2026

Решение 9 предполагает, что на P1 mock-авторизация просто исчезает. На защите проекта
переключение ролей в один клик — половина демонстрации: кабинет вуза, права аналитика,
скрытые комментарии показываются именно так.

**Решение.** Настоящий вход по паролю работает всегда. Дополнительно, пока включён
`DEMO_AUTH_ENABLED`, действует переключение пользователя cookie `skilllink_user`.
По умолчанию режим выключен только в продакшене.

**Что это ограничивает.** Включённый демо-режим — это обход авторизации. Настоящая сессия
имеет приоритет над cookie, но сам режим в промышленном контуре держать нельзя. Записано
отдельной строкой в [SECURITY_LIMITATIONS.md](SECURITY_LIMITATIONS.md).

---

## 28. Текущий пользователь переехал с `/api/auth/me` на `/api/me`

**Дата:** 21.09.2026

NextAuth занимает всё пространство `/api/auth/*` перехватывающим маршрутом.

**Решение.** Собственный эндпоинт текущего пользователя перенесён на `/api/me`.

**Почему сразу.** Статический сегмент в Next перекрывает перехватывающий, так что технически
соседство работало бы. Но держать свой маршрут внутри чужого пространства имён — это мина
на будущее. Фронт к эндпоинту ещё не подключался, поэтому переезд ничего не ломает.

**Затрагивает фронт:** `GET /api/auth/me` → `GET /api/me`.

---

## 29. Клиент Prisma создаётся лениво

**Дата:** 21.09.2026

Клиент создавался прямо при импорте модуля и падал без `DATABASE_URL`. Из-за этого
юнит-тест чистой функции падал, если в её файле был хоть один транзитивный импорт,
ведущий к базе.

**Решение.** `prisma` — прокси, который создаёт клиент при первом обращении к свойству.
Методы привязываются к экземпляру, поведение для вызывающего кода не меняется.

**Почему это важнее удобства.** Модуль, который падает на импорте, делает нетестируемым
всё, что его касается. Проблема всплыла на первом же тесте настроек авторизации.

---

## 30. Текст документа хранится, а файл — нет

**Дата:** 21.09.2026

Концепция обещает «генерацию пакета документов из шаблонов с автоподстановкой реквизитов».
Решение 14 при этом ограничивает документы метаданными и ссылкой.

**Решение.** В таблицу `documents` добавлены `content` (собранный текст) и `template_key`.
Файлы по-прежнему не хранятся и не загружаются — это остаётся P2.

**Почему.** Без хранения текста генерация не оставляет после себя ничего: пользователь
увидел бы документ один раз в ответе и потерял его. Текст — это не файл, и ограничение
решения 14 он не нарушает.

**Изменение схемы:** миграция `20260921082617_document_template_content`.
**Согласовать с Тиграном.**

---

## 31. Недостающий реквизит заменяется видимым прочерком

**Дата:** 21.09.2026

**Решение.** Если реквизита нет в данных, в текст подставляется `__________`, а сам реквизит
возвращается в списке `missing`.

**Почему.** Пустая строка дала бы документ, который выглядит заполненным. Такой подпишут
не глядя. Явный прочерк заставляет дописать — и в ответе сразу видно, каких данных не хватает
в карточках вуза и программы.

---

## 32. Собранный из шаблона документ не требует ссылки на файл

**Дата:** 21.09.2026

Правило «нельзя отправить на согласование документ без `fileReference`» блокировало
сгенерированный пакет: текст есть, файла нет.

**Решение.** Условие смягчено: на согласование уходит документ, у которого есть **либо**
ссылка на файл, **либо** текст из шаблона.

**Почему.** Собранный текст и есть документ. Требовать вдобавок файл — значит заставлять
менеджера выгружать текст вручную только ради формальной проверки.

---

## 33. Спецификация OpenAPI собирается из Zod-схем

**Дата:** 21.09.2026

Концепция обещает: «интеграционные интерфейсы описаны по спецификации OpenAPI».

**Решение.** Реестр эндпоинтов в `src/shared/openapi/registry.ts` ссылается на те же
Zod-схемы, которыми маршруты проверяют вход. Документ собирается через `z.toJSONSchema`
командой `npm run openapi` и отдаётся живым сервером по `GET /api/openapi.json`.

**Альтернатива.** Написать спецификацию руками. Отклонено: рукописный документ расходится
с кодом на второй неделе, и хуже отсутствующей спецификации только неверная.

**Что это ограничивает.** Схемы **ответов** описаны обобщённо. DTO заданы типами TypeScript
в `shared/contracts`, а не схемами времени выполнения; выводить из них JSON Schema означало бы
дублировать описания. Точные поля ответов остаются в `API_CONTRACT.md`.

**Страховка от расхождения.** Тест обходит `src/app/api`, собирает экспортированные методы
и падает, если маршрут не описан в реестре — или описан, но не существует.

---

## 34. Версии трёх вложенных зависимостей подменены

**Дата:** 22.09.2026

`npm audit` находил шесть уязвимостей, пять — уровня high. Все шесть сидят
в инструментах, а не в коде, который обрабатывает запросы пользователей:

| Пакет | Кто тянет | Почему у нас не достижимо |
| --- | --- | --- |
| `mysql2` 3.15.3 | CLI Prisma — для работы с MySQL | У нас PostgreSQL, этот код не запускается |
| `deepmerge-ts` 7.1.5 | `@prisma/config` — загрузка `prisma.config.ts` | Объединяет только наш собственный конфиг |
| `postcss` 8.4.31 | Next.js — сборка CSS | Обрабатывает только наш CSS и только при сборке |

`npm audit fix --force` предлагал откат Prisma на 6-ю версию и переход на Next 16 —
оба ломающие.

**Решение.** В `package.json` — `overrides`, и только внутри тех пакетов, которые
тянут уязвимую версию: `prisma → mysql2 ^3.24.4`, `@prisma/config → deepmerge-ts ^8.0.2`,
`next → postcss ^8.5.28`. Первая и третья — та же мажорная версия. Для `deepmerge-ts`
исправление есть только в 8-й; Prisma берёт из пакета одну функцию, `deepmerge`,
и в 8-й она на месте.

**Проверено:** `npm audit` — 0 уязвимостей; `prisma validate` и `generate`, миграции
на пустой базе, промышленная сборка, 556 тестов, сквозной сценарий и пробник.

**Когда убрать.** Когда Prisma и Next сами поднимут эти версии — подмена станет
не нужна; `npm ls mysql2 deepmerge-ts postcss` покажет, что они совпали.

---

## 35. Русская сортировка задаётся на колонке, а не на базе

**Дата:** 22.09.2026

Кириллица сортировалась неправильно и по-разному: на macOS — почти случайно,
в `postgres:16-alpine` — по кодам символов.

**Решение.** `COLLATE "ru-x-icu"` на двенадцати колонках, по которым API сортирует
списки (миграция `20260922073000_russian_collation`).

**Альтернативы.**

- Локаль при создании базы (`--locale-provider=icu`). Отклонено: работает только
  там, где базу создали правильно, — у каждого разработчика своя, и ошибка
  возвращается незаметно.
- Сортировать в приложении через `Intl.Collator`. Отклонено: не работает
  с постраничной выдачей — сортировать нужно до `LIMIT`, то есть в базе.
- `COLLATE` в самих запросах. Отклонено: Prisma не умеет задавать его в `orderBy`,
  пришлось бы переписать списки на сырой SQL.

**Страховка.** Тест падает, если у колонки пропадёт сортировка или появится новое
поле сортировки; пробник проверяет порядок на живой базе.

**Статус.** Согласовано: Тигран слил Pull Request #1 22.09.2026.

---

## 36. За прокси с HTTPS задаётся `AUTH_URL`

**Дата:** 22.09.2026

**Что нашлось.** Вход через имитацию HTTPS-прокси (заголовки `Host`, `X-Forwarded-Proto`,
`X-Forwarded-Host` как у Caddy) проходил, cookie получали приставку `__Secure-`,
но после входа NextAuth отправлял пользователя на **`https://localhost:3100`**.

Причина в Next.js: при `next start` он собирает адрес запроса из собственного имени
(`localhost:порт`), а заголовок `Host` не использует. NextAuth строит все переходы
от этого адреса. Протокол при этом берётся из `X-Forwarded-Proto` — поэтому снаружи
всё выглядело почти правильно.

**Решение.** Публичный адрес задаётся переменной `AUTH_URL` — её читает сам next-auth.
В контейнере — через `DOCKER_AUTH_URL`, по той же причине, что и `DOCKER_AUTH_SECRET`.
Пустое значение безопасно: next-auth считает его незаданным (проверено запуском).

**Проверено:** без `AUTH_URL` — переход на `localhost`; с ним — на адрес сайта.
Сессия видна и снаружи через прокси, и изнутри сервера (`apiFetch` ходит
на `127.0.0.1` без заголовков прокси) — в обоих случаях.

**Попутно исправлено.** README и HANDOFF велели `export AUTH_SECRET=...` перед
`docker compose`, а контейнер с прошлого исправления читает `DOCKER_AUTH_SECRET`:
по инструкции секрет в контейнер не попадал. Теперь это ловит тест
`src/shared/config/compose-docs.test.ts`.

---

## 37. Уведомления собираются из данных, прочитанность хранит фронт

**Дата:** 22.09.2026

Дизайн-система ставит колокольчик с лентой в шапку каждой страницы. Уведомления
были P2 с невыбранным каналом — дизайн выбрал его сам: лента в интерфейсе.
**Одобрено Артуром 22.09.2026** (`DECISIONS.md`, решение 14).

**Решение.** `GET /api/notifications` собирает ленту на лету: сроки своих этапов,
изменения других людей в своих связках и документах, открытые рекомендации высокого
приоритета. Представителю вуза — только движение по его вузу.

**Почему без таблицы уведомлений.** Лента из данных не расходится с ними: закрытый
этап перестаёт быть «просроченным» и в ленте. Таблица требовала бы изменения схемы,
согласования с Тиграном и фоновой рассылки — за неделю до защиты.

**Прочитанность — на фронте**: время последнего просмотра в `localStorage`,
передаётся в `since`. Ограничение честное: на другом устройстве лента снова
непрочитанная. Нужна общая — это одна колонка `users.notifications_seen_at`.

**Время события — когда оно случилось**, а не когда посчитано: иначе просрочка
выглядела бы новой при каждом запросе, и «отметить прочитанным» не работало бы.

**Проверено:** просроченных в ленте столько же, сколько в личной статистике;
каждая ссылка открывается тем, кому пришло уведомление (контрольный опыт: подмена
id — пробник поймал 5 из 20); в ленте представителя вуза нет внутреннего.

Новая пометка TEMP: `NOTIFICATION_WINDOW_DAYS = 14` — за сколько дней показываются
изменения других людей.

---

## 38. Глобальный поиск собран из списков

**Дата:** 22.09.2026

Окно поиска в стиле Spotlight ищет по вузам, программам, связкам, продуктам, навыкам
и документам. Без отдельного эндпоинта фронт отправлял бы шесть запросов на каждую букву.

**Решение.** `GET /api/search` вызывает те же списки, что открывает фронт, и группирует
результаты. Своего запроса к базе нет намеренно: списки уже проверяют права
и ограничивают представителя вуза его вузом. Свой запрос пришлось бы защищать
заново, и одно упущение показало бы чужой вуз в строке поиска.

---

## 39. Рекомендация называет свой объект

**Дата:** 22.09.2026

`target.label` брался из заголовка рекомендации с пометкой «имя уже вшито в заголовок».
Для большинства правил это неправда: «Просрочен этап 6: Подписание документов» не говорит,
какой вуз, а две «Нет данных по программе» подряд неотличимы. Нашлось на ленте
уведомлений, но касалось и страницы рекомендаций, и дашборда.

**Решение.** Имя берётся из самого объекта — «СПбГУТ — Программная инженерия»,
«Компьютерная безопасность · УрФУ», «Навык «Kubernetes»» — одним запросом на тип
для всей страницы. Заголовок остаётся запасным вариантом, если объект удалён.

**Затрагивает фронт:** значение `target.label` изменилось — теперь это имя объекта,
а не повтор заголовка. Форма поля та же.

---

## 40. Исчерпанные попытки входа называют себя

**Дата:** 22.09.2026

Было: после пяти неудач подряд учётная запись закрывалась на 15 минут, а отказ выглядел
ровно как «неверный пароль». Человек вводил правильный пароль, получал отказ и решал,
что забыл его. На защите это 15 минут растерянности при переключении на представителя вуза.

Одинаковый ответ был выбран сознательно — чтобы по нему нельзя было узнать, какие
адреса заблокированы. Пересмотрено: блокируется и несуществующий адрес, поэтому код
не выдаёт, есть ли учётная запись; а тот, кто исчерпал попытки, и так знает об этом.

**Решение.** Код `too_many_attempts` в ответе входа (`CredentialsSignin` с собственным
`code`). «Нет пользователя» и «неверный пароль» по-прежнему неразличимы.

**Проверено** на живом сервере: пять неудач по несуществующему адресу — `credentials`,
шестая — `too_many_attempts`; обычная ошибка пароля — `credentials`; верный пароль входит
и обнуляет счётчик. Проверка осталась в пробнике.

**Затрагивает фронт:** экран входа должен различать два кода — `API_CONTRACT.md`, «Вход».

---

## 41. Интерфейс собран в том же приложении

**Дата:** 22.09.2026

По первоначальному плану фронт собирался отдельно, по дизайн-системе команды
(шесть документов в `docs/design/`). Чтобы к дедлайну система была цельной, интерфейс
решили собрать в этом же приложении по той же дизайн-системе.

**Решение.** Интерфейс живёт в этом же приложении Next.js, а не отдельным проектом:

- `src/ui/` — дизайн-система: примитивы, таблица, состояния, всплывающие окна, каркас,
  поиск, уведомления, хуки и обращения к API. Страницы берут всё оттуда и своих кнопок,
  карточек и таблиц не заводят.
- `src/app/(app)/` — внутренние страницы внутри общего каркаса;
  `src/app/(auth)/login` — вход.
- Оформление — переменные в `src/app/globals.css` плюс CSS Modules. Никакого Tailwind
  и никаких UI-библиотек: значения дизайн-системы заданы точно (цвета, радиусы, размеры
  шрифта), и слой утилит между ними и разметкой ничего не добавляет, зато добавляет
  зависимость и конфигурацию, которую потом некому поддерживать.
- Иконки — свой набор линейных значков в `src/ui/primitives/Icon.tsx`. Библиотека икон
  ради двадцати начертаний — лишняя зависимость (CLAUDE.md).
- Данные — только через API теми же маршрутами, что описаны в контракте. Прямых обращений
  к сервисам и Prisma из страниц нет: фронт остаётся отделяемым.

**Почему не отдельный проект.** Отдельный фронт — это второй репозиторий, вторая сборка,
CORS, свои переменные окружения и своя выкладка. За неделю до защиты это цена без выгоды:
страница и эндпоинт, который её кормит, проверяются одной командой и выкатываются одним
образом.

**Что это меняет в правилах.** Пункт «не переделывать вёрстку фронта» остаётся в силе
для экранов дизайнеров команды: они встают рядом и не переписываются. Дизайн-система
общая — `docs/design/`.

**Проверка.** `npm run typecheck`, `npm test` (тесты меню и форматирования — новые),
`npm run build` и живой проход по экранам в браузере.

## 42. Страницы проверяют сессию куки, а права — сервер

**Дата:** 22.09.2026

`middleware.ts` отправляет на `/login` того, у кого нет куки сессии. Подпись токена
при этом не проверяется: middleware выполняется в edge-среде, где нет ни bcrypt,
ни Prisma, и полноценная проверка потребовала бы отдельной сборки конфигурации входа.

Это не ослабляет защиту: страницы не содержат данных — всё приходит запросами к API,
где каждый маршрут проверяет сессию и права по-настоящему. С подделанной кукой
откроется пустой каркас, который тут же получит 401 и вернёт на вход.

Маршруты `/api/**` из middleware исключены намеренно: они обязаны отвечать кодом 401,
а не перенаправлением на HTML-страницу, иначе любой запрос из интерфейса ломается.

---

## 43. Страницы не кэшируются браузером без проверки

**Дата:** 23.09.2026

Сергей открыл стенд после выкладки интерфейса и увидел старую заглушку
«SkillLink API». Браузер был ни при чём: Next отдаёт готовые страницы
с `Cache-Control: s-maxage=31536000`. Этот заголовок адресован промежуточным
кэшам, но браузер, не найдя `max-age`, выбирает срок хранения сам — Safari
выбрал «долго» и показывал страницу, которой в приложении уже нет.

На защите это стоило бы показа: у всех, кто открывал стенд раньше, осталась бы
старая версия, и никакая выкладка этого бы не изменила.

**Решение.** Страницы и ответы API отдаются с `Cache-Control: no-cache`:
браузер обязан спросить сервер перед показом, а при совпадении ETag получает
пустой ответ 304. Файлы сборки под `/_next/static` кэшируются навсегда — у них
хеш в имени. Правило задано дважды: в `next.config.ts` (чтобы локальный запуск
вёл себя так же) и в Caddyfile стенда (последнее слово за прокси).

**Проверка.** `scripts/deploy/check.sh` смотрит этот заголовок и падает, если
страницы снова начнут кэшироваться без проверки. Проверка написана до
исправления и на тогдашнем стенде падала — это и был тот самый заголовок.

**Что это не чинит.** Копию, уже сохранённую браузером, заголовок не отменяет:
её нужно один раз обновить с очисткой кэша. Предупреждение добавлено
в сценарии показа, пункт подготовки.

---

## 44. Плотность реестров и первый экран главной — по решению Артура

**Дата:** 23.09.2026

Артур решил два продуктовых вопроса: реестр должен выглядеть рабочим инструментом
(20–25 строк на экран), а главная — отвечать «где горит» (проблемы — главный блок,
показатели — одной строкой).

**Плотность.** Строка таблицы — 32 пикселя, содержимое в одну строку: длинное
обрезается многоточием, полное — в подсказке (`CellText`). Раскладка столбцов
фиксированная: иначе запрет переноса растягивал столбец с длинным названием
и выталкивал остальные за край. Подписи над фильтрами скрыты визуально
и оставлены для программ чтения с экрана — их смысл и так читается из подсказки
внутри поля. Итог на 1440×900: 20 строк реестра на экране, на Full HD — 25.

Для этого в ответы API добавлены краткие названия вузов (`universityShortName`
у связок, программ и проблемных связок): полное название в плотной строке
обрезалось на первом слове.

**Главная.** Пять плиток заменены полосой показателей (`KpiStrip`), под ней —
«Требует внимания» на три пятых ширины и приоритетные действия рядом. Строка
проблемы ведёт сразу на этап (`stageId`).

**Найдено по дороге — две неправды на первом экране.**

1. Подпись «5 связок встали» выдавала лимит вывода за число проблем: в демо-наборе
   проблемных этапов двенадцать. Теперь сводка отдаёт `problemStageTotal`,
   главная пишет «12 этапов стоят… показаны 10 самых давних», а пробник сверяет
   это число со списками просроченных и заблокированных этапов (обратный
   эксперимент: сдвиг счётчика на единицу ловится).
2. В реестре связок у текущего этапа значок «просрочен на N дн.» подставлял дни
   до контрольной даты всей связки. У СПбГУТ «Программная инженерия» выходило
   «74 дня» вместо настоящих 58. У текущего этапа теперь свой `daysToDeadline`.

Все изменения API — только новые поля, прежние не менялись.

## 45. Устойчивый порядок списков и подготовка стенда к показу

**Дата:** 23.09.2026

Сверка стенда со сценарием показа (`scripts/demo-check.ts`) нашла две ошибки,
которые проверки до этого не видели.

**Лента рекомендаций шла от неважного к важному.** Страница «Рекомендации»
и вкладка карточки связки запрашивали `sort=priority`. Приоритет — перечисление
`LOW < MEDIUM < HIGH < CRITICAL`, так что по возрастанию первой шла «Связка
без движения 30 дн.» средней важности, а три критичные просрочки стояли внизу.
На четвёртом шаге показа докладчик говорит «лента по важности» — экран показал
бы обратное. Значение вынесено в `RECOMMENDATION_SORT_MOST_IMPORTANT = '-priority'`,
пробник проверяет порядок тем же запросом (обратный эксперимент: с `priority`
проверка падает).

**Страницы теряли строки.** Списки сортировались по одному полю, и строки
с равным значением база отдавала в произвольном порядке. На рабочей базе обход
по страницам терял 6 программ из 44, 6 связок из 40, 8 вузов из 51, а лента
рекомендаций повторяла одни строки и пропускала другие. `buildOrderBy` теперь
добавляет последним ключом `id` (`TIE_BREAKER`), остальные списки с постраничной
выдачей — тоже. Пробник обходит четыре реестра по семь строк и сверяет с одним
запросом; без `id` падают все четыре.

**Демо-данные стареют.** Даты набора отсчитываются от момента заливки,
и через двое суток на главной вместо двенадцати проблемных этапов тринадцать,
через четверо — пятнадцать. Сдвигать сроки в наборе не стали: живые данные
и должны меняться со временем, а задача — показать тот набор, по которому
написан сценарий. Поэтому перед показом стенд перезаливается
(`scripts/deploy/reseed.sh`, без пересборки приложения) и сверяется
со сценарием (`npm run demo:check`) — [DEPLOY.md](DEPLOY.md).

**Образ миграций не обновлялся.** `docker compose run` берёт уже собранный образ.
Образ миграций на стенде был собран при первой выкладке и с тех пор не менялся:
новая миграция не дошла бы до базы, а `SEED=1` залил бы старый набор.
`remote-up.sh` теперь собирает его при каждом развёртывании.

## 46. Длинные списки выбора — с поиском на сервере; Escape — по слоям

**Дата:** 23.09.2026

**Списки обрезались на сотне.** Выпадающие списки вузов (создание связки
и программы, фильтры программ, документов и аналитики), программ и связок
(фильтры документов и аналитики) загружались одним запросом с `pageSize=100`.
Всё дальше сотни пропадало молча: на объёмной базе (1 004 вуза, 6 003 программы,
3 003 связки) связку с вузом № 987 нельзя было создать, а в фильтр документов
попадала одна связка из тридцати.

`RemoteSelect` (`src/ui/data`) берёт первые 50 вариантов, остальное — поиском
по тому же параметру `q`, что у реестров, и честно пишет, сколько показано.
Подпись выбранного значения, не попавшего в выборку, берётся из карточки
(`GET <endpoint>/<id>`). Поле поиска появляется от тринадцати вариантов —
в демо-наборе списки выглядят как раньше. Новых эндпоинтов не понадобилось.

Отвергнуто: поднять `pageSize` до 1000 (API ограничивает сотней, и список
в тысячу строк без поиска всё равно непригоден) и отдельный эндпоинт
«справочник вузов» (тот же список, вторая точка поддержки).

**Escape закрывал всё сразу.** Каждый всплывающий слой слушал клавишу сам,
и одно нажатие в выпадающем списке внутри модального окна закрывало и список,
и окно — форма пропадала со всем введённым. Теперь слои — стопкой, Escape
закрывает верхний (`escape-stack.ts`, модульный тест). Проверено в браузере:
первый Escape закрывает список и возвращает фокус на его кнопку, введённое
на месте; второй — закрывает окно.

**Попутно.** Локальная объёмная база жила без двух последних миграций, и сортировка
по названию на ней была неправильной («Пробная…» между «№ 0» и «№ 1»). На стенде
и в демо-базе миграции применены; объёмная приведена `prisma migrate deploy`.

## 47. Дни — по московскому календарю; в день срока — «сегодня»

**Дата:** 23.09.2026

**Сутки по UTC расходились с датой на экране.** Интерфейс показывает даты по Москве,
а сервер считал дни до срока по суткам UTC. Срок «01.10, 01:00» по Москве — это
30.09, 22:00 по UTC, и 30 сентября днём рядом с датой «01.10» стояло «срок через
0 дн.». `daysBetween` (`src/shared/utils/date.ts`) теперь считает московские сутки:
постоянное смещение +3 часа, перехода на летнее время в Москве нет с 2014 года.
Разница целых суток от этого не меняется, поэтому числа демо-набора те же
(сверка со сценарием — 25 из 25). Модульные тесты на границы суток; с прежним
расчётом по UTC два из них падают.

**«Просрочен на 0 дн.»** Срок этапа — точный момент (начало плюс норматив),
и к вечеру дня срока этап уже просрочен, а календарных дней не прошло. Значок
писал «Просрочен на 0 дн.», главная — «−0 дн.», причина — «Этап просрочен на 0 дн.».
Теперь — «Срок вышел сегодня», в узком столбце «сегодня» (`deadlineBadgeText`,
тест). Момент просрочки не менялся: этап, у которого срок прошёл по часам,
по-прежнему в числе проблемных — иначе главная и списки разошлись бы.

**Попутно — ошибка в контракте.** Пример `daysOverdue` в API_CONTRACT.md был
отрицательным, сервер всегда отдавал положительное число. Интерфейс брал модуль
и ошибки не показывал; исправлен пример и описан знак.

## 48. Устаревшая сессия ведёт на вход, а не в пустой экран

**Дата:** 23.09.2026

**Что было.** Middleware проверяет только наличие cookie сессии и вошедшего
со страницы входа отправляет на главную. Когда cookie остался, а пользователя
за ним нет, главная получала 401 и уводила на `/login`, middleware возвращал
на главную — экран оставался пустым. Войти было нельзя, пока не почистишь
cookie. Так случилось бы утром перед защитой: перезаливка демо-данных создаёт
пользователей заново, и у каждого, кто входил накануне, стенд открылся бы
пустым. Тот же тупик ждал отключённого администратором пользователя.

Локально не воспроизводилось: в режиме разработки включён демо-вход,
и сервер молча подставлял менеджера вместо пропавшего пользователя. Найдено
на боевой сборке с `DEMO_AUTH_ENABLED=false`, как на стенде.

**Что сделано.**

- На 401 `useResource` и `useMutation` вызывают `leaveToLogin()`: снимают сессию
  (`signOut`) и полным переходом открывают `/login?reauth=1&from=<где был>`.
  Действие без сессии тоже уводит на вход: отказ «Требуется вход» в карточке
  этапа читался бы как отказ системы.
- Middleware пропускает `/login?reauth=1` и с cookie — на случай, если снять
  сессию не удалось. Имя параметра — `src/shared/auth/reauth.ts`, общее
  для middleware и клиента.
- Страница входа пишет «Сессия устарела — войдите снова», после входа
  возвращает туда, где человек был.

**Проверено.** Боевая сборка на демо-базе, демо-вход выключен: вкладка вошла,
демо-данные перезалиты, открытие реестра привело на вход с подсказкой, вход
вернул в реестр; нажатие «Начать этап» на открытой до перезаливки карточке
привело на вход, а не к ошибке в карточке. Пробник проверяет, что вход
с `reauth` открывается при старом cookie (обратный эксперимент: без исключения
в middleware — 307), `check.sh` — то же на стенде.

**Попутно.** Сквозной сценарий проверял сработавшие правила рекомендаций
по первой странице в 50 строк. На рабочей базе, где записи копятся от прогонов,
нужная уехала на 53-е место, и проверка падала без ошибки в приложении. Теперь
сценарий читает все страницы, а «повторная генерация не плодит дубликаты»
сравнивает общее число, а не длину страницы, которая при сотне записей
одинакова всегда.

## 49. Пункты контрольной точки не отмечаются до закрытия предыдущих этапов

**Дата:** 23.09.2026. **Требует подтверждения Артура:** меняет пятый шаг показа.

**Противоречие.** Начать этап 7 «Передача учебных материалов, лицензии
и документации», пока не подписан договор (этап 6), нельзя — на этом держится
третий шаг показа: «система не напоминает, она просто не позволяет». Но пункты
его чек-листа отмечались: сотрудник мог отметить «Передана лицензия», пока этап
не начат (запрет был только для завершённых и отменённых этапов), а представитель
вуза в кабинете видел три материала «к подтверждению» и подтверждал их. Проверено
на боевой сборке: подтверждение «Передана лицензия» у не начатого этапа 7 —
ответ 200, отметка сохраняется. Жюри, заглянувшее в чек-лист этапа 7 или в кабинет
вуза на пятом шаге, увидело бы, что «не позволяет» — неправда.

**Предложение.** Отметка пункта — такое же утверждение о сделанной работе, как
начало этапа, поэтому подчиняется тому же правилу. Пока не закрыты этапы до
контрольной точки (6, 7, 11), её пункты не отмечаются — ни сотрудником, ни вузом:
409 с тем же текстом, что у «Начать этап». Снять отметку можно всегда.
Кабинет вуза показывает такие материалы как «Ещё не переданы» с причиной
и не считает их ожидающими подтверждения (`canConfirm`, `lockedReason`
в `PortalMaterialDto`).

**Что меняется в показе.** Пятый шаг: вместо «три материала к подтверждению» —
«три материала ещё не переданы: не закрыт этап 6». Это продолжает историю
третьего шага, а не противоречит ей. `demo:check` сверяет новые числа.

**Отвергнуто.** Запретить отметку пунктов у любого не начатого этапа — шире,
чем требует решение о контрольных точках, и меняет работу с обычными этапами,
которые по решению 5 идут параллельно. Если нужно — отдельное решение.

## 50. Просрочка не вытесняется из ленты уведомлений

**Дата:** 23.09.2026

Решение 37 обещало: «просроченных в ленте столько же, сколько в личной статистике».
Держалось это, пока событий мало. Время просрочки — истёкший срок, то есть прошлое,
а лента обрезалась по времени: свежие изменения выталкивали просрочки из показанной
части. На рабочей базе в ленте из 50 не осталось ни одной из пяти, в колокольчике
(20 строк) — тем более. Пробник это ловил, но только когда база набирала события.

**Решение.** В показанную часть сначала попадают все просрочки, остальное место —
по времени. Порядок внутри ленты прежний, от новых к старым: просрочка не «всплывает»
наверх и не становится непрочитанной заново. Модульный тест: тридцать свежих
изменений и две старые просрочки при `limit` 20 — обе просрочки в ленте (без
исправления тест падает).

**Попутно — пробник на растущей базе.** Проверка обхода реестра вузов по рейтингу
сравнивала все страницы с одной выдачей, а одна выдача ограничена сотней: на рабочей
базе вузов стало 105, и проверка падала без ошибки в приложении. Теперь обход
сверяется с первыми строками выдачи.

## 51. Демонстрационный набор: документы на своих связках, порядок рекомендаций не случаен

**Дата:** 23.09.2026

**Документы и встречи висели на соседних связках.** Набор ссылался на связки
по номеру в списке. 22.09 в начало списка вставили завершённую связку КНИТУ-КАИ,
и всё съехало на одну позицию. У СПбГУТ «Программная инженерия», где подписание
ещё идёт (этап 6, чек-лист 0 из 3), во вкладке «Документы» значилось «Договор
о сотрудничестве — Подписан» — сразу после того, как на показе система отказалась
считать договор подписанным. У связки на этапе 13 договор был «на согласовании»,
а встреча «юрслужба вуза запросила сведения по лицензии» стояла не у МТУСИ, где
этап 7 заблокирован как раз ответом юрслужбы. Теперь документы и встречи ссылаются
на связку по ключу «вуз-программа», неизвестный ключ роняет заливку.

**Порядок рекомендаций при равной важности был случайным.** Его задавало время
создания, а генерация читала связки без сортировки, и три критичные просрочки
создавались в одну миллисекунду. Верхнее действие на главной после перезаливки
могло смениться — сверка со сценарием это поймала. Теперь генерация упорядочивает
черновики (`compareDraftsByImportance`): важность, затем правило — просрочки,
дефициты навыков, остальное, — затем острота: давность просрочки, спрос на навык.
Новые записи получают время создания по этому порядку с шагом в миллисекунду.
Три перезаливки подряд дают один и тот же порядок; модульный тест.

**Что меняется в показе.** Верхнее действие — «Просрочен этап 6: Подписание
документов» у СПбГУТ: самая давняя просрочка, и это та связка, что на шаге 3.
Дефициты — Kubernetes (спрос 0,77) перед PostgreSQL (0,70); раньше было наоборот.
Числа главной не меняются: рекомендации те же, другой только порядок.

## 52. Пределы базы — на входе API, а не в тексте внутренней ошибки

**Дата:** 23.09.2026

Проверка входа знала смысл полей, но не типы колонок. Всё, что проходило Zod
и не помещалось в PostgreSQL, доходило до запроса и отвечало 500 «Внутренняя
ошибка сервера». Нашлось обстрелом API на локальном сервере: мусор в каждый
параметр, путь и тело каждого маршрута.

- **Символ с кодом 0** (`%00`, `\u0000`) PostgreSQL не принимает ни в записи,
  ни в поиске. Давал 500 на двадцати адресах API: поиск `q` в девяти списках, карточки по `id`,
  создание и правку, загрузку CSV, cookie демо-входа; вход по паролю отвечал
  «ошибка конфигурации». Теперь он останавливается там же, где проверяется
  остальное: `parseBody`, `parseOptionalBody`, `parseQuery` — `VALIDATION_ERROR`
  с именем поля; параметры пути — в общей обёртке `handle`, ответ «не найдено»
  (такого идентификатора быть не может); в CSV — «Файл не похож на текстовый CSV»:
  нулевые байты бывают только в двоичных файлах, и .xlsx раньше получал
  «не найдены колонки» при колонках на месте.
- **Количества.** `z.number().int()` пропускает целые до 2^53, колонка `Int` —
  до 2 147 483 647: «Студентов: 99 999 999 999» падало на записи. Количества
  идут через `countSchema()` из `shared/zod`, импорт проверяет тот же предел —
  раньше предпросмотр обещал «Будет создан», а запись падала. `minDaysOverdue`
  без границы давал несуществующую дату — теперь не больше 36 500.
- **Текст ошибки базы уходил наружу.** Импорт писал в `detail` строки сообщение
  Prisma целиком — с вызовом и данными. Теперь наружу — только то, что отдаёт
  общая `toAppError` (её же использует `handle`), остальное — «внутренняя ошибка».
- **Персональные данные в журнале.** Обработчик писал в журнал `error.message`,
  а у Prisma это весь вызов с аргументами: при сбое записи контакта в журнал
  попали бы ФИО, телефон и почта. `describeForLog` оставляет имя ошибки, код
  и последнюю строку — причину. Так же пишет журнал аудита.

Сторож в `storable.test.ts`: целое без `.max()` в схемах модулей роняет тесты.
Контрольный опыт — без исправления падают 8 тестов из 48.

## 53. Вход и границы запроса: возврат, перебор, размер тела

**Дата:** 23.09.2026

Нашлось сквозной проверкой кода по четырём направлениям; каждое место
воспроизведено до исправления.

- **Возврат после входа уводил на чужой сайт.** `from` проверялся условием
  «начинается с `/`», а `//evil.example` и `/\evil.example` браузер читает как
  адрес другого сайта. Ссылка `/login?reauth=1&from=//evil.example` открывала
  вход даже вошедшему — с надписью «Сессия устарела» — и после ввода настоящего
  пароля переводила на чужую страницу. Теперь `safeReturnPath` разбирает адрес
  тем же парсером URL, что и браузер, и пропускает только свой сайт.
- **Ограничение перебора обходилось параллельными запросами.** Неудача
  записывалась после запроса к базе и bcrypt (~50 мс), и все одновременные
  попытки проходили проверку блокировки раньше, чем первая успевала записаться.
  `throttledAttempt` засчитывает попытку сразу после проверки, без `await`
  между ними; удачный вход обнуляет счётчик. Тест: сто одновременных догадок —
  проверяется пять (без исправления — сто).
- **Тело запроса читалось целиком.** `request.json()`, `.text()`, `.arrayBuffer()`
  не знают предела, а проверка длины по заголовку при передаче chunked не
  срабатывает: наблюдатель мог прислать на импорт гигабайты, и процесс держал
  их в памяти до проверки прав. `readBodyBytes` читает поток кусками и обрывает
  на пределе; через него идут `parseBody`, `parseOptionalBody` и импорт; права
  на импорт — до чтения файла.
- **Причина отклонения документа уходила представителю вуза.** В истории этапов
  и в ленте событий комментарии для этой роли скрыты (решение 9), а карточка
  документа их отдавала. Сборщик карточки теперь требует пользователя — ни один
  путь к ней не может забыть о правиле. Раздел пробника.
- **Кнопка входа зависала при обрыве сети**: `signIn` в этом случае бросает
  исключение, и ожидание не снималось. Теперь — сообщение «Сервер не ответил».

## 54. Интерфейс: данные — только своего адреса, этап 14 — со связкой

**Дата:** 23.09.2026

Нашлось сквозной проверкой интерфейса; каждое место воспроизведено в браузере
или по коду до исправления.

- **Загрузка отдавала данные чужого адреса.** `useResource` хранил последний
  ответ без адреса и отдавал его, что бы ни запросили потом. Панель рекомендации
  не закрывалась: адрес пропадал, данные прежней рекомендации оставались
  (проверено в браузере: Escape меняет адрес, панель висит). В форме связки после
  смены вуза можно было выбрать программу прежнего вуза, пока не пришёл новый
  список. Теперь ответ хранится вместе с адресом (`resource-state.ts`), прежние
  данные показывает только список с `keepPreviousData`. Так же ведут себя
  распространённые библиотеки загрузки: новый ключ — новая загрузка.
- **Этап 14 и прогресс не обновлялись** после смены статуса: страница брала
  из ответа только изменённый этап, а сервер пересчитывает и этап 14 (решение 2),
  и прогресс. Теперь изменённый этап показывается сразу, а связка перечитывается.
- **Пустая страница без выхода**: выдача сокращалась под человеком на последней
  странице, а переключателя страниц в пустом списке нет. `usePageInRange` во всех
  семи реестрах.
- **Пробел в поиске программ и продуктов заменял таблицу ошибкой**: схемы
  обрезают пробелы, и `?q=%20` проваливал `min(1)`. `parseQuery` считает значение
  из пробелов отсутствующим — для всех маршрутов сразу.
- **«Показать ещё» в истории вуза на 120 событиях давал ошибку** — сервер отдаёт
  не больше 100. Предел — общая константа для схемы и страницы.
- **Время встречи читалось поясом браузера, а показывалось по Москве**: в другом
  поясе 14:30 записывалось как 12:30. Поле «дата и время» — по Москве
  (`dateTimeInputToIso`, `dateToDateTimeInput`); тесты в трёх поясах.
- Мелкое: окно действия над этапом показывало отказ прошлой попытки «Начать этап»;
  «Собрать пакет документов» не обновлял вкладку; ссылка из уведомления на ту же
  связку не открывала этап; «Связки с этим продуктом» показывали все связки;
  панель документа сохраняла выбранный статус при переходе к другому документу.

## 55. Этапы связки: проверяется итог записи, изменения — по очереди

**Дата:** 23.09.2026

- **Завершённый этап терял результат, заблокированный — причину** (решение 4).
  Правила перехода проверяли запрос, а записывался этап, собранный из запроса
  и сохранённых полей. `{"status":"COMPLETED","result":"   "}` проходило
  проверку по сохранённому результату и записывало пустой; `{"result":null}`
  у завершённого не проверялось вовсе — правка полей без смены статуса правил
  не касалась; `{"blockingReason":""}` у заблокированного оставляло его без
  причины. Теперь итог собирается один раз (`resolveStageFields`), проверяется
  (`assertStageFieldsComplete`) и он же записывается. Завершение с пустым
  результатом в теле берёт сохранённый — как обещает контракт.
- **Изменения этапов одной связки шли без очереди.** Проверки — вне транзакции,
  транзакция ничего не блокировала. Тринадцать одновременных закрытий этапов 1–13
  оставляли этап 14 «в работе» — три раза из трёх; снятие обязательного пункта
  одновременно с завершением давало завершённый этап с открытым пунктом.
  `lockCooperation` берёт строку связки `FOR UPDATE` в начале каждой транзакции,
  меняющей этапы (смена статуса, отметка пункта, выпуск версии продукта); то, что
  могли изменить соседи, — контрольная точка и чек-лист — проверяется ещё раз
  под блокировкой. Раздел пробника с контрольным опытом.
- **Повторная отметка пункта переписывала, кто его отметил**: подтверждение
  представителя вуза переходило к менеджеру на устаревшей странице. Повтор
  ничего не меняет.
- **Правило этапа 14 было в трёх местах**: в сервисе, в выпуске версии продукта
  и в демонстрационном наборе. Копия в наборе считала по номеру последнего
  закрытого этапа, и у связки УрФУ «Компьютерная безопасность» с начатым этапом 1
  этап 14 значился «Не начат». Теперь одно правило (`computeControlStatus`)
  и один пересчёт (`recomputeControlStage` в `workflow.repo`). Числа показа
  не меняются: сверка со сценарием на новом наборе — 25 из 25.

## 56. Рекомендации: вернувшаяся проблема снова открыта, застой — по движению этапов

**Дата:** 23.09.2026

- **Рекомендация, закрытая системой, не возвращалась.** Ключ рекомендации —
  «правило, объект» — навсегда. Когда проблема уходила, генерация закрывала
  запись («Выполнена»), а когда та же проблема возвращалась — обновляла у записи
  текст и оставляла «Выполнена». Новая просрочка по той же связке не появлялась
  ни в ленте, ни на главной, ни в уведомлениях: они читают только открытые.
  Теперь закрытая системой запись (`resolvedById` пуст) при повторе открывается
  заново и встаёт среди свежих; решение человека не переписывается никогда.
  Раздел пробника: просрочка → срок перенесли → срок снова в прошлом.
- **«Связка без движения» смотрела на запись связки.** Смена статуса этапа
  и отметка в чек-листе запись связки не меняют: связка, где вчера закрыли три
  этапа, получала «без движения 30 дн.», а правка цели сбрасывала счётчик без
  всякой работы. Движение — самое позднее из смены статуса этапа, отметки
  в чек-листе и правки связки (`lastCooperationActivity`).
- **Демонстрационный набор подделывал застой**: у связки УрФУ, созданной 5 дней
  назад, дату изменения сдвигали на 30 дней. Подделка убрана; время записи связок
  в наборе — по сюжету (начало работы), и правило само находит связку, где работа
  стоит: МТУСИ «Анализ данных», 46 дней. Реестр связок по умолчанию сортируется
  по времени изменения — теперь это порядок по сюжету, а не по ходу заливки.
- **Два определения просрочки.** Главная и уведомления считают этап просроченным
  с момента срока, правило рекомендаций — с первых полных суток. В день срока
  главная писала «просрочен… сегодня», а рекомендаций не было — и вместо
  просрочки срабатывал застой: в наборе у СПбГУТ «Информационная безопасность»
  «без движения 196 дн.» при этапе 13, срок которого истёк сегодня. Теперь
  и здесь — с момента срока, в день срока важность средняя.

Что меняется в показе: первые шесть пунктов ленты и пять приоритетных действий
те же, сверка со сценарием — 25 из 25. Внизу ленты — «Просрочен этап 13»
у СПбГУТ «Информационная безопасность» и «Связка без движения 46 дн.» у МТУСИ
«Анализ данных» вместо прежнего «без движения 30 дн.» у УрФУ.

## 57. Привязки записи — в один вуз, счётчики — в пределах видимости, журнал — как обещан

**Дата:** 23.09.2026

- **Документ и встреча могли связать разные вузы.** Привязки к связке, вузу
  и программе проверялись каждая сама по себе — существует и видна. Встреча
  с вузом A, программой вуза B и контактным лицом вуза B проходила: представитель
  вуза A видел название чужой программы и ФИО с должностью чужого контакта,
  представитель B — название вуза A. Проверка была скопирована в два модуля.
  Теперь одна (`shared/links/entity-links.ts`): все привязки ведут в один вуз,
  программа совпадает с программой связки; участники встречи — контакты этого
  вуза, сотрудники ИТ-Школы или представитель этого же вуза; ответственный —
  только сотрудник ИТ-Школы (раньше и представитель другого вуза, а несуществующий
  id отвечал «документ не найден»).
- **Счётчики раскрывали чужие вузы.** Число связок продукта и программ с навыком
  считалось по всей базе и отдавалось представителю вуза: «Конвейер сборки
  и поставки — 2 связки» при одной своей. `_count` теперь с отбором по вузу
  пользователя.
- **Журнал не писал обещанного.** SECURITY_LIMITATIONS: «создание и изменение вуза,
  программы… автоматический пересчёт контрольного этапа». Записей не было ни одной:
  типы действий объявлены, вызовов нет. Добавлены — с именами полей, без значений.
  Пересчёт этапа 14 пишется после транзакции: сбой вставки внутри неё оборвал бы
  саму смену статуса.

Раздел пробника, контрольный опыт — без исправления падают все шесть проверок.

**Попутно — пробник на растущей базе.** Две проверки падали без ошибки
в приложении: закрытые связки искались на первой странице реестра, и сотня новых
их вытесняла; обход ленты рекомендаций сравнивался с одним запросом, который
отдаёт не больше сотни. Закрытые — отбором по статусу, обход — по первым строкам.

## 58. Документы и связки: без дублей от двойного нажатия, итог записи — по правилам

**Дата:** 23.09.2026

- **Двойное «Новая версия» давало две «версии 2».** Новая версия создавалась
  до архивации исходной, без транзакции: второй запрос создавал свою и только на
  архивации получал отказ. Теперь одной транзакцией, исходная сначала уходит
  в архив условно — второй запрос получает 409 до того, как что-либо создаст.
  От архивной версии новая не создаётся: номер считался от исходной, и рядом
  с существующей «версией 2» появлялась ещё одна.
- **Двойное «Собрать пакет» собирало два комплекта.** Проверка «что уже есть»
  и создание — теперь одной транзакцией в очереди связки (`lockCooperation`,
  решение 55).
- **Утверждённый документ можно было опустошить и подписать.** Содержимое
  (ссылка или текст из шаблона) проверялось только при отправке на согласование;
  правка ссылки правилам не подчинялась. Теперь правило «дальше черновика
  документ не пустой» проверяет итог любой записи — перехода и правки.
  Правка к тому же условная по статусу: подпись между проверкой и записью давала
  правку подписанного документа.
- **Связку можно было создать сразу закрытой** — с четырнадцатью нетронутыми
  этапами и без даты закрытия. Новая связка — только открытая (черновик, в работе,
  на паузе); форма предлагает только их.
- **Несуществующий продукт в правке связки отвечал «Связка не найдена»**: id
  уходил в connect, ошибка Prisma P2025 превращалась в 404. Проверяется так же,
  как при создании. Ответственный связки — только сотрудник ИТ-Школы.

Раздел пробника, контрольный опыт — без исправления падают все шесть проверок.

## 59. Главная: демо-данные помечены, оценка — оценкой, этап 14 не считается дважды

**Дата:** 23.09.2026

- **Показатели главной не помечали демонстрационные данные.** У всех пяти
  `isMock: false`, хотя посчитаны они по связкам и вузам набора с `isMock: true`;
  общий признак страницы смотрел на программы и рынок, но не на связки. CLAUDE.md:
  демонстрационные данные никогда не выдаются за реальные. Теперь признак
  у каждого показателя — по данным, из которых он посчитан, и общий — по всем.
- **Время до начала занятий выдавалось за факт**, хотя все даты начала занятий
  в наборе — плановые, в будущем. Решение 8: оценочный показатель помечается.
  Теперь `basis: "estimate"`, если хоть одна дата — плановая, и пояснение говорит
  сколько.
- **Этап 14 входил в долю «в срок».** Его закрывает система по остальным, его
  срок — сроки этапов 1–13, уже учтённые. Одна работа считалась дважды — по той
  же причине он не входит в процент прохождения. Одно определение для главной
  и личного кабинета. **Число показа меняется: 89,4% → 89,1%** (41 из 46 вместо
  42 из 47); шпаргалка и сверка со сценарием обновлены.
- **Без рыночных данных — «0 критических дефицитов».** Рядом с «Нет данных»
  по покрытию стояли нули: отсутствие данных читалось как отсутствие дефицитов.
  Теперь счётчики тоже `null`.
- **«Операций на связку: 0,4» показывалось как «0»**: главная округляла всё,
  кроме процентов, до целых. Точность — по показателю.
- **«4 вузов», «204 дней»**: единица приходила одной формой. Теперь склоняется
  по числу, и правило склонения одно на сервер и интерфейс — вторая копия
  не знала дробных чисел: при 8,6 писала «операций» вместо «операции».

## 60. Рейтинг и дефициты: действующая программа, спрос по навыку, период по календарю

**Дата:** 23.09.2026

- **Программы архивного вуза оставались в аналитике.** «Действующая программа»
  была записана в семи запросах как «программа не в архиве» — без вуза: программы
  архивного вуза сдвигали шкалу рейтинга, попадали в лучшие, закрывали дефициты
  и порождали рекомендации. Теперь одно определение — `ACTIVE_PROGRAM_WHERE`
  (programs.rules). Пробник с контрольным опытом.
- **Навык с несколькими замерами спроса считался несколько раз.** Замеров на навык
  за период бывает несколько — по регионам и источникам. Дефицит считался по каждой
  строке: навык дважды в списке, покрытие на главной с двойным счётом, две
  рекомендации с одним ключом затирали друг друга. Теперь одна строка на навык
  (`demandPerSkill`): федеральный замер, без него — наибольший региональный.
- **Последний период — по алфавиту.** Периоды бывают кварталами и месяцами;
  строкой «2026-Q1» больше «2026-09». Теперь по календарю (`latestOfPeriods`), и одна
  функция вместо двух копий — в дефицитах и в рекомендациях.
- **Покрытие на главной считалось по первым двумстам строкам**, отсортированным
  по дефициту: чем больше навыков, тем больше покрытых выпадало. Теперь сводка
  считается по всем навыкам до обрезания.
- **Рейтинг брал пустые показатели первыми.** PostgreSQL ставит NULL первыми при
  сортировке по убыванию: при двухстах программах без заявок срез для главной
  состоял только из них. Теперь пустые — в конце; «всего» — по базе, а не по срезу
  («Показаны 20 из 500» при шести тысячах).
- **Раскрытие балла не сходилось с баллом** при пустом показателе: балл
  пересчитывался на учтённые веса, вклады — нет. Теперь вклады делятся на тот же вес.
- **«Активные связи» значили разное**: карточка вуза считала и приостановленные,
  главная и кабинет — нет. Одно определение — `ACTIVE_COOPERATION_STATUSES`.

Числа показа не меняются: сверка со сценарием — 25 из 25, покрытие те же 88,9%.

## 61. Импорт: поля только из колонок файла, строки — по правилам API

**Дата:** 23.09.2026

- **Импорт программ стирал данные.** Файл с тремя обязательными колонками
  («Вуз», «Программа», «Уровень») обнулял у существующих программ код, направление,
  длительность и все три показателя рейтинга. У вузов то же самое исправили
  раньше — «обновляются только поля, чьи колонки есть в файле», — а у программ
  забыли: каждый планировщик решал это сам. Теперь правило одно на оба
  (`presentColumns`).
- **Импорт проверял строки своими правилами**, а не правилами API: через файл
  заводились вуз с названием из одной буквы, сайт «не-ссылка», программа
  длительностью в тысячу месяцев. Строки проверяются теми же схемами, что и формы
  (`createUniversitySchema`, `updateProgramSchema` и другие), ошибка называет колонку.
- **Повтор программы в файле давал двойника** — у вузов повтор ловился, у программ
  нет. Теперь — ошибка строки со ссылкой на первое вхождение.
- **Импорт менял архивные записи** в обход правила «архив не редактируется».
  Архивные вуз и программа теперь пропускаются с объяснением.

Раздел пробника, контрольный опыт — без исправления код и показатели стираются,
повтор проходит вторым обновлением.

## 62. Дни этапов — по календарю везде; интерфейс не скрывает обрезанное и сбои

**Дата:** 23.09.2026

- **Фильтр «просрочено не меньше N дней» и пометка «скоро срок» считали сутки
  по 24 часа**, а бейджи — по московскому календарю (решение 47). Этап с бейджем
  «−1 дн.» не попадал в `minDaysOverdue=1`, пока не пройдут сутки с часа срока;
  два этапа «через 3 дн.» получали разную пометку по часу срока. Теперь оба —
  по календарю (`moscowDayStart`, `daysBetween`); тесты в трёх поясах.
- **Синхронизацию рыночных данных интерфейс разрешал только администратору**,
  а контракт и сервер — любому с правом записи; менеджер читал «запускает
  администратор». Интерфейс — по контракту.
- **Вкладки карточек молча обрезали списки до пятидесяти**, а счётчик на вкладке
  показывал всё число. Таблица, которой передано общее число, пишет «Показаны
  первые N из M».
- **Сбой загрузки спроса в карточке программы выглядел как «Нет данных»** —
  как будто на навык нет спроса. Загрузка и сбой называются своими словами.

## 63. Визуальное направление 07: графит, сигналы вместо материала

**Дата:** 23.09.2026

Серёжа прислал `docs/design/07_Human_Crafted_Override.md` — документ с высшим
приоритетом над 01–06 — и бриф к нему. Главная претензия: интерфейс выглядит
сгенерированным — одинаковые тёмные карточки с фиолетовой рамкой, градиент на
каждой кнопке, свечение, иконки в квадратиках. Переделка идёт в порядке,
который задаёт сам документ (раздел 39): убрать шум → выстроить иерархию →
главная → фирменные элементы → движение.

**Шаг 1 (этот).** Токены `globals.css`: холст и три уровня графита (L0–L3),
акценты фиолетовый/розовый/оранжевый/бирюзовый — только как сигналы, текст
тёплого белого, линии белого с малой прозрачностью. Свечения погашены
(имена токенов оставлены, чтобы страницы не ломались), градиент — один
фирменный `--gradient-brand` для редких мест. Радиусы по разделу 14.
Кнопки: основная — светлая сплошная, акцентная (`variant="accent"`) —
фиолетовая, для одного продуктового действия на экран; вторичная — прозрачная
с тонкой линией; «призрачная» — без контейнера до наведения. Карточки —
без рамки и тени, отделяются тоном. Боковое меню — тонкая вертикальная метка
у текущего пункта, наведение — сдвиг на 3px. Таблица — редакционный список:
наведение — лёгкий фон и сдвиг содержимого на 4px, сетка не двигается.
Фиолетовые значения в модулях стилей переведены в графит правилом
по свойству (рамка → линия, подложка → графит, свечение → убрано).

**Что взято не буквально — потому что данных нет, а выдумывать их нельзя
(CLAUDE.md, «Данные»):** «время до найма» остаётся «временем до начала
занятий» — данных о найме в системе нет; «компания» в схеме связей —
ИТ-Школа РТК, других компаний в модели нет; «рекомендуемые программы
с баллом совпадения и причинами» — это рейтинг программ с его настоящим
баллом и вкладами показателей.

**Шаг 2 — главная (раздел 10 документа 07).** Пять плиток показателей заменены
на Live Rail: числа — типографикой в одну строку, под ними маршрут из четырнадцати
этапов с засечками и фазами, на нём — связки в работе точками на своём текущем
этапе, подписанные вузом; застрявшие (просрочка или блокировка) — красные и
получают один импульс при появлении. Внизу блока — «N этапов требуют внимания»
и время обновления. «Требует внимания» — лента событий на линии времени с записью
этапа «06 / 14», а не таблица в рамке; «Приоритетные действия» — строки; «Связки
в работе» — строки-маршруты «вуз ── программа ── продукт · 06 / 14», звенья
включаются фирменным градиентом при наведении; «Ключевые программы» — ранжированные
строки: номер, вуз, программа, из чего сложился балл, балл. Появление — одна
композиция: числа → линия дорисовывается → точки → импульс → группы ниже; после
этого всё неподвижно. Показатели, текст сводки и блоки шпаргалки — прежние;
в ответ сводки добавлено краткое имя вуза у ключевых программ.

**Шаг 3 — фирменные элементы карточки связки (разделы 7, 8, 30).** Схема связки —
Collaboration Link Map: «Компания ── Вуз ── Программа ── IT-продукт» типографикой,
без иконок в квадратах; компания — ИТ-Школа РТК (другой в модели нет). Звенья
дорисовываются по очереди, по цепочке проходит один импульс фирменного градиента;
наведение на узел подсвечивает соседей и звенья к ним, остальное приглушает.
От цепочки — ветка к текущему этапу «06 / 14 · название», красная, если этап
просрочен или заблокирован. Лента этапов — 14-stage Depth Rail: перспектива
с точкой схода у текущего этапа, каждый этап сдвинут по оси Z на расстояние от
него — пройденные уходят назад и тише, будущие дальше, проблемные не гаснут;
у текущего — светлый номер и короткая фирменная линия, над лентой — «Текущий
этап 06 / 14». Замки контрольных точек, цвета статусов и щелчок по номеру —
прежние: на них опирается шаг 3 показа.

**Шаг 4 — движение и остатки шума (разделы 5, 9, 17, 18, 23, 24, 29).**
- *Строка реестра → карточка* (`ui/lib/morph.ts`): по щелчку название объекта
  отделяется от строки копией, остальные строки приглушаются; когда карточка
  отрисовала заголовок, копия переезжает на его место и масштабируется, заголовок
  проявляется. Копия, а не общий элемент двух страниц: страницы Next монтируются
  заново, а карточке бывает нужно время на загрузку — название ждёт на месте.
  Не дождалось за полторы секунды — исчезает. Работает во всех реестрах через
  общую таблицу и шапку страницы; при «уменьшить движение» выключено.
- *Поиск*: окно вырастает из кнопки — стартует с её места и формы и
  разворачивается, содержимое проявляется следом.
- *Уведомления*: открытие вниз от колокольчика — сдвиг, прозрачность, масштаб
  0,985 → 1; строки по очереди с шагом 30 мс.
- *Вход*: слева — схема «Вуз ── Программа ── IT-продукт» и маршрут из 14 засечек
  вместо трёх пунктов с иконками; появление — холст, знак, схема дорисовывается,
  панель выходит справа, поля по очереди. После входа форма гаснет, панель
  раскрывается в холст, схема уходит вправо — и через 420 мс открывается главная;
  меню и шапка при первом открытии после входа дописывают сцену (`arrival.ts`).
- *Шум*: знак SkillLink без квадрата-подложки и градиентной обводки; аббревиатуры
  вузов и программ без неоновой рамки; у пустых состояний нет декоративной плитки
  со значком — они говорят текстом.


## 64. Визуальный заход: плашки реестров, переход «карточка раскрывается», живой фон

**Дата:** 23.09.2026

Правки внешнего вида по запросу ответственного за визуал. Логика, API и данные
не менялись — только оформление, разметка страниц и каркас.

**Реестры строками-плашками.** У `DataTable` появился вид `appearance="cards"`:
каждая строка — отдельная плашка с воздухом, крупнее, с меткой при наведении.
По умолчанию таблица прежняя (`grid`), поэтому документы и продукты не затронуты.
Плашками сделаны университеты, программы и связки; у ячеек — вторичные подписи
(сокращение вуза, регион, код и направление программы, маршрут «вуз — программа
→ продукт», лента из 13 этапов, «через N дн.»). Главная: «Требует внимания»,
действия, связки и рейтинг — отдельными плашками; рекомендации — карточками
с меткой важности. **Расходится с решением 44:** строки выше, на экран влезает
меньше строк, чем «двенадцать» из решения Артура, — плотность стоит сверить с ним.

**Переход «карточка раскрывается в свою страницу»** (`ui/layout/navigation-motion.ts`,
06, раздел 17). Только на карточках объектов — строках реестров и плашках с
`data-morph-title`; меню, шапка и кнопки переходят сразу. Сделан на View Transitions:
соседние строки расступаются, нажатая плашка вырастает в рабочую область, название
перелетает в заголовок. Анимируются только положение, размер и прозрачность снимков.
Элемент, взятый в переход, не должен исчезать во время анимации — поэтому заготовки
помечены `data-skeleton`, а переход ждёт данных до 0,26 с. Без поддержки View
Transitions и при «уменьшить движение» — прежний путь (`ui/lib/morph.ts`).

**Живой фон** (`ui/layout/LiveBackground.tsx`). Холст и поверхности —
чёрно-фиолетовые вместо нейтрального графита; под содержимым — фиолетовое свечение
за курсором, второе с запаздыванием и медленная дымка. Двигается только `transform`;
при «уменьшить движение» и на сенсорных экранах свечение неподвижно.
Цвета — токены `--bg-glow*`, `--bg-haze-*`.

## 65. Плотность реестров — по решению 44, подтверждённому Артуром

**Дата:** 24.09.2026

Решение 64 перевело реестры вузов, программ и связок в плашки и само отметило
расхождение с решением 44. Артур подтвердил 24.09: актуально 20–25 строк на экран,
реестр — рабочий инструмент.

**Что сделано.** Реестры вузов, программ, связок и документов — снова `grid`,
строка 32–33 px: на 1440×900 — 19–20 строк, на 1280 таблицы без прокрутки. Вид
плашек (`appearance="cards"`) и всё остальное из решения 64 — живой фон, цвета,
переход «карточка раскрывается» — не тронуты; разметка ячеек прежняя, изменены
стили: вторичная подпись стоит после главной в той же строке и обрезается первой.
Уточнения, которым в узком столбце места нет (длительность у уровня, число связок,
тип документа, просрочки у прогресса, «через N дн.» у срока), — в подсказке ячейки.

**Документы.** Реестр был в три строки на ячейку (в том числе от правки 23.09
«название вуза в две строки» — она сама расходилась с решением 44): вуз — кратко,
ответственный — «Фамилия И. О.», в строке — дата выдачи, подписание — в подсказке
(подписан ли, говорит статус). Для этого в `DocumentLinksDto` добавлено
`universityShortName` — только новое поле.

**Урок.** Решение 44 было записано, но правка 23.09 («названия вузов в две строки»)
его нарушила: обход смотрел, помещается ли название, а не сколько строк на экране.
Проверка плотности теперь — число строк на 1440×900 и отсутствие прокрутки на 1280
(docs/FRONTEND.md).

## 66. Удобство интерфейса: поиск в меню, короче ленты и карточки

**Дата:** 24.09.2026

Сплошной обход всех экранов на 1440 и 390 px. Правился только вид — данные,
запросы, права и логика прежние. Реестры не трогались: их плотность — решение 65.

- **Поиск** — кнопкой внизу бокового меню, а не плавающей в правом углу: там она
  закрывала «Выйти», последние колонки таблиц и этап 14 ленты. На узком экране,
  где меню спрятано, — компактный плавающий кружок. Ctrl+K и окно поиска прежние.
- **Карточка вуза** больше не шире экрана («Сильнейшая: …» переносится), числа
  прижаты к низу плашки, адрес сайта не рвётся посреди слова.
- **Лента этапов в карточке связки** — номера без постоянных подписей; название —
  всплывающей плашкой при наведении и фокусе. Подписи в 10 px обрезались и не читались.
- **Рейтинг в аналитике** — одна составная полоса вкладов вместо трёх; вес и вклад —
  в подсказке сегмента. Строка вдвое ниже.
- **Рекомендации** — описание и обоснование по две строки, полностью — в подсказке
  и панели; обоснование по-прежнему видно всегда. Код правила — только в панели.
- **Главная, «Связки в работе»** — две строки: вуз и программа, под ними продукт.
- **Подвал** — одной строкой: разделы уже в меню, остались служебные ссылки.
- **Мелочи:** крошка «Личный кабинет», источник рыночных данных словами вместо
  ключа `mock`, одна пометка демо-данных на карточке программы, поле фильтра
  150 px вместо 170.
- **Вид `cards` на телефоне** раскладывается карточками с подписями (`data-label`);
  реестры сейчас в виде `grid` (решение 65), так что это — запас для плашек.


## 67. Плотный реестр без вида «таблицы из офисной программы»

**Дата:** 24.09.2026

Плотность реестров — решение 65 (20–25 строк, строка 32 px) — не меняется.
Меняется только оформление общей таблицы (`ui/data/Table.module.css`, вид `grid`),
поэтому оно действует во всех реестрах, в рейтинге аналитики и на вкладках карточек.

- **Шапка — подписью, а не полосой:** мелкие заглавные с разрядкой над тонкой линией.
- **Разделители строк почти невидимые** (`--line-faint`): строки держит ритм, не сетка.
- **Строка под курсором — капсулой:** тон светлее, скруглённые края, фиолетовая метка
  слева, название (`data-morph-title`) светлеет в акцент — видно, что строка открывается.
- **Появление — короткой волной** сверху вниз (`--row`, шаг 14 мс, до 24-й строки),
  один раз на новую выдачу; обновление и смена сортировки её не запускают.
- **Таблица — на панели карточки** во всех реестрах; цифры — обычного кегля,
  без 17 px жирным, иерархия цветом, а не жирностью.
- **Документы:** «К чему относится» не выезжает на соседний столбец у вуза без
  краткого названия — вуз сжимается последним, после программы.

## 68. Реестры разгружены: меньше столбцов, ярко — только главное

**Дата:** 24.09.2026

Плотность прежняя (решение 65, строка 32 px). Жалоба — «как книга, тяжело выделить
нужное»: каждая строка несла 7–8 одинаково ярких сведений.

- **Столбцы.** Убраны второстепенные и не сортируемые: у вузов — «Программы»,
  у программ — «Навыки», у связок — «Ответственный», у документов — «Версия»
  и «Выдан», у продуктов — «Версия» и «Навыков». Всё это есть в карточке объекта
  (у документа — в его панели); API и данные не менялись. Столбцы с сортировкой
  и фильтрами остались все.
- **Уточнения — в подсказку:** код и направление программы, регион вуза.
- **Иерархия цветом** (общая таблица): ярко только первый столбец — то, что ищут,
  остальное вторым цветом; цифры обычного кегля.
- **Статус в таблице — точкой** с приглушённой подписью, без цветной плашки;
  тревожные (просрочка, приостановлен, отклонён) подписью остаются цветными.
  Для этого у `Badge` есть `data-badge` и `data-tone`; вне таблиц значок прежний.

## 69. Реестры — лентой, как рекомендации (отменяет плотность решения 65)

**Дата:** 24.09.2026

Ответственный за визуал счёл и плотную таблицу (решения 65, 67, 68), и её
разгруженный вариант тяжёлыми для чтения — «как книга» — и выбрал вид ленты
рекомендаций в компактном размере. **Это расходится с решением 44/65
(20–25 строк на экран), подтверждённым Артуром 24.09:** в ленте 11–14 записей
на экран 1440×900 (карточка ~62 px). Решение принято ответственным за визуал;
Артуру нужно сообщить до выкладки.

**Что сделано.** У `DataTable` третий вид — `appearance="list"`: каждая запись —
карточка; слева название и строка пояснения (общий `ui/data/ListTitle`), справа
3–4 факта с подписями, ширина факта — ширина столбца. Столбцы, чьё значение уже
в пояснении, помечены `hideInList` и не показываются, но их заголовок остаётся
кнопкой сортировки над лентой. Переведены: университеты, программы, связки,
документы, IT-продукты, рейтинг в аналитике. Сортировка, фильтры, переход в карточку
и API не менялись; на телефоне карточка переносит факты под название.

## 70. Заставка при входе и мелкая анимация по навыку ui-ux-pro-max

**Дата:** 24.09.2026

Без новых зависимостей: рецепты навыка написаны под GSAP, но всё сделано CSS
и Web Animations — GSAP в проект не добавлен.

- **Заставка** (`ui/layout/Splash.tsx`) при первом входе во вкладке: знак SkillLink
  прорисовывается, буквы поднимаются по одной, маршрут из 14 засечек зажигается,
  затем экран сворачивается кругом к центру. Разметка серверная и играет на CSS —
  видна сразу, до загрузки JavaScript. ~2 с от появления сцены (отсчёт — от
  встроенного скрипта, не от начала запроса: иначе при медленном сервере сцена
  обрывалась). Повторно во вкладке и при «уменьшить движение» не показывается —
  встроенный скрипт до первой отрисовки ставит `html[data-splash=skip]`, поэтому
  у `<html>` `suppressHydrationWarning`.
- **«Магнитные» главные кнопки** (`ui/layout/magnetic.ts`): кнопки `primary`
  и `accent` смещаются к подошедшему курсору не больше чем на 4 px, свойством
  `translate` — `transform` кнопки не трогается.
- **Лента реестров появляется волной** с шагом 30 мс и сдвигом 10 px.
- **Точки прогресса связки** зажигаются по очереди слева направо.

## 71. 3D-созвездие на экране входа — Three.js (новая зависимость)

**Дата:** 24.09.2026

**Зачем зависимость.** Настоящая 3D-сцена с тысячами частиц средствами CSS
не делается. Документ 07 (раздел 31) разрешает Three.js ровно для этого случая —
необязательной картинки входа с запасным вариантом — и запрещает на рабочих
экранах. `three@^0.186` (MIT, без install-скриптов, `npm audit` — 0 уязвимостей),
`@types/three` — для разработки.

**Что сделано** (`app/(auth)/login/Constellation.tsx`). За экраном входа два
скопления светящихся точек — «вузы» вверху слева, «IT-компании» внизу справа —
и связи между ними по диагонали через свободную середину, по связям бегут
импульсы: знак SkillLink в пространстве. Сцена покачивается и поворачивается
за курсором; камера подлетает при появлении и пролетает сквозь созвездие
после входа (по `body[data-auth-leaving]`, который ставила форма и раньше).
Заголовок и форму сцена не пересекает.

**Бережность** (навык ui-ux-pro-max, раздел Three.js). Библиотека грузится
динамическим `import()` только на странице входа и после её показа — первая
загрузка `/login` не выросла (145 КБ). Частицы — один `Points` на `BufferGeometry`,
~2,4 тыс. (вдвое меньше на узком экране); плотность пикселей ≤ 1,5; во скрытой
вкладке кадры не считаются; ресурсы освобождаются при уходе. «Уменьшить
движение» — один неподвижный кадр; нет WebGL — сцены нет, фон страницы прежний.
В headless Chrome на MacBook — 60 кадров/с.

## 72. Вход — «варп в систему»: из созвездия в сайт

**Дата:** 24.09.2026

После «Войти» сцена входа (решение 71) переходит в сайт одним движением, ~1,5 с:
1. скопления «вузы» и «IT-компании» летят навстречу и сливаются в центре — связь;
2. камера ныряет сквозь ядро, звёзды растут и летят навстречу, фиолетовая вспышка;
3. из вспышки проступает сайт: рабочая область выплывает из глубины и выпрямляется
   из наклона (`.arrival main`, `arriveDepth` в ui/layout/Shell.module.css), меню
   и шапка — как раньше (решение 63).

Экран входа уходит через 420 мс (время перехода не менялось), поэтому на время
прыжка холст переносится в `body` поверх сайта и доигрывает над ним, затем гаснет
и сам освобождает ресурсы. Во скрытой вкладке прыжок всё равно доигрывает — иначе
холст остался бы висеть. При «уменьшить движение» прыжка нет, вход прежний.
Проверено по кадрам: прыжок — сигналом `body[data-auth-leaving]`, появление —
меткой `ARRIVAL_KEY`; настоящий вход с паролем при проверке не выполнялся.

**Без рывка на переходе.** Смена страницы посреди прыжка на мгновение занимает
браузер (загрузка кода и сборка главной) — звёзды замирали. Теперь: главная
предзагружается сразу после проверки пароля (`router.prefetch`); с прыжком страница
меняется под пиком вспышки (`WARP_NAVIGATE_MS`, форма узнаёт о прыжке по
`body[data-warp=ready]`), без прыжка — через 420 мс, как раньше; в прыжке
плотность пикселей холста — 1, звёзды растут втрое, а не вшестеро; холст
и вспышка — отдельными слоями (`will-change: opacity`). Боевая сборка, чистый
кэш, настоящий клиентский переход: 6 прогонов, ни одного кадра длиннее 34 мс.

## 73. Реестры вузов и программ — 3D-карусель бирок; Motion (новая зависимость)

**Дата:** 24.09.2026

Референс — New Zealanderlivery Service (awwwards): бирки веером, центральная —
лицом к зрителю, соседние повёрнуты к центру и уходят в глубину. В реестре вузов
появился переключатель «Бирки / Список» (выбор запоминается в браузере); по
умолчанию — бирки (`universities/UniversityCarousel.tsx`). Бирка — как багажная:
«СОКРАЩЕНИЕ → ГОРОД», полное название, регион, сокращение на фирменном градиенте
вместо фото, программы, связки, рейтинг (только ролям с аналитикой, пустой —
«Нет данных»), штамп «Демо» у демонстрационных записей. Штрихкод и номер —
украшение из идентификатора, не данные.

Листается перетаскиванием (веер проворачивается за рукой, отпускание — пружиной
на ближайшую бирку), стрелками на экране и клавиатуре, горизонтальной прокруткой
тачпада; вертикальная прокрутка страницы не перехватывается. Щелчок по боковой
бирке выводит её в центр. Щелчок по центральной открывает страницу вуза: остальные
бирки за ~0,4 с складываются в стопку за ней, от дальних к ближним; затем бирка
перетекает в страницу (`universities/expand.ts`): поверх неё проявляется тёмно-фиолетовый
лист (светлый, гаснущий в чёрный, мерцал), лист растёт до рабочей области, под ним меняется страница,
и лист тает, когда у новой страницы есть заголовок и нет заготовок.

Сначала переход был сделан на View Transitions (как у строк реестров, решение 64) —
отказались: браузер замораживает экран, пока новая страница не отрисуется и не
получит данные (до ~0,8 с), и переход ощущался как зависание; светлая бирка ещё и
вспыхивала на тёмной подложке. Лист анимирует только transform и opacity (WAAPI) —
это ведёт видеокарта, и движение идёт плавно, даже пока главный поток собирает
страницу. Лист лежит под шапкой и меню. Во скрытой вкладке анимации стоят, поэтому
лист снимается ещё и по таймеру.
**Программы** — та же карусель, но бирка другая, чтобы реестры не выглядели копией
друг друга: тёмный «билет на курс» (`programs/ProgramTag.tsx`) — цветная полоса
с уровнем и кодом (цвет по уровню: бакалавриат и специалитет фиолетовые, магистратура
розовая, аспирантура оранжевая, СПО и ДПО бирюзовые), название, «вуз → направление»,
срок, навыки, связки и отрывной корешок по перфорации с показателями набора
(оценка — «≈», пусто — «Нет данных»). Лист раскрытия у программ малиновый.
Программ больше, чем вузов, но в карусели одновременно не больше 13 бирок, остальные
рисуются по мере листания; страницы по 25 и фильтры — прежние.

Общее вынесено, чтобы не дублировать: карусель — `ui/data/TagCarousel.tsx` (вид бирки
передаёт реестр через `renderTag`, оттенок листа — `tint`), перетекание в страницу —
`ui/lib/expand.ts`. Тест оформления теперь принимает переменную, объявленную в самом
модуле (`--tone` бирки программы), — чужие необъявленные по-прежнему ошибка.

Страница центрального вуза предзагружается, пока он в центре. Бирки — только текущая
страница реестра, фильтры и страницы — прежние. «Уменьшить движение» — без пружин
и складывания, переход сразу.

Промежуточная полноэкранная карточка вуза (по мотивам Melius) была сделана и убрана
по просьбе пользователя: второй шаг между биркой и страницей лишний.

**Зависимость `motion` (MIT)** — из списка библиотек фронта, который дал
пользователь. Нужна для пружинной физики листания и анимаций с задержкой по
очереди (складывание). Tailwind и shadcn из того же списка не ставились:
оформление — по-прежнему CSS Modules и переменные (решение 41).

## 74. Воронка связок на главной — по образцу Bklit Funnel

**Дата:** 24.09.2026

На главной между «Требует внимания» и «Связки в работе» — воронка: сколько связок
вуз — программа — продукт дошло до каждой из пяти фаз конвейера (Привлечение →
Оформление → Внедрение → Эксплуатация → Контроль). Образец — Bklit Funnel из списка
библиотек фронта; сам Bklit не ставился (нужен Tailwind и shadcn, решение 41 и
выбор пользователя 24.09): компонент `ui/data/Funnel.tsx` написан на SVG, Motion
и CSS Modules.

**Подсчёт** (`app/(app)/phase-funnel.ts`, с тестом): фаза связки — фаза текущего
этапа (решение 5), связка без текущего этапа прошла все фазы; «дошла до фазы» —
текущая фаза не раньше этой. Отменённые связки не входят. Отдельного эндпоинта нет —
считается на фронте из `GET /api/cooperations` (не больше 100 за запрос); если связок
больше, под воронкой написано, по скольким она посчитана. Бэкенд и контракт не менялись.
`TODO: PM DECISION` — нужен ли серверный агрегат по фазам, когда связок станет больше сотни.

**Вид.** Одна величина — один оттенок (фиолетовый акцент), лента сужается от шага
к шагу, между шагами зазор фона. Под графиком те же числа подписями: сколько дошло,
доля от всех и от прошлого шага — читается без наведения и заменяет таблицу.
Шаги вырастают по очереди слева направо; при наведении или фокусе с клавиатуры —
подсказка «дошли до фазы / сейчас на ней». «Уменьшить движение» — без роста.

## 75. Вход: звёзды во весь экран и сборка сайта по частям в 3D

**Дата:** 24.09.2026

Уточняет решение 72. Жалобы: звёзды собирались «в отдельном квадрате», переход на
главную — резкий.

**Квадрат.** Звёздная пыль лежала коробкой 22×12 единиц; при нырке камеры её края
читались как отдельный прямоугольник. Теперь звёздное небо — в сцене (не в
покачивающемся мире) и рассчитано под окно с запасом 15 % по видимой области
с дальней точки подлёта камеры: краёв не видно ни в начале, ни в прыжке.

**Прыжок** (2 с вместо 1,5): звёзды всего экрана закручиваются воронкой к центру,
скопления «вузы» и «IT-компании» сливаются в яркое ядро, затем звёзды разлетаются
из ядра мимо камеры. Вспышка — мягкая фиолетовая во весь экран вместо белой
в центре; страница меняется под ней, как раньше (`WARP_NAVIGATE_MS`). Холст гаснет
дольше (с 68 % прыжка) — под ним уже собирается сайт.

**Сборка сайта** (`.arrival` в `ui/layout/Shell.module.css`): меню разворачивается
от левого края (rotateY), шапка откидывается сверху (rotateX), блоки страницы по
очереди подлетают из глубины с наклоном. На главной части помечены `data-assemble`:
«Требует внимания» и «Связки в работе» прилетают слева, «Приоритетные действия»
и «Ключевые программы» — справа, воронка — по центру. Только transform и opacity.
Метку `.arrival` каркас снимает через 4 с — дальше страницы приходят обычным
появлением. Узкий экран и «уменьшить движение» — без сборки, как раньше.

**Без остановок (дополнение того же дня).** Посреди прыжка звёзды замирали
«фотографией»: сразу после входа браузер загружает и собирает главную, и общий
с ней поток переставал рисовать кадры. Сцена вынесена в фоновый поток: Web Worker
рисует на OffscreenCanvas (`login/constellation.worker.ts`), и кадры идут, как бы
ни была занята страница. Угасание холста и вспышка — CSS-анимации (`warpFade`,
`warpFlash`), их тоже ведёт не главный поток. Код сцены — один
(`login/constellation-scene.ts`, без DOM): браузер без OffscreenCanvas получает
ту же сцену на странице, как раньше. Новых зависимостей нет.

## 76. Звёзды собирают главную по частям

**Дата:** 24.09.2026

Уточняет решение 75. Просьба: конец входа подтормаживал; звёзды, собравшиеся в ядро,
должны сами собрать главную по частям, в движении.

**Сборка.** Звёзды закручиваются в ядро (0,9 с) и не замирают — кружат в нём, пока
открывается сайт. Экран входа (`login/Constellation.tsx`) после перехода ждёт
страницу: каркас, шапку, блоки (заготовки — не дольше 0,7 с). Пока ждёт, части
скрыты (`body[data-star-assembly=pending]`). Затем меряет блоки по порядку — меню,
шапка, дальше сверху вниз — и передаёт сцене их прямоугольники: каждая звезда
вылетает из ядра к точке на контуре (три четверти) или внутри своего блока, по дуге
навстречу камере, со своей задержкой; блок проявляется из глубины (transform
и opacity, Web Animations), когда звёзды к нему почти долетели, а звёзды гаснут на
нём. Блоки главной со своей стороной (`data-assemble`) прилетают слева и справа.
То, что пришло позже (данные главной), собирается обычной сборкой каркаса
(`.arrival`, решение 75). Страница не пришла за 2,6 с — звёзды разлетаются мимо
камеры, как раньше.

**Подтормаживание.** Две причины: под шапкой с размытием 18 px летели блоки, и
размытие пересчитывалось каждый кадр; в разлёте звёзды раздувались в крупные
светящиеся точки во весь экран — лишняя заливка для видеокарты. На время сборки
размытие шапки выключено (сплошной фон), точки больше не раздуваются. Угасание
холста CSS-анимацией не нужно — звёзды гаснут сами; холст снимается по окончании.

**Переделка того же дня.** Пользователь увидел не сборку, а разлёт: на локальном
сервере главная после входа компилируется и грузит пользователя дольше 2,4 с,
и ядро отпускало звёзды. И «лагающий» конец воронки: в момент, когда звёзды
собрались, скорость вращения скачком менялась с нуля на большую, а все звёзды
сжимались в одну белую точку. Теперь:
- ядро — наклонный звёздный диск: у каждой звезды свой радиус и скорость
  (внутренние быстрее), вращение разгоняется плавно с начала прыжка — скорость
  нигде не прыгает, и видно, что диск движется;
- ядро ждёт главную до 10 с (`PAGE_WAIT_MS`), данные главной — до 1,5 с; пока
  ждёт, скрыта и загрузочная заготовка каркаса (`data-shell-loading`);
- звёзды собирают экран по одному блоку (шаг 150 мс): 90 % звёзд ложатся на контур
  блока, светятся на нём и мерцают, блок проявляется внутри контура, затем звёзды
  гаснут; меню во всю высоту не забирает себе больше звёзд, чем крупный блок;
- вспышки больше нет — страница под звёздами скрыта до сборки; сайт открывается
  раньше, на середине воронки (`WARP_NAVIGATE_MS` = 600 мс).

**След звёзд (дополнение).** Звёзды держались на контуре и гасли 0,9 с — дольше,
чем проявлялся блок, и их след висел поверх загруженной страницы; холст убирался
только по сообщению фонового потока. Теперь свечение на контуре 120 мс, угасание
160 мс, разброс вылета звёзд одного блока 60 мс; блок начинает проявляться за 100 мс
до прилёта звёзд и становится непрозрачным, когда они уже гаснут. Холст снимается
сразу, как доиграл последний блок.

**Холст под сайтом (дополнение).** Даже короткое угасание поверх готовых блоков
читалось как след. Холст прыжка теперь лежит не поверх сайта (z-index 1200), а под
его содержимым и над живым фоном (z-index −1, позже фона в документе): звёзды
прилетают на пустое место, где блок ещё скрыт, и проявившийся блок закрывает их
своей непрозрачной поверхностью — поверх содержимого звёзды не видны вовсе.

**След — настоящая причина (дополнение).** Прыжок впервые снят покадрово: Chrome без
окна, экран входа, вместо главной — подложенные меню, шапка и блоки (60 кадров/с,
без пауз). На кадрах след оказался тёмными точками: при обычном сложении погасшая
звезда (цвет — ноль) оставалась в холсте непрозрачной и закрывала фон под собой;
тот же эффект давал тёмный кружок в центре ядра. Теперь у каждой звезды неба
своя прозрачность (цвет — rgba): гаснущая звезда теряет и свет, и непрозрачность,
погасшая невидима. Сначала вместо этого материалы перестали писать непрозрачность
вовсе (только свет) — в Chrome без окна это работало, а обычный Chrome на Mac такие
пиксели холста не показывает, и 3D на экране входа пропал; откатено. Звёзды больше не ложатся на контур блока
(пунктир висел на прозрачных частях — заголовках колонок), а летят в блок струёй
через его центр, расходятся по нему и гаснут ровно к касанию; блок становится
непрозрачным к их прилёту. Диск ядра заполнен до центра.

**Запасной путь (дополнение).** Если фоновый поток не справлялся (нет WebGL
в потоке — Safari до 17, часть сборок Яндекс Браузера; поток не загрузился;
молчит), холст просто снимался — и 3D на экране входа пропадал. Теперь при ошибке,
отказе или молчании потока дольше 2,5 с на место отданного потоку холста встаёт
новый, и та же сцена рисуется на странице. Проверено в Chrome без окна
(видеокарта Apple M4) четырьмя случаями: обычный поток, поток не создаётся, поток
молчит, браузер без OffscreenCanvas — во всех сцена на экране входа есть.

**Фильтры над бирками (дополнение).** Раскрытые списки фильтров уходили под 3D-бирки:
блоки страницы при появлении получают свой слой (анимация с transform), и блок
с бирками, идущий после панели фильтров, рисовался поверх неё. Панель фильтров
(`Toolbar`, `ui/layout/Page.module.css`) поднята над содержимым страницы
(`position: relative; z-index: 2`), шапка сайта остаётся выше — на всех реестрах.

**Мобилка (дополнение к 73–76, правило после PR #73).** Созвездие входа на сенсорном
экране (`hover: none`) не поворачивается за касаниями; сборка сайта звёздами — только
на широком экране без сенсора, как и сборка каркаса (на узком блоки съезжали бы вбок) —
там звёзды собираются в диск и разлетаются, сайт приходит сам; слои звёзд
(`.constellation`, холст прыжка) — `overflow: hidden; contain: strict`; подсказки
крайних шагов воронки прижаты к своему краю. Экран входа на 375 и 390 px:
`scrollWidth` = `clientWidth` в покое и в прыжке (Chrome с эмуляцией касаний; настоящий
iPhone Safari не проверялся — его нет).

## 77. Логотипы вузов на бирках — файлами в public/logos

**Дата:** 24.09.2026

На бирке вуза (решение 73) вместо сокращения на градиенте — логотип вуза на белой
подложке (у белого логотипа СПбГУТ подложка тёмная). Выбрано из двух вариантов:
иконки сайтов по адресу вуза (сервис favicon.yandex.net) требовали поля `website`
в ответе реестра и настоящих сайтов в демо-наборе — это API и данные, их автовыкладка
не выкладывает, а иконки мелкие и разнобойные; файлы — только фронт и не зависят
от внешнего сервиса на показе.

Логотипы шести вузов демо-набора — в `public/logos/`, сопоставление по сокращению
в `universities/university-logos.ts` (TEMP). Источники: официальные сайты (sut.ru,
mtuci.ru, kai.ru, nstu.ru, donstu.ru) и Викисклад для УрФУ (символ, общественное
достояние). Это товарные знаки вузов рядом с демо-данными (на бирках штамп «Демо») —
`TODO: PM DECISION` Артура: оставлять ли настоящие логотипы на показе. Вуза нет
в списке — остаётся сокращение. Правильное решение на потом —
поле «логотип» у вуза в базе с загрузкой при заведении вуза (Тигран, после защиты).
Новая переменная `--logo-plate` — белая подложка логотипов.

## 78. Контрольная точка — шлагбаум для всех следующих этапов

**Дата:** 24.09.2026

Точка защищала только саму себя: этап 7 не начинался до подписания договора,
а этап 8 «Поддержка внедрения» — начинался и завершался. Отмена этапа 6 с любым
комментарием считалась закрытием и открывала этап 7. Легенда в карточке связки
при этом обещала «дальше не пускает». Нашёл скептический аудит (M-01, M-02, R-02).

Теперь любой этап, кроме автоматического 14-го, нельзя начать, завершить или отметить
в нём пункт, пока не **завершены** все контрольные точки до него. Для самой точки
по-прежнему нужны закрытыми все предыдущие этапы, и отменённый обычный этап (например,
необязательный 5-й) закрыт. Блокировать и отменять можно всегда. Правило — одна
функция `findBlockingStages` в `workflow.rules.ts`; её же используют отметка пункта
и кабинет вуза. Демо-набор правилу не противоречит: в нём нет этапов, начатых
за незавершённой точкой (проверено запросом). Сценарий показа не меняется.

## 79. Интерфейс по референсам: граф связей, бирки как A24, дашборд Bklit/Bento

**Дата:** 24.09.2026

Пользователь изучил с нами референсы (A24, New Zealanderlivery Service, Altitude 101,
Seasats, Melius) и библиотеки (Bklit UI, shadcn Blocks, Kokonut UI, Motion, Anime.js)
и попросил внести всё предложенное, кроме экрана входа и перехода на главную
(решения 71–76 не тронуты). Tailwind и shadcn не ставились (решение 41) — всё на
CSS Modules, переменных, SVG и Motion. Новых зависимостей нет: «рассыпка» текста
написана своими двадцатью строками, Anime.js ради неё не понадобился.

- **Граф связей вуза** (Melius «Node Explainer»): вуз → программы → IT-продукты,
  связка — изогнутый провод цвета статуса с меткой этапа и ссылкой на связку, импульс
  по проводам связок в работе, вкладки-фильтры по статусу со счётчиками, легенда.
  Щелчок по центральной бирке реестра раскрывает его панелью из бирки (вместо
  перетекания в страницу; страница — кнопкой), он же — в «Обзоре» страницы вуза.
  Продукты расставлены по среднему ряду своих программ — провода не перекрещиваются.
- **Бирки** (A24): блок фактов центральной бирки слева на тонких линиях (`FactSheet`),
  «Весь список» со всеми позициями, листание колесом (на крайней бирке прокрутка
  отдаётся странице), подпись у курсора «листать / открыть» (Altitude). На телефоне
  блок фактов скрыт — факты на самой бирке.
- **Воронка** — как Bklit Funnel: плавные кривые, два ореола, проценты в «таблетках».
- **Главная** (Bento): бегущая строка событий из ленты уведомлений (New Zealanderlivery
  Service), кольца «этапы в срок / связки без просрочек / покрытие навыков», карта
  России с вузами (контур Natural Earth, проекция Альберса — `ui/data/russia-map.ts`,
  координаты городов — TEMP в `city-coordinates.ts` до поля в базе).
- **Программа**: радар «спрос рынка против покрытия» во вкладке «Дефициты».
- **Страницы вуза и программы**: заголовок с засечками (`PageHeader variant="display"`),
  линейка прокрутки с процентами (Altitude).
- **Меню** (Seasats, Altitude): свой оттенок у группы,
  мини-сводка раздела при наведении (число из `meta.total` того же API), буквы
  перекатываются при наведении. После просмотра (24.09) по просьбе пользователя
  убраны: «Следующий вуз / программа», вкладка «Потоки связок», номера пунктов
  меню; перекатывание букв сделано компактнее. Компоненты `NextObject` и `Sankey`
  остаются в `src/ui` без использования. **Телефон**: нижняя панель разделов (A24), кнопка
  поиска поднята над ней. **Фон**: сетка точек. **Приветствие**: «рассыпка» букв.

Все данные — теми же маршрутами API, контракт и база не менялись. «Уменьшить
движение» — без анимаций; пустые значения — «Нет данных». Проверено в Chrome без окна
(демо-вход проекта без пароля): 13 экранов на 1440 без ошибок, 390 и 375 px без
горизонтальной прокрутки. `TODO: PM DECISION` — координаты вузов и логотипы в базе.

## 80. Два режима интерфейса: «Рабочий» и «Презентационный»

**Дата:** 25.09.2026

Наставник хакатона: SkillLink — рабочий инструмент аналитиков, лишний визуал ему
не нужен, на каждом экране сразу должно быть видно, где горит. Визуал дизайнера
(решения 70, 73, 79) при этом готов и нужен для показа. Удалять его не стали —
режим выбирает сам человек.

**Рабочий** (по умолчанию для всех ролей):
- реестры вузов и программ по умолчанию «Список», а не «Бирки»; выбор вида
  по-прежнему запоминается, и запомненный выбор главнее режима;
- на главной нет бегущей строки, колец «Здоровье портфеля» и карты вузов —
  «Требует внимания» и «Приоритетные действия» идут сразу за приветствием
  и полосой «Активно сейчас» (и появляются раньше); ленту событий и список вузов
  для карты главная в этом режиме не запрашивает;
- нет заставки, «рассыпки» букв приветствия, линейки прокрутки, импульсов
  и прорисовки проводов графа связей, «магнитных» кнопок, перекатывания букв
  в меню; живой фон неподвижен (дымка не дышит, свечение не ходит за курсором);
- счётчики, полосы, воронка, радар и бирки не анимируются — как при
  «уменьшить движение».

**Презентационный** — всё как было до этого решения.

**Механика.** Выбор — в `localStorage` (`skilllink.uiMode`, чтение и запись
в try/catch: приватное окно не ломает страницу), копия — атрибут
`data-mode="work|showcase"` на `<html>`. Атрибут ставит встроенный скрипт до
первой отрисовки (`UI_MODE_BOOT_SCRIPT`, по образцу скрипта заставки) — рабочий
режим не мигает визуалом; скрипт заставки идёт следом и в рабочем режиме её
пропускает. Чистые функции — `ui/lib/ui-mode.ts` (с тестом), хуки — `ui/hooks/ui-mode.ts`:
`useUiMode()` (через `useSyncExternalStore`, смена режима сразу видна во всех
компонентах и других вкладках) и `useCalmMotion()` = «уменьшить движение» ИЛИ
рабочий режим. Декоративные компоненты, которые раньше спрашивали только
`usePrefersReducedMotion`, теперь спрашивают `useCalmMotion`. Где проще CSS
(меню, дымка фона) — правило `:global(html[data-mode='work'])`.

**Переключатель** — пара кнопок «Рабочий / Презентационный», как «Бирки / Список»
в реестрах (`ui/layout/UiModeSwitch.tsx`): в шапке рядом с именем пользователя
(на экране от 1081 px), в настройках — раздел «Режим интерфейса», в личном кабинете —
строка в «Настройках» (там его найдёт и представитель вуза, и человек с телефона).

**Не менялось.** Экран входа и 3D-сцена входа (решения 71–76), сборка сайта после
входа, переходы между страницами (View Transitions и перелёт названия, решение 64),
появление блоков — это не украшение, а связь экранов, они уважают только
«уменьшить движение», как раньше. Заставка показывается и на странице входа, поэтому
в рабочем режиме её нет и там. API, контракт и база не менялись.

**Что это ограничивает.** Режим живёт в браузере, а не в профиле: на другом
устройстве человек выберет его заново. Хранить в профиле — поле в базе и эндпоинт
(Тигран), не нужно до защиты.

## 81. Пакет документов без дублей, одна незакрытая связка на «вуз + программа + продукт»

**Дата:** 25.09.2026

**Пакет документов.** Сверка «что уже есть» шла только по ключу шаблона. Договор,
заведённый вручную, и новая версия собранного документа (ключа у неё нет) проходили
мимо — пакет добавлял к связке второй договор. Теперь шаблон закрывает действующий
документ связки с тем же ключом шаблона **или того же типа** (у каждого шаблона свой
тип), в любом статусе, кроме «В архиве». Лицензия без выбранного IT-продукта не
собирается вовсе: документ «Лицензия на IT-продукт «__________»» не имеет предмета.
`force` снимает только первую проверку. Правило — `packageSkipReason` в `documents.rules.ts`.
Недостающие реквизиты и пропущенные шаблоны ответ называет по-русски (словарь
`TEMPLATE_PLACEHOLDER_LABELS` рядом с `TEMPLATE_PLACEHOLDERS`, поле `name` у шаблона).
Служебная строка «TEMP: болванка…» убрана из текстов документов — она уходила вузу на
согласование; неутверждённость текстов отмечена комментарием у конфига. Тексты
перестроены так, чтобы ФИО и должность не требовали склонения.

**Дубли связок.** Вторая незакрытая связка с тем же вузом, программой и продуктом
(«не выбран» — тоже значение) делила бы с первой этапы, документы и встречи. Создание,
смена продукта и переоткрытие отвечают `CONFLICT` с `details.cooperationId`. Проверка —
в сервисе, в транзакции под блокировкой строки программы (`lockProgram`), так что два
одновременных «Создать» не проходят её оба. Частичный уникальный индекс не понадобился:
схема не менялась. Демо-набор правилу не противоречит — все его связки на разных
программах. Сквозной сценарий заводил вторую связку на ту же программу для проверки
выпуска версии продукта; теперь заводит её на отдельной программе того же вуза.

## 82. IT-продукт заводится и правится; версию со связками меняет только выпуск

Продукты были только в демо-наборе: `POST` не было, и у новой связки продукт навсегда
оставался «не выбран». Добавлены `POST /api/products`, `PATCH /api/products/:id` и
`PUT /api/products/:id/skills` (как у программ), в интерфейсе — «Добавить продукт»
в реестре и «Изменить» в панели продукта, одна форма на оба действия.

- **Название уникально без учёта регистра.** Ограничение базы различает регистр, но
  «облачная платформа РТК» и «Облачная платформа РТК» в списке связки неотличимы.
  Проверка в сервисе даёт понятный 409 с полем `name`; гонку двух запросов ловит
  ограничение базы, и ответ тот же, а не общее «запись уже существует: name».
- **Ссылка на документацию — только http(s).** Карточка выводит её ссылкой, а
  `z.url()` пропускает `javascript:` и `data:`.
- **Версию продукта с открытыми связками правкой не поменять** — 409 с полем `version`.
  Новая версия означает, что вузам передана устаревшая; для этого есть выпуск версии
  (решение по групповой операции, раздел 15в контракта): он ставит задачи и переоткрывает
  этап 12. Тихая правка поля оставила бы этап закрытым со старыми материалами.
  У продукта без открытых связок версию можно исправить. `TODO: PM DECISION` — нужна ли
  кнопка выпуска версии в интерфейсе: сейчас выпуск есть только в API.
- Заведённый вручную продукт — `isMock: false`. В журнал пишутся `product.create`,
  `product.update`, `product.skills.replace` с именами полей, без значений.

Схема базы не менялась.

## 83. Рекомендации и уведомления не говорят неправды о просрочке

**Дата:** 25.09.2026

«Закрыть» убирала рекомендацию «Просрочен этап N» с главной, хотя этап оставался
просроченным, и «Пересобрать» её не возвращала. После завершения или отмены этапа
рекомендация о его просрочке висела «Новой», пересборка отвечала «закрыто 0».
Таблица переходов статусов жила только в интерфейсе: API принимал `DONE → NEW`.
В колокольчике одна просрочка приходила дважды — от срока этапа и от рекомендации.

**Решение.**

- Таблица переходов — `RECOMMENDATION_TRANSITIONS` в `shared/contracts`, одна на сервер
  и интерфейс; остальное — `INVALID_TRANSITION`.
- `DONE` по правилам с проверяемым условием (просрочка, застой, продукт не выбран,
  нет показателей программы) — только когда условие ушло, иначе `CONFLICT` с тем,
  что сделать вместо этого. Не согласен — отклонить с основанием. Проверка —
  та же функция `draftsForCooperation`, что у пересборки: закрытие и пересборка
  не расходятся в том, что считать просрочкой.
- Пересборка закрывает системой открытые, включая принятые, чья проблема ушла,
  и открывает закрытые, чья проблема вернулась, кто бы их ни закрыл. Исключение —
  дефицит навыка, закрытый человеком: действие сделано, дефицит остаётся.
  Отклонённые с основанием не трогаются никогда. Это уточняет решение 56: там
  «решение человека не переписывается», но закрытие человеком при живой проблеме
  теперь невозможно, а закрытое раньше было неправдой о состоянии дел.
- Смена статуса или срока этапа и отметка в чек-листе сразу сверяют открытые
  рекомендации связки: неправда закрывается, правда получает свежий текст. Сверка —
  после транзакции этапа и без исключений наружу: сбой не отменяет смену статуса,
  недосверенное доделает пересборка. Пересчёт одной связки — несколько запросов,
  без очереди и фоновых задач.
- Лента склеивает просрочку по виду события и объекту (связка и номер этапа),
  оставляя пункт срока этапа: он ведёт к самому этапу, и его время не сдвигает
  пересборка. Переоткрытая рекомендация о том же случае проблемы (тот же этап и срок,
  то же последнее движение) сохраняет время создания и не всплывает непрочитанной.
- Не начатый этап за незавершённой контрольной точкой (`isLockedByControlPoint`
  поверх `findBlockingStages`) не даёт ни рекомендации «просрочен», ни уведомлений
  о сроке: начать его нельзя, действие — закрыть точку.

**Расхождение с решением 37.** Оно обещало «просроченных в ленте столько же, сколько
в личной статистике». Теперь в ленте их меньше ровно на запертые контрольной точкой:
счётчики просрочек (главная, реестр связок, личная статистика) не менялись — что в них
считать, решает продукт. Пробник сверяет ленту со статистикой за вычетом таких этапов.
`TODO: PM DECISION` — считать ли их в счётчиках.

**Изменения схемы:** нет. **Затрагивает фронт:** `RECOMMENDATION_TRANSITIONS` из
`shared/contracts` вместо своей таблицы на странице рекомендаций; новые ответы 409
у `PATCH /api/recommendations/:id` показываются тем же сообщением об ошибке.

## 84. Просрочен только начатый этап; не начатый с вышедшим сроком — «план сдвинут»

**Контекст.** Срок каждого этапа ставится при создании связки и дальше не двигается.
Когда задерживается этап перед ним или держит контрольная точка, у этапов 7–10 срок
выходит раньше, чем их вообще можно начать. Они получали «Просрочен на N дн.» при
статусе «Не начат»: из десяти строк «Требует внимания» шесть были такими, одна связка
(СПбГУТ — Программная инженерия) занимала четыре строки. Задача Артура (ТЗ беку, п. 1);
закрывает `TODO: PM DECISION` из решения 83 — считать ли такие этапы в счётчиках.

**Решение.**
- Просрочен только этап **в работе или заблокированный** с вышедшим сроком —
  `OVERDUE_STAGE_STATUSES` в `workflow.rules.ts`. Одно множество на правило `isOverdue`
  и на запросы: список просрочек, фильтр связок «только просроченные», проблемные
  этапы и счётчик главной, личная статистика, выгрузка (там была своя копия правила —
  теперь вызов `isOverdue`). Рекомендации и лента берут то же правило.
- Не начатый этап с вышедшим сроком — `isPlanShifted`, новое поле в DTO этапа
  и текущего этапа. Интерфейс показывает серую пометку «план сдвинут» с подсказкой;
  на ленте этапов такой этап больше не красный. В счётчики и списки проблем не идёт.
- Связку, где текущий этап так и не начали, ловит правило «Связка без движения»:
  оно смотрит на текущий этап в любом статусе.

**Числа сценария:** проблемных этапов 12 → 5 (по одному на связку, список на главной
больше не обрезается), у СПбГУТ — Программная инженерия просрочено 5 → 1.
Через двое суток после заливки — 6 (срок первого этапа УрФУ), дальше число само не растёт.

**Изменения схемы:** нет. **Затрагивает фронт:** новое поле `isPlanShifted`
у `WorkflowStageDto` и `CurrentStageDto`; `isOverdue` больше не бывает `true`
у не начатого этапа.

## 85. Демо-данные: даты по сюжету, сроки после занятий — от начала занятий

**Контекст.** ТЗ Артура беку, п. 2. В демо-наборе у всех вузов, программ, продуктов,
документов и встреч «Заведён/Обновлён» — момент заливки, при первом контакте в мае.
Срок этапа 11 «Проведение занятий» (октябрь) раньше начала занятий (декабрь).
Все завершённые этапы закрывались в первые две недели связки: у СПбГУТ ИБ «Проведение
занятий» завершено за полгода до начала занятий, за последний месяц не закрывалось
ничего — тренд главной (решение 81) на демо был бы пуст. У наблюдателя ФИО «Наблюдатель
Демонстрационный». 420 заявок СПбГУТ — «оценка» в карточке программы
и «подтверждена вузом» в кабинете вуза.

**Решение (только `prisma/seed.ts`, логика системы не менялась).**
- Сроки этапов 11–14 не раньше начала занятий + 30/60/90/120 дней
  (`DAYS_AFTER_CLASSES_START`, TEMP). В самой системе сроки по-прежнему по нормативам.
- Хронология этапов: этап начинается, когда закрыт предыдущий, закрывается незадолго
  до своего срока, опоздавший — через несколько дней после. Кто в срок, а кто
  с опозданием — как было, доля «в срок» та же (89,1 %). История этапов, журнал
  и отметки чек-листа — теми же датами.
- У СПбГУТ ИБ занятия идут два месяца (раньше — начнутся через 30 дней): этапы 11–12
  у неё закрыты. Лицензия и встреча «Передача учебных материалов» — до закрытия этапа 7.
- Вузы, контакты, программы, продукты, документы, встречи — даты записи по сюжету:
  вуз заведён до первой связки, документ — в день выпуска и обновлён последней
  сменой статуса (день подписания совпадает с переходом «Подписан» в истории).
- Показатели программ СПбГУТ — источник «вручную» (данные вуза): у вуза есть кабинет,
  и заявки там подтверждены. Пометка «оценка» у них уходит; у вузов без кабинета — остаётся.
- Наблюдатель — Лебедева Анна Сергеевна.

**Числа сценария:** среднее время до начала занятий 204 → 186 дней; проблемных этапов
5 → 4 (у СПбГУТ ИБ этап 13 в срок); через двое суток после заливки — 5. Тренд главной
на демо больше не пустой: сервер отдаёт «+1 за 30 дней» у связей и «+0,5» у доли в срок.
Подпись тренда на экране рисует фронт (ТЗ фронту, задача 1) — пока её нет.

**Изменения схемы:** нет. **Затрагивает фронт:** нет.

## 86. Одно число связок на главной: разбивка по статусам с сервера

**Контекст.** ТЗ Артура беку, п. 3. На демо меню (подсказка при наведении) говорило
«8 связок», шапка главной — «7 активных», фильтр реестра «В работе» — 6, воронка — 8,
а блок «Связки в работе» молча обрезал список семи до шести. Каждое место считало
по-своему, и ни одно число не объясняло другое.

**Решение.**
- Сервер отдаёт в сводке главной `cooperationCounts` — связки по статусам одним
  запросом (`countCooperationsByStatus`): `active` = в работе + черновики, `inWork`,
  `drafts`, `paused`, `completed`, `total` = все, кроме отменённых. Показатель
  «Активные связи» берётся из той же разбивки — разойтись они не могут.
  Отдельный счёт активных в `analytics.repo` заменён ею.
- Семантика зафиксирована: **главное число — активные (в работе и черновики)**.
  Шапка: «7 активных связей (6 в работе, 1 черновик)»; подсказка меню — те же 7;
  кольцо — доля среди них же. Под блоком «Связки в работе»: «Показаны 6 из 7».
  Воронка по смыслу считает всех, кто дошёл до фазы, поэтому под ней: «8 связок
  в воронке: 7 активных, 1 завершённая». Фильтр «В работе» в реестре — статус
  «В работе» (6), он совпадает с «6 в работе» в шапке.
- Пробник сверяет разбивку с показателем и с реестром теми же фильтрами.

**Изменения схемы:** нет. **Затрагивает фронт:** новое поле `cooperationCounts`
в `GET /api/analytics/overview`; подсказка меню «Сотрудничество» считает активные.

## 87. Подписанный договор сам отмечает пункты этапа 6

**Контекст.** ТЗ Артура беку, п. 4. Подписанный договор («Подписан» в документах)
не отмечал пункты чек-листа этапа 6 «Подписание документов» — подпись вводилась
дважды: статусом документа и тремя галочками. Обратное направление уже работало:
подтверждение вуза в кабинете закрывает пункт этапа 7.

**Решение.**
- Договор (`AGREEMENT`) или лицензия (`LICENSE`) связки переходит в «Подписан» →
  если договор по связке подписан и другие договоры подписи не ждут, отмечаются
  пункты этапа 6, помеченные в конфиге `closedBySignedDocuments` (все три, TEMP).
- Лицензию этап 6 не ждёт: она передаётся на этапе 7, за контрольной точкой,
  и обычно подписывается позже договора. Одна подписанная лицензия при договоре
  на согласовании — не «документы подписаны».
- Правила те же, что у отметки руками (`setTaskDone` под блокировкой связки):
  не на закрытой связке, не на завершённом или отменённом этапе, не за шлагбаумом
  контрольной точки. Отказ здесь — не ошибка: статус документа меняется всегда,
  а ответ говорит, что стало с этапом (`stageChecklist`), интерфейс пишет это
  в сообщении.
- **Отступление от ТЗ:** автор отметки — не «система», а тот, кто подписал
  документ. У отмеченного пункта обязан быть автор (CHECK-ограничение
  и правило `db:verify`); в журнале у отметки `source: "document.signed"`.
- Запасной вариант из ТЗ сделан тоже: у чек-листа этапа 6 строка «Подписано
  по документам: «Договор…», 12.09.2026» — подпись видна, даже если пункты
  не отмечены (этап был за шлагбаумом, когда договор подписали).

**Изменения схемы:** нет. **Затрагивает фронт:** `PATCH /api/documents/:id/status`
отвечает `DocumentStatusChangeDto` с необязательным `stageChecklist`.

## 88. 152-ФЗ в коде: политика, минимизация выгрузки, обезличивание контакта, сроки хранения

**Контекст.** Разбор безопасности и 152-ФЗ для защиты: в системе есть персональные
данные (сотрудники, представители вузов, контактные лица вузов, IP в журнале).
Опись, основания, сроки, уровень защищённости и меры — `docs/PRIVACY.md` (оценка
разработчиков, не заключение юриста и не заявление о соответствии — CLAUDE.md).

**Решение (код — сейчас).**
- `/privacy` — проект политики оператора, открыт без входа (ч. 2 ст. 18.1 152-ФЗ):
  `OPEN_PATHS` в middleware, ссылки со страницы входа и из подвала. На странице —
  только то, что правда сегодня: журнал Caddy и резервные копии по расписанию
  там не обещаны, пока не включены.
- Выгрузка вузов: почта контакта — только ADMIN и MANAGER (`canSeeContactDetails`),
  телефон — никому, колонки у всех ролей одинаковые. Журнал выгрузки: набор, фильтры
  со значениями (строка поиска — признаком `true`) и адрес клиента.
- `POST /api/universities/:id/contacts/:contactId/anonymize` (ADMIN) — право субъекта
  на удаление: ФИО → «Контакт удалён», остальное стирается, запись остаётся ради
  встреч; обезличенный контакт не подставляется в шаблоны документов и не бывает
  основным. Журнал — без ФИО.
- `npm run db:retention` — сроки хранения журнала (1 год) и адреса в нём (90 дней),
  без флага только показ. Расписание пока не включено.

**Отложено до после защиты (черновой PR #93).** Журнал запросов Caddy, `header -Server`,
пределы памяти и процессора контейнеров, `cap_drop`, отдельная роль базы без
суперпользователя. Они применяются при выкладке и меняют работу стенда — перед
показом это лишний риск.

**Изменения схемы:** нет. **Затрагивает фронт:** `ContactDto.isAnonymized`; новый маршрут;
пустая колонка «Почта» в выгрузке вузов у аналитика и наблюдателя.

## 89. Настройки сервера по 152-ФЗ — до защиты, а не после

**Контекст.** В решении 88 журнал запросов Caddy, пределы контейнеров и роль базы
без суперпользователя были отложены на после защиты: они меняют работу стенда
при выкладке. Команда решила иначе: на хакатоне систему оценивают как готовый
продукт, и защита данных должна быть включена, а не описана.

**Как снят риск.**
- Потребление на стенде замерено (`docker stats`): приложение ~110 МБ, база ~22 МБ,
  Caddy ~20 МБ при 4 ГБ машины — пределы (1 ГБ приложению) с запасом в разы.
- Роль `skilllink_app` проверена локально: приложение под ней прошло `demo:check`
  26/26, сквозной сценарий целиком и пробник 270/270 — без единого отказа в правах.
- Выкладка в два шага: сначала Caddy и пределы (роль ещё не задана — приложение
  работает как раньше), затем роль отдельной строкой в `.env.cloud`. После каждого
  шага — внешние проверки и `demo:check`; откат роли — убрать строку (DEPLOY.md).
- До тихого окна 26.09 14:00.

**Изменения схемы:** нет (роль и права — не схема). **Затрагивает фронт:** нет.

## 90. ИИ-помощник на российской модели: пишет черновики, решают правила

**Дата:** 25.09.2026

`TODO: PM DECISION` — включать ли ИИ-помощник на стенде и на показе; решение Артура.
**25.09:** команда решила — включаем. Код выкладывается выключенным (`AI_ASSIST_PROVIDER=off`,
шаблон); модель включается строкой в `.env.cloud`, когда будет ключ YandexGPT (DEPLOY.md, раздел 8).
Код готов и по умолчанию выключен (`AI_ASSIST_PROVIDER=off`): без решения на стенде
работают шаблоны, и система ведёт себя как раньше.

Правила уже знают, что горит: просрочка этапа, застой, продукт не выбран, нет показателей
программы, дефицит навыка. Но сводку для руководителя, письмо вузу и план на день
менеджер до сих пор пишет руками — пересказывает то, что система уже посчитала.
Помощник делает три черновика: **сводку по связке** (3–5 предложений: где она, что
мешает, что дальше), **письмо вузу по рекомендации** (что нужно от вуза и к какому сроку)
и **«что сделать сегодня»** (3–7 дел с одной строкой «почему»).

**Как это соотносится с запретом «ML-модели и LLM в бизнес-логике» (CLAUDE.md).**
Запрет не нарушается, и устроено всё так, чтобы его нельзя было нарушить:

- модель не принимает решений и не меняет данных. **Что** рекомендовать и **в каком
  порядке** — только правила (`recommendations.rules.ts`, `workflow.rules.ts`). Порядок
  дел на сегодня — та же `compareDraftsByImportance`, что у ленты рекомендаций;
  важность просроченного этапа — тот же `ruleOverdueStage` с теми же порогами;
- модель получает уже посчитанные факты и только переписывает их читаемым текстом.
  Ответ проверяется: пустой, отказ фильтра, письмо без подписи ИТ-Школы, список дел
  с другим числом пунктов — отбрасываются;
- без модели работает всё: тот же текст собирает детерминированный **шаблон из тех же
  фактов**. Выключена, не настроена, ошибка, таймаут, пустой ответ, лимит — шаблон
  и ответ 200. Сбой модели никогда не даёт 500;
- результат — черновик для человека. Ничего не отправляется и не сохраняется само,
  ничего не генерируется при открытии страницы — только по кнопке. Интерфейс всегда
  пишет, кто автор: «Черновик ИИ (YandexGPT) — проверьте перед отправкой» или
  «Шаблон без ИИ: помощник не подключён».

**Решение.**

- Провайдеры — `src/integrations/llm/`: интерфейс `LlmProvider` (system + user → текст),
  `YandexGptProvider` (Yandex Foundation Models, основной), `GigaChatProvider` (запасной),
  `DisabledLlmProvider` (выключено, без сети). Выбор — `AI_ASSIST_PROVIDER`
  (`off | yandexgpt | gigachat`), свой таймаут `AI_ASSIST_TIMEOUT_MS` (15 с), без повторов:
  человек ждёт у кнопки, а шаблон готов сразу. YandexGPT идёт через общий клиент
  интеграций (`requestJson` получил необязательные заголовки — `Api-Key` и `x-folder-id`),
  с заголовком `x-data-logging-enabled: false`. GigaChat — через встроенный `node:https`:
  только так Node принимает сертификат НУЦ Минцифры (`GIGACHAT_CA_CERT_PATH`) без новых
  зависимостей; токен доступа кэшируется до истечения.
- Модуль `src/modules/ai-assist/`: факты — из существующих сервисов (карточка связки,
  лента рекомендаций, `problemStageWhere` главной); шаблоны и сборка промптов — по одной
  функции на каждый черновик; лимит и кэш — в памяти процесса, как у ограничения
  попыток входа (`throttle.ts`).
- **Персональные данные в модель не уходят.** Каждая строка фактов и ответ модели
  проходят очистку (`ai-assist.privacy.ts`): «Ответственный: ФИО» из обоснования
  просрочки вырезается, почта и телефоны заменяются пометкой, ФИО сотрудников
  и контактных лиц вуза — словом «ответственный» или «представитель вуза» в любом
  падеже и с инициалами, «Фамилия И. О.» — даже если человека нет в базе. Официальные
  названия вузов не трогаются. Тест сверяет промпты со всеми ФИО, почтой и телефонами
  демо-набора из `prisma/seed.ts`.
- Лимит: 20 обращений к модели на пользователя в час; кэш одинаковых фактов — 10 минут
  (`AI_ASSIST_LIMITS`, TEMP). Ответ из кэша и шаблон лимит не расходуют.
- Права: сводка и «что сегодня» — `ANALYTICS`, письмо — `WRITE` (действие от имени
  ИТ-Школы). Представитель вуза — 403; чужое и несуществующее — 404, как везде.
- Журнал: `ai.draft` на каждую генерацию — кто, вид, объект, провайдер, модель или шаблон
  и почему. Текст промпта и ответа в журнал не пишется.
- Маршруты: `POST /api/cooperations/:id/ai-summary`, `POST /api/recommendations/:id/ai-letter`,
  `POST /api/ai/today` (docs/API_CONTRACT.md, раздел 10а).

**Почему российская модель.**

- **152-ФЗ, статья 12** — трансграничная передача персональных данных. Даже очищенные
  факты — сведения о работе с вузами; отправлять их за рубеж не нужно. YandexGPT
  и GigaChat работают в дата-центрах в России: **данные не покидают РФ, персональные
  данные в модель не передаются**. Соответствие 152-ФЗ этим не заявляется — это
  по-прежнему предмет отдельного аудита (SECURITY_LIMITATIONS.md).
- **Стенд уже в Yandex Cloud.** YandexGPT — тот же облачный контур, тот же договор
  и платёжный аккаунт; сервисный аккаунт и ключ заводятся в той же консоли.
- **Концепция обещает заказчику:** «зарубежные облачные сервисы не используются».
  Зарубежная модель нарушила бы обещание, российская — нет.
- **Для жюри Ростелекома** это аргумент сам по себе: помощник встраивается в российский
  контур без исключений из политики.
- **GigaChat — запасной российский провайдер**: если в Yandex Cloud что-то пойдёт
  не так, переключение — одна переменная, код не меняется.

**Ограничения.** Лимит и кэш — в памяти процесса: при нескольких экземплярах у каждого
свой счёт, перезапуск обнуляет. Модель может ошибиться в формулировке — поэтому
черновик, пометка источника и факты под ним. Очистка ФИО опирается на базу и на вид
«Фамилия И. О.»: имя без фамилии и инициалов в свободном тексте она не узнает.

**Изменения схемы:** нет. **Затрагивает фронт:** `AiDraftDto`, подписи источника
в `shared/contracts/ai-assist.ts`; кнопки «Черновик письма», «Составить сводку»,
«Что сделать сегодня».

## 91. ТЗ визуалу: читаемость на проекторе, заголовок объекта, плотные «Настройки»

**Дата:** 25.09.2026 · **Статус:** локальная ветка `visual/tz-visual`, в main не влито.
(В пробной ветке `visual/tailwind-trial` номер 84 занят пробой Tailwind — при вливании
второй из них перенумеровать.)

По ТЗ визуалу (пункты 2–5). Пункт 1 — декор с рабочих экранов — уже выполняет рабочий
режим (решение 80): бегущей строки, колец, карты и «рассыпки» в нём нет, код остаётся
для презентационного режима и для `/presentation`. Саму презентацию делает не визуал.

**Читаемость (п. 5).** На проекторе 1280×720 жюри не видело подписей: 11 px
и третичный текст с контрастом 3,8–4,1:1.
- `--text-caption-size` и `--text-kicker-size`: 11 → 12 px.
- `--text-tertiary`: `#73717c` → `#86848f` — 4,67–5,37:1 на всех поверхностях
  от холста до `--surface-3`; `--status-neutral` — тот же цвет (бывает текстом).
- Цвет `--text-disabled` (2,2:1) у текста, который нужно прочесть, заменён на
  tertiary: ключ в настройках, счётчик фазы ленты этапов, номер рекомендации,
  статус «Отклонена». Разделители остались тусклыми.
- Подсказка клавиши поиска и счётчик колокольчика: 10 px → `--text-caption-size`.
- Проверка в браузере на 1280×720: главная, рекомендации, связки, настройки —
  ни одного видимого текста мельче 12 px и с контрастом ниже 4,5:1.
  Инициалы в аватарах (9–11 px) не тронуты: это знак, а не подпись.

**Заголовок объекта (п. 4).** Новый токен `--text-object-title-size`
(`clamp(22px, 2.6vw, 34px)`) вместо `--text-display-size` (до 42 px): тот же токен
у экрана входа и листа фактов, их не трогаем. У `PageHeader` появился `subtitle`:
на странице вуза крупно короткое имя, полное — строкой ниже, мельче. Самое длинное
название вуза — одна строка на 1440 и 390 px; самое длинное название программы —
одна строка на 1440, две на 390 (было четыре).

**Удалено (п. 2).** `ScrollRuler` (линейка «000%» на страницах вуза и программы),
штрихкод и «номер бирки» на бирке вуза (карусель осталась), `NextObject` и `Sankey`
— не использовались с решения 79, на презентацию не планировались.

**Бирка вуза открывает страницу вуза (просьба визуала).** Щелчок по центральной бирке
больше не раскрывает панель «Связи вуза» (решение 79): бирка сразу перетекает в страницу
вуза, как у программ. Граф связей остаётся на самой странице вуза, во вкладке «Обзор».
`UniversityGraphSheet` удалён, у `TagCarousel` убран параметр `onOpen`.

**«Настройки» (п. 3 и ТЗ фронту, задачи 6–9)** — по образцу настроек Claude.
Слева колонка разделов («Интерфейс», «Рыночные данные», «Источники данных»,
«Интеграции», «О системе»), справа один раздел: заголовок и строки — название
с короткой подписью слева, значение или действие справа, тонкая линия между строками,
без карточек. Раздел — в адресе (`/settings#integrations` из подвала открывает его,
«Назад» возвращает предыдущий). На узком экране разделы — полосой над содержимым
с прокруткой внутри неё, строка складывается. Длинные пояснения разделов — за
значком (i) (`Tooltip`, как у показателей). Три значка интеграции — один статус:
«Выключена» > «Требует настройки» > «Подключена (демо)», пояснение источника —
подписью строки. У источника на виду название, тип и последняя загрузка; надёжность,
число показателей, дата сбора, описание и адрес — по «Подробнее» (`aria-expanded`).
Ни одно сведение не убрано. `TODO: PM DECISION` — формулировки статусов интеграций
согласовать с Артуром.

**Изменения схемы:** нет. **Затрагивает фронт:** токены `--text-tertiary`,
`--text-caption-size`, `--text-kicker-size`, `--status-neutral` — видно на всех экранах;
API не менялся.

## 92. Плашка входа — стеклянная карточка по образцу AuthKit

**Дата:** 25.09.2026 · **Статус:** локальная ветка `visual/tz-visual`, в main не влито.

Визуалу понравилась карточка входа на authkit.com. Перенесены приёмы, а не код
и не палитра: на CSS-модуле экрана входа и токенах `--glass-*` в `globals.css`,
в фирменном лавандовом `--glass-line` вместо их голубого.

- **Стеклянная плита:** почти непрозрачная тёмная основа с морозным оттенком,
  кромка 1 px отдельным слоем, свет изнутри сверху и тёмный ореол снизу
  (`--glass-shadow`) вместо обычной тени; четыре светящиеся точки в углах.
- **Бегущая по кромке линия** (их `card-animated-loop`): конический градиент
  с узким светлым сектором, маской вырезан до рамки; угол `--edge-angle`
  (`@property`) делает оборот за 12 с, яркость дышит с периодом 3 с. Без
  `@property` (Safari до 16.4) линия стоит на месте. При «уменьшить движение» — стоит.
- **Появление:** панель проявляется из расфокуса (blur 8 px → 0) чуть снизу
  с лёгким увеличением, 760 мс; поля — по очереди, как раньше.
- **Поля:** морозная заливка и кромка, при фокусе кромка ярче и мягкое кольцо
  фирменного цвета — только на экране входа, общий `Input` не менялся.
- **Кнопка «Войти»:** вариант `accent` (сплошной фиолетовый) — как единственная
  цветная кнопка у AuthKit; свечение при наведении.

**Не менялось:** 3D-созвездие за экраном и прыжок после входа (решения 71–76),
уход панели после входа (`.leaving`, `expand`), левая колонка. Их прожекторы-лучи
не перенесены: это фон, а фон остаётся прежним.

**Изменения схемы:** нет. **Затрагивает фронт:** только экран входа; новые токены
`--glass-*` в `globals.css`.

## 93. Экран входа в объёме: карточка наклоняется, колонка — слоями

**Дата:** 25.09.2026 · **Статус:** локальная ветка `visual/tz-visual`, в main не влито.

Продолжение решения 92 по просьбе визуала: «весь экран входа в 3D, карточка
двигается при наведении». `src/app/(auth)/login/Depth.tsx`, пакет `motion`
(уже в проекте), пружины без новых зависимостей.

- **Карточка** (`TiltCard`) наклоняется к курсору: над ней — до 9°, чуть
  приподнимается (×1,02), по стеклу скользит блик из точки под курсором
  (`--glass-glare`); курсор в стороне — до 3°. Наклоняет обёртка `.tilt`, а не
  сама панель: у панели свои анимации появления и ухода. После входа карточка
  выравнивается, и уход панели идёт как раньше.
- **Левая колонка** (`DepthLayer`) — четыре слоя (знак, заголовок, подпись,
  схема связи) сдвигаются против курсора на 6–22 px: ближние дальше, отсюда объём.
- Один обработчик движения на окно (`DepthScene`), пружины мягкие; курсор ушёл
  из окна — сцена возвращается в покой.
- **Телефон и «уменьшить движение»:** всё неподвижно (`hover: hover` и
  `pointer: fine` обязательны) — правило для мобилки (PR #73).

**Не менялось:** 3D-созвездие, прыжок после входа, уход панели.

**Изменения схемы:** нет. **Затрагивает фронт:** только экран входа; токен `--glass-glare`.

## 94. Главная в презентационном режиме: графики вместо списков и общая подсветка вуза

**Дата:** 25.09.2026 · **Статус:** локальная ветка `visual/tz-visual`, в main не влито.

Визуалу главная казалась сырой. Причина — не нехватка украшений: в рабочем режиме
(решение 80) движение выключено, а три блока из пяти — строки текста. Сделано только
для **презентационного** режима; рабочий не изменился (решение 80 и задача 2 ТЗ
фронту — свёртка аналитики — не затронуты). Правило: каждая анимация показывает
данные, проигрывается один раз, когда график доходит до экрана при прокрутке
(`useInView` из `motion`), и уважает «уменьшить движение». API и база не менялись.

- **«Активно сейчас»:** под «Активными связями» и «Этапами в срок» — сравнение
  за 30 дней из уже приходящего `trend` (#81): рост бирюзой со стрелкой, падение
  красным, ноль — нейтрально, без слов «без изменений» (ТЗ фронту, задача 1).
  Стоит в строке пометок: ряд чисел выровнен по низу, отдельная строка поднимала бы колонку.
- **Шкала просрочек** (`ui/data/DeadlineStrip`) над «Требует внимания»: каждый
  проблемный этап — точка на оси «дней после срока», точки выезжают из «сегодня»
  на своё место, ось краснеет к давним; заблокированные — квадратами справа.
- **Полоска маршрута** (`ui/data/StageBar`) у «Связок в работе»: деления заливаются
  волной, текущий этап — фиолетовым, красным при просрочке, жёлтым при блоке.
  Какие именно этапы просрочены, список не знает — цветом отмечен только текущий.
- **Разложенный балл** (`ui/data/ScoreBar`) у «Ключевых программ»: полоса длиной
  в балл из вкладов трёх факторов рейтинга (решение 7) их цветами, с подписью цветов.
- **Дефициты** (`ui/data/GapBars`) под покрытием навыков: пять самых больших
  дефицитов из `/api/skills/gaps?limit=5` — спрос рынка полосой, покрытая часть бирюзой.
- **Общая подсветка вуза:** наведение на вуз в полосе «Активно сейчас», на шкале
  или в любой строке приглушает всё, что относится к другим вузам. У проблемного
  этапа нет id вуза — он берётся из уже загруженных связок, иначе по названию.

**Изменения схемы:** нет. **Затрагивает фронт:** новые компоненты `DeadlineStrip`,
`StageBar`, `ScoreBar`/`ScoreLegend`, `GapBars` в `src/ui`; у `LiveRail` поля
`trend`/`isShare` и параметры `showcase`/`focus`/`onFocus`; токен `--success-surface`.

## 95. Главная в 3D: объёмные кольца и колонки, всплывающая карточка вместо затемнения

**Дата:** 25.09.2026 · **Статус:** локальная ветка `visual/tz-visual`, в main не влито.

Продолжение решения 94 по отзыву визуала: «нет 3D-диаграмм, подсветка при наведении
непонятна». Только презентационный режим; рабочий не изменился. Всё — проекцией в SVG
и CSS 3D на `motion`, без WebGL и новых зависимостей. API и база не менялись.

- **`ui/data/Pie3D` — объёмное кольцо.** Толщина со стенками: передняя половина
  показывает внешнюю стенку, задняя — внутреннюю сквозь отверстие; блик на верхней
  грани, тень на «полу». При появлении сектора выметаются по кругу с доворотом;
  кольцо крутится мышью или пальцем с инерцией (по вертикали палец листает страницу);
  сектор под курсором или подписью выдвигается наружу и приподнимается, его значение
  встаёт в центр. Используется в «Здоровье портфеля» (три кольца-доли вместо плоских
  колец решения 79; где известны штуки — сектора по штукам) и «Где сейчас связки»
  (фаза текущего этапа каждой связки) рядом с картой.
- **`ui/data/Bars3D` — объёмные колонки** «Связки по вузам»: передняя грань, боковина,
  крышка; снизу — идущие по плану, сверху красным — требующие внимания. Колонки
  растут по очереди, под курсором приподнимаются с разбором; щелчок — страница вуза.
  Поле — по ширине блока, на узком экране подписи наклонены. Подсказка — сбоку
  от колонки на уровне её верха (справа, не помещается — слева), не шире 260 px,
  название вуза переносится; всегда в пределах блока. Над колонкой по центру
  длинное название вылезало за край и закрывало колонки.
- **`ui/data/Peek` — всплывающая 3D-карточка** при наведении на строку «Требует
  внимания», «Связки в работе» и точку шкалы просрочек. Внутри — маршрут
  вуз → программа → продукт, лента этапов на наклонной плоскости с поднятой
  «колонной» текущего этапа, этап и что случилось. Карточка плывёт за курсором
  на пружине и кренится по ходу движения. **Затемнение «чужого» из решения 94 убрано**
  отовсюду: оно не объясняло, что происходит.
- **Показание кольца — под ним, а не в отверстии.** В наклоне отверстие — узкий
  эллипс: и подпись в две строки, и позже тёмная плашка всё равно ложились на
  грани. Теперь строка-«табло» под кольцом: крупное значение и подпись, высота
  постоянная — при наведении ничего не сдвигается; у серого сектора значение
  светлое, а не серое. Доля в процентах — в легенде у всех
  подписей сразу, чтобы легенда не перескакивала при наведении. Пока сектор
  выдвинут, у соседей на срезах в зазор дорисованы торцы — кольцо не выглядит полым.
- **`ui/hooks/reveal` — `useReveal`:** «дошёл до экрана» через IntersectionObserver
  и запасной сверкой положения при монтировании, прокрутке и раз в 300 мс, пока
  не показан: элемент въезжает в экран и без прокрутки, когда выше догружаются
  блоки. Без запасного пути графики оставались пустыми навсегда.

**Проверено** в браузере на 1280 и 390 px (скриншоты целой страницы): кольца,
колонки, карточка при наведении; на 390 px нет горизонтальной прокрутки и подписи
колонок не налезают. В рабочем режиме ни одного нового элемента.

**Изменения схемы:** нет. **Затрагивает фронт:** новые компоненты `Pie3D`, `Bars3D`,
`PeekProvider`/`usePeek`/`CooperationPeek`, хук `useReveal`; `DeadlineStrip`
получил `itemProps` вместо `focus`/`onFocus`; у `LiveRail` убраны `focus`/`onFocus`.

## 96. Личный кабинет: шапка-портал, плитки по смыслу, мои связки и события

**Дата:** 25.09.2026 · **Статус:** локальная ветка `visual/tz-visual`, в main не влито.

Визуалу кабинет казался «весь чёрный, ничего непонятно»: три серые карточки — имя,
пять одинаковых плиток, абзац про настройки. Кабинет переделан в рассказ о человеке
и его работе; структура одна в обоих режимах, движение — только в презентационном.
API и база не менялись: связки — `GET /api/cooperations?responsibleId=<id>`, события —
`GET /api/notifications`, у представителя вуза этих блоков нет (у него свой кабинет).

- **Шапка** — стеклянная панель с плывущим сиянием. Аватар-сфера (`profile/Orb.tsx`):
  внутри перетекают пятна цветов бренда, стеклянный блик, по кромке бежит градиент,
  дышит ореол. Вокруг по наклонной орбите летают связки человека (до 8), красные —
  где просрочка или блок, за каждой — хвост. Положения считаются каждый кадр
  проекцией в SVG: задняя половина орбиты и её планеты рисуются до сферы (тусклее,
  меньше), передняя — после. Первая версия на CSS 3D сплющивала боковые планеты
  в эллипсы и рисовала заднюю планету поверх сферы. Наведение на планету — подпись
  связки и пауза орбиты, щелчок — связка; сцена чуть наклоняется за курсором.
  Имя, должность, роль и почта — «таблетками», и одна фраза-сводка:
  «У вас 3 связки в 3 вузах · 5 этапов просрочено».
- **«Ваша работа»** — плитки с иконкой и светом цвета по смыслу, числа набегают;
  «Просроченные этапы» — красные, при нуле — зелёное «Просрочек нет». Доля этапов
  в срок — объёмным кольцом `Pie3D` (решение 95). Пояснения — во всплывающей подсказке.
- **«Мои связки»** — до шести связок карточками: вуз, программа, продукт, полоска
  этапов `StageBar`, текущий этап и статус; где горит — красная кромка слева.
- **«Последние события»** — шесть последних событий ленты на линии времени.
- **Настройки** — стеклянной карточкой посередине экрана (до 720 px) со строками,
  как на странице настроек (решение 91): режим и выход рядом с текстом, а не у
  дальнего правого края; пояснение про изменение профиля — за значком (i).

**Спокойно:** в рабочем режиме и при «уменьшить движение» сияние стоит
(`html[data-mode='work']`), сфера с орбитой — неподвижный кадр (`useCalmMotion`),
числа не считают. На 390 px — одна колонка, без
горизонтальной прокрутки.

**Изменения схемы:** нет. **Затрагивает фронт:** только страница `/profile`.

## 97. Отчёт руководителю: лист A4 для печати и PDF

**Дата:** 25.09.2026 · **Статус:** ветка `feat/manager-report`, в main не влито.

Решение Артура: «Отчёт руководителю PDF — да». Руководитель ИТ-Школы открывает
отчёт и за минуту видит состояние сотрудничества с вузами; отчёт печатают или
сохраняют в PDF и отправляют. Страница `/reports/portfolio`, ссылка — кнопкой
«Отчёт руководителю» в шапке главной (рядом с «Пересобрать рекомендации») и аналитики.

- **PDF — средствами браузера, без новой зависимости и без серверной генерации.**
  Кнопка «Печать / PDF» вызывает `window.print()`, в окне печати выбирают «Сохранить
  как PDF». Заголовок вкладки на время страницы — «Отчёт руководителю SkillLink
  25.09.2026»: это имя файла, которое браузер предложит. Серверный PDF (Chromium
  в контейнере или библиотека вёрстки PDF) — отдельная зависимость и отдельный
  маршрут API, ради одного листа не оправданы.
- **Данные — только существующие API.** Сводка главной `GET /api/analytics/overview`
  и списки дел `GET /api/workflow/overdue` и `/api/workflow/blocked` постранично
  (по 100, не больше 20 страниц). Главная показывает десять самых давних проблемных
  этапов, отчёт — все: этап, который есть в обоих списках (заблокирован и за сроком),
  считается одной строкой с причиной «заблокирован», как `problemStageTotal` на сервере.
  Краткие названия вузов — из той же сводки; списки дел отдают только полные.
  Сервер, контракт и схема не менялись.
- **Доступ** — как у сводки: право `ANALYTICS`. Представителю вуза сводка отвечает
  403, страница показывает этот отказ, как другие закрытые для него страницы;
  списки дел ему не запрашиваются.
- **Лист одинаков на экране и на бумаге.** На экране — светлый лист шириной 210 мм
  с полями 14 мм посреди рабочей области, поэтому переносы строк совпадают с печатью.
  Цвета — новые токены бумаги в `globals.css` (`--paper*`, `--ink*`, контраст подписей
  не ниже 4,5:1 на белом); значки дизайн-системы внутри листа получают «чернильные»
  оттенки через переопределённые на листе переменные, своих значков не заведено.
  Режимы интерфейса лист не различает: это документ, а не витрина.
- **Печать скрывает каркас точечно.** Правило `@media print` в `globals.css` действует,
  только пока на странице есть `[data-print-document]`: всё, что не лист и не его
  обёртка, скрыто (меню, шапка, подвал, поиск, живой фон, заголовок страницы
  с кнопкой), у обёрток сняты отступы каркаса, анимации выключены. Поля A4 —
  у именованной страницы `@page report`: безымянный `@page` действовал бы на печать
  любой страницы, если стили отчёта остались подключены после перехода. Проверено:
  главная и аналитика после посещения отчёта печатаются с меню, как раньше.
- **Разрывы.** Строка таблицы, показатель, пункт списка и пара «программы — навыки»
  не рвутся; заголовок блока не остаётся без содержимого; шапка таблицы повторяется.
  На демо-данных — две страницы: на первой шапка, показатели, «Требует внимания»
  и приоритетные действия, на второй программы, навыки и источник данных.
- **Тексты листа** — чистые функции `reports/portfolio/report.ts` с тестом: разбивка
  связок и воронка теми же словами, что на главной, сравнение за 30 дней, причина
  проблемы, «Нет данных» вместо нуля, строка об источнике с прямой пометкой
  демонстрационных данных — у получателя PDF нет подсказок интерфейса.

**Изменения схемы:** нет. **Затрагивает фронт:** новая страница `/reports/portfolio`,
`ROUTES.managerReport`, токены `--paper`, `--paper-line`, `--paper-line-faint`,
`--ink`, `--ink-secondary`, `--ink-tertiary`, `--ink-accent`, `--ink-danger`,
`--ink-warning`, `--ink-success`, `--ink-warm`; правило печати в `globals.css`.

## 98. Четыре решения Артура: рейтинг от нуля, дефициты вне профиля, четыре статуса рекомендаций, права аналитика

**Контекст.** Сводный разбор слабых мест (Д2, Д3, Б2 и решение 7 по рекомендациям);
Артур подтвердил все четыре рекомендации 25.09.

**Рейтинг — нормирование от нуля.** `normalized = value / max` вместо
`(value − min) / (max − min)`: min-max ставил 0,0 программе со 150 заявками и вдвое
ронял рейтинг её вуза. Ноль — только у нуля. Баллы вузов в сценарии: ДГТУ 36,6,
КНИТУ-КАИ 47,7, МТУСИ 50,7, НГТУ 47,6, СПбГУТ 85,7 (было 7,5 / 23,4 / 28 / 22,7 / 79,1);
порядок прежний. Сравнение внутри уровня (бакалавриат с бакалавриатом) — после защиты.

**Дефициты — пометка «вне профиля».** Дефициты считаются против всего рынка, и магистратуре
ИИ «не хватало» Java и JavaScript. Пока профиля направления нет, в разрезе одной программы
непокрытый навык помечается `outOfProfile`, если его не преподаёт ни одна программа той же
укрупнённой группы направлений (первые две цифры кода), а для других категорий, кроме
языков программирования, — и навыки той же категории. Такой дефицит не критический, идёт
в конце, на радар не выносится, в пояснении сказано почему (`SKILL_PROFILE`, TEMP). Сводки
по вузу и по всем программам не меняются — «2 критических дефицита» на главной те же.
Ограничение: на демо-наборе ни одна программа группы 09 не преподаёт Python, поэтому у них
Python — «вне профиля». Профиль направления вручную — после защиты.

**Рекомендации — четыре статуса:** Новая → В работе → Выполнена / Отклонена. «Принята»
упразднена: переходов в неё нет (409), миграция перевела такие записи в «В работе»
со снятием даты решения, `db:verify` следит, чтобы она не появилась снова (27 правил).
Значение перечисления в базе осталось — удаление значения enum в PostgreSQL означало бы
пересоздание типа ради мёртвого значения.

**Аналитик — реальные права.** Новое право `ANALYTICS_WORK` (ADMIN, MANAGER, ANALYST):
пересобрать рекомендации, вести их статусы, загрузить рыночные данные. Раньше аналитик
по правам равнялся наблюдателю. Связки, этапы, документы, справочники и письмо вузу
от имени ИТ-Школы — по-прежнему только ADMIN и MANAGER; наблюдатель — только чтение.

**Изменения схемы:** нет (миграция данных `20260925170000_recommendations_four_statuses`
без изменения структуры). **Затрагивает фронт:** `SkillGapDto.outOfProfile`,
`permissions.canWorkAnalytics`, `RECOMMENDATION_WORKFLOW_STATUSES` для фильтров.

## 99. Настройки администратора: пользователи, журнал, смена пароля, статус ИИ

**Дата:** 25.09.2026 · **Статус:** в main (#100), на стенде с 25.09.
Номера 97 и 98 заняты параллельными ветками.

ТЗ, п. 5: «Администратор — настройка системы: пользователи, роли, справочники».
До этого учётные записи заводились и блокировались только в базе, а журнал действий
отдавался API, но не был виден нигде. Артур выбрал пункты 1, 2, 3 и 6.

**1. Вкладка «Пользователи» (только `ADMIN`).** Новые вкладки встали в «Настройки»
Серёжи в том же стиле — строками (решение 91), колонка разделов показывает их только
администратору. Маршруты: `POST /api/users`, `GET`/`PATCH /api/users/:id`,
`POST /api/users/:id/password-reset`; `GET /api/users` получил фильтр `isActive`
обратно совместимо — формы выбора ответственного не затронуты.

- **Пароль придумывает сервер** и отдаёт один раз: 14 знаков из 54 без похожих
  (`0/O/o`, `1/l/I/i`), `crypto.randomInt`, около 80 бит; в базе — хеш bcrypt.
  Администратор не придумывает пароль сам — «Qwerty123» для всех новичков был бы
  самым вероятным исходом. Ответы `/api/users/*` — `Cache-Control: no-store`
  правилом в `next.config.ts`: заголовок, выставленный в маршруте, общее правило
  `no-cache` перекрывало.
- **Правила — чистыми функциями** (`auth.rules.ts`), сервис собирает факты: себя
  не заблокировать и не лишить `ADMIN`; нельзя оставить систему без действующего
  администратора; `MANAGER`/`ADMIN` с открытыми связками или этапами не переводится
  в роль, которая не может быть ответственной (`RESPONSIBLE_ROLES`) — 409 «сначала
  передайте связки». Блокировать ответственного можно: связки остаются за ним,
  интерфейс предупреждает, сколько их. Правило `assertStaffResponsible` не менялось.
- **«Последний администратор» — в транзакции** с `SELECT … FOR UPDATE` по строкам
  действующих администраторов и изменяемого: два администратора, одновременно
  блокирующие друг друга, иначе оба видели бы «есть ещё один».
- **Вуз**: у представителя обязателен, у остальных запрещён — то же правило, что
  сверяет `db:verify`. Смена роли с представителя снимает вуз сама.
- **Блокировка действует сразу**: `getCurrentUser()` и так читает пользователя из базы
  с `isActive: true` на каждый запрос — выданная сессия перестаёт работать
  со следующего запроса.
- **Свой пароль сбросом не меняется** (409): только в личном кабинете с текущим паролем,
  иначе оставленная открытой сессия администратора давала бы его пароль одним щелчком.
- **Новый пароль снимает блокировку входа** этой учётной записи (`releaseAccount`
  в `throttle.ts`): новый пароль чаще всего просят именно после пяти неудач.

**2. Вкладка «Журнал действий» (только `ADMIN`).** Фильтров API хватило (сотрудник,
действие, тип объекта, период). Добавлено: список действий и типов объектов —
в контракт (`AUDIT_ACTIONS`, `AUDIT_OBJECT_TYPES`), подписи — в `labels.ts`;
тип `AuditAction` журнала теперь берётся из того же списка, поэтому действие без
подписи не соберётся (тест `labels.test.ts`). В записи — `cooperationId` для этапа
и пункта чек-листа: своей страницы у них нет, ссылка ведёт на связку. Подробности —
кратко и без идентификаторов (`settings/audit-view.ts`); персональных данных
в журнале нет по правилу записи.

**3. Смена своего пароля** — `POST /api/me/password`, любая роль, только для себя.
Новый пароль не короче 10 символов, не длиннее 72 байт (предел bcrypt), не равен
текущему и почте. Текущий пароль проверяется через `throttledAttempt` — тот же
счётчик, что у входа: иначе форма стала бы обходом ограничения перебора для того,
кто завладел сессией. Неверный текущий — 422, исчерпаны попытки — 403.

**Признак «пароль временный» без изменения схемы.** Плашку «смените временный пароль»
в кабинете (`passwordTemporary` в `GET /api/me`) даёт последнее событие пароля
в журнале: `user.create`/`user.password.reset` — временный, `user.password.change` —
свой. Поле `password_temporary` в `users` было бы надёжнее (журнал чистится через год,
сбой записи журнала не ломает операцию), но ради необязательной подсказки схему
не трогали — вопрос к Тиграну, если понадобится обязательная смена.

**6. Статус ИИ-помощника** — блок `aiAssist` в `GET /api/integrations/status`:
провайдер, имя, готовность, модель, причина. Берётся из `LlmProvider.info()`,
а не из настроек: ключ и каталог в ответ не попадают — тест ищет в ответе любой
их фрагмент длиной 6 знаков. Прежние поля ответа не менялись.

**Чего нет (SECURITY_LIMITATIONS, раздел 2).** Смена и сброс пароля не завершают
уже выданные сессии: сессия — JWT на 8 часов, отзыва нет. Блокировка их закрывает,
но после разблокировки не истёкшая сессия снова действует. Решение — версия пароля
в токене и в базе; это поле, согласование с Тиграном. Обязательной смены временного
пароля и второго фактора для администратора нет.

**Проверено.** Модульные тесты правил, сервиса и статуса ИИ; пробник — весь путь
через настоящий вход NextAuth: заведение → вход по временному паролю → смена →
старый не подходит → блокировка (вход и сессия не работают) → разблокировка → сброс,
и 403 менеджеру и представителю вуза на каждом маршруте администратора.

**Изменения схемы:** нет. **Затрагивает фронт:** `CurrentUserDto.passwordTemporary`,
`IntegrationsStatusDto.aiAssist`, `AuditLogEntryDto.cooperationId` — новые поля,
прежние не менялись; новые DTO `ManagedUserDto`, `IssuedPasswordDto`, `PasswordChangedDto`.

**Общие демо-учётки защищены** (добавлено при слиянии). С 30.09 по 14.10 стенд смотрят
эксперты — одновременно и под одними и теми же демо-учётками. Один проверяющий, сменивший
пароль менеджера или заблокировавший его, закрыл бы вход всем остальным, а рядом никого
не будет. Поэтому засеянные демо-учётки (`SHARED_DEMO_ACCOUNTS`) не меняет никто —
409 с объяснением; в интерфейсе их поля и кнопки выключены, в профиле вместо смены пароля —
пояснение. Управление пользователями и смена пароля проверяются на заведённом пользователе.

## 100. Проверка «не робот» на входе: задача на вычисление после трёх неудач

**Дата:** 25.09.2026 · **Статус:** ветка `feat/login-captcha`.

**Зачем.** Ограничение перебора (решения 40, 53) закрывает вход после пяти неудач с адреса
и после пятидесяти по учётной записи со всех адресов. Между «первой опечаткой» и
«вход закрыт» ничего не было: перебор с многих адресов шёл по одной учётной записи
с полной скоростью до общего потолка, а на стенде под одной учётной записью входят
все проверяющие (`SHARED_DEMO_ACCOUNTS`, решение 99).

**Что сделано.**
- **Когда.** После трёх неудач подряд по учётной записи с одного адреса или десяти
  по ней со всех адресов за час (`LOGIN_CAPTCHA`, TEMP) вход отвечает кодом
  `captcha_required` — до запроса к базе и bcrypt, без засчитанной неудачи.
  Удачный вход требование снимает. Закрытый вход проверку не просит: человеку важнее
  узнать, что ждать 15 минут. Счётчик адреса по всем учётным записям её не включает —
  за одним адресом бывает целая аудитория.
- **Что за задача.** Сервер (`src/shared/auth/captcha.ts`) загадывает число
  от 0 до 100 000 и отдаёт SHA-256 от «соль + число» с подписью HMAC ключом,
  производным от `AUTH_SECRET`. Браузер перебирает числа в фоновом потоке
  (`captcha.worker.ts`, WebCrypto) — в среднем 50 тысяч хешей, доли секунды на ноутбуке,
  секунда-две на телефоне — и повторяет вход сам: второй раз «Войти» нажимать не нужно.
- **Проверка решения** — один хеш и одна подпись, синхронно: решение помечается
  использованным до первого `await`, два одновременных запроса с одним решением
  не пройдут оба. Срок жизни — 5 минут, внутри соли. Длина срока в соли фиксирована
  (13 цифр) и закрыта точкой: иначе цифру можно было бы перенести из числа в срок
  с тем же хешем и продлить решение в десять раз (известная ошибка такой схемы).
- **Почему своя, а не Yandex SmartCaptcha или reCAPTCHA.** Не нужны ключи и договор,
  данные посетителя никуда не уходят (152-ФЗ, reCAPTCHA — ещё и передача за рубеж),
  вход не зависит от доступности чужого сервиса все две недели проверки стенда.
  Картинок нет — задача доступна и незрячим, и с экранным чтецом.
- **Маршрут** `GET /api/login-challenge` — без входа, `Cache-Control: no-store`.
  Экран входа показывает строку состояния «Проверяем, что вход не автоматический…» →
  «Проверка «не робот» пройдена»; подпись под формой называет правило.

**Честная граница** (docs/SECURITY_LIMITATIONS.md): программа без браузера решает
такую задачу в десятки раз быстрее — задача удорожает перебор, а останавливает его
по-прежнему ограничение попыток. Использованные решения, как и счётчики, живут в памяти
процесса.

**Проверено.** Модульные тесты `captcha.test.ts` (решение, повтор, подделка подписи,
истёкшее, перенос цифры в срок, мусор в поле) и `throttle.test.ts` (когда нужна);
`npm run probe` — три неудачи, затем `captcha_required`, решённая задача пускает к проверке
пароля, повтор и подделка — снова `captcha_required`, пятая неудача закрывает вход.

## 101. Демо-данные во всех статусах; «нет данных по программе» не для отменённых связок

**Дата:** 25.09.2026 · **Статус:** ветка `feat/seed-all-statuses`.

**Зачем.** С 30.09 по 14.10 эксперты смотрят стенд сами. В наборе всё было «в работе»:
10 программ из 10 «действует», все рекомендации «новые», ни одной связки на паузе
или отменённой, ни одного вуза и документа в архиве. Проверяющий не видел, как система
ведёт себя с остальными статусами, — а они и есть повседневная работа.

**Что добавлено в `prisma/seed.ts`:**
- **IT-продукты** «планируется» (Платформа виртуальных лабораторий) и «выводится»
  (прежний учебный стенд сетей).
- **Вуз в архиве** — ТУСУР: переговоры год назад не пошли дальше знакомства.
- **Программы**: черновик (УрФУ, разработка игр), приостановленная (ДГТУ), в архиве (НГТУ).
- **Связки**: на паузе (ДГТУ — вуз пересматривает учебный план, заметка с причиной;
  следующий этап не начат, поэтому пауза не даёт просрочки) и отменённая (НГТУ — вуз
  выбрал собственный стенд; дата закрытия, запись в журнале, письмо вуза встречей
  «переписка»).
- **Документ в архиве** — первая редакция договора СПбГУТ, заменённая второй.
- **Рекомендации**: одна «в работе», две «отклонены» с причиной (застой на паузе,
  застой отменённой связки), одна «выполнена» с комментарием — на завершённой связке.

**Цифры сценария не сдвинулись** (`demo:check` — всё как в сценарии, 26 из 26):
рейтинг и аналитика считают только действующие программы вузов не в архиве
(`ACTIVE_PROGRAM_WHERE`); состав закрытых этапов подобран так, что доля этапов в срок
осталась 89,1 %; выполненная рекомендация — с приоритетом «высокий», как у правила при
короткой просрочке, чтобы в ленте не стоять среди открытых критичных.

**Найденная ошибка.** Правило «нет данных по программе» задумано только для программ,
с вузом которых идёт работа, но считало любые связки — и срабатывало на программу
с отменённой связкой. Теперь отменённые не в счёт (`recommendations.repo`,
`programRuleSelect`); завершённые — в счёт: курс прочитан, связь с вузом есть.

**Замечено для фронта.** Лента рекомендаций по умолчанию показывает все статусы по важности,
и закрытая критичная встала бы среди открытых. Предложено показывать по умолчанию
открытые, закрытые — по фильтру.

**Проверено.** `db:verify` — 21 правило; `demo:check` 26/26; `smoke`, `probe`,
модульные тесты.
