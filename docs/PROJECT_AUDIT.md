# PROJECT_AUDIT.md — аудит стартовой точки

Дата: 21.09.2026. Аудит проведён до написания кода.

## 1. Что было получено

| Файл | Содержание |
| --- | --- |
| `CLAUDE.md` | Постоянный контекст проекта, раздел «Принятые решения» |
| `docs/tz/skilllink-tz.md` | ТЗ продукта SkillLink v1.0 |
| `docs/concept.md` | Концепция, отправленная заказчику |
| `docs/design/README.md` | Пустая заглушка вместо макетов |

## 2. Чего не было

| Чего нет | Последствие | Что сделано |
| --- | --- | --- |
| `docs/tz/tech-lead-tz.md` | Один из трёх источников требований, названных в `CLAUDE.md`, недоступен | Работаем по ТЗ продукта и разделу «Принятые решения» — он объявлен главнее остальных источников. Зафиксировано в [TECHNICAL_DECISIONS.md](TECHNICAL_DECISIONS.md), решение 1 |
| **Стартового каркаса кода** | Раздел «Расхождения стартового каркаса с ТЗ» в `CLAUDE.md` описывает несуществующий код | Проект написан с нуля сразу по ТЗ. Все перечисленные расхождения учтены как требования |
| **Макетов в `docs/design/`** | Нельзя составить `DESIGN_INTEGRATION.md` по фактическим экранам | Составлен по экранам из ТЗ. Требует сверки с макетами — см. [DESIGN_INTEGRATION.md](DESIGN_INTEGRATION.md) |
| **Кода фронта** | Нечего подключать к API | Страницы в `src/app` — заглушка. Заменяются вёрсткой без изменений в API |

## 3. Состояние среды разработки

| Проверено | Было | Стало |
| --- | --- | --- |
| Node.js | не установлен | v26.8.1 |
| npm | не установлен | 11.19.0 |
| PostgreSQL | запущен `postgresql@16` (не был в `PATH`) | используется он же, база `skilllink` |
| Docker | не установлен | не установлен; `docker-compose.yml` в репозитории для команды |
| git | 2.50.1 | репозиторий инициализирован |

Docker Desktop требует установки через GUI и пароля администратора — это делает разработчик
самостоятельно. Для локальной работы он не обязателен: `docker-compose.yml` описывает тот же
`postgres:16-alpine`.

## 4. Расхождения, перечисленные в CLAUDE.md, и как они закрыты

| Требование | Как реализовано |
| --- | --- |
| `Program` → `EducationalProgram` | Модель называется `EducationalProgram`, таблица `educational_programs` |
| `parallelStreams` → `groupCount` | Поле `groupCount` / `group_count` |
| Добавить `level`, `duration`, `status` | `level` (ProgramLevel), `durationMonths`, `status` (ProgramStatus) |
| `ProgramSkill.coverage` → `level`, `importance`, `source`, `confidence` | Все четыре поля есть, `coverage` отсутствует |
| `Vacancy` и `VacancySkill` → `MarketDemand` | Одна агрегированная таблица `market_demand` с `source`, `period`, `confidence`, `is_mock` |
| `/api/analytics/market-demand` → `/api/skills/demand` | `GET /api/skills/demand` |
| `/api/analytics/skill-gap` → `/api/skills/gaps` | `GET /api/skills/gaps` |
| `src/lib/*` → `src/modules/*` и `src/shared/*` | Каталога `src/lib` нет |
| `rules.ts` → `shared/config/analytics.config.ts` | Все коэффициенты там, каждый помечен `// TEMP` |
| Добавить `code` в формат ошибки | `{ error: { code, message, details } }` во всех ответах |

## 5. Противоречия между документами и как они разрешены

| Противоречие | Разрешение |
| --- | --- |
| ТЗ (раздел 10) перечисляет 9 факторов аналитики; концепция говорит, что рейтинг строится по трём | Решение 7 в `CLAUDE.md`: рейтинг — только три показателя, остальные факторы показываются отдельными сигналами. Реализовано так |
| ТЗ (раздел 5) описывает 4 роли; концепция — 3 роли, включая представителя вуза | Решение 9: 4 роли ТЗ плюс `UNIVERSITY_REP`. В схеме и правах заведены все пять |
| Концепция обещает RLS в PostgreSQL; MVP не успевает | Решение 10: разграничение в сервисах, RLS — P2. Честно описано в [SECURITY_LIMITATIONS.md](SECURITY_LIMITATIONS.md) |
| Концепция обещает расчёт показателей представлениями СУБД | Решение 10 в [TECHNICAL_DECISIONS.md](TECHNICAL_DECISIONS.md): на MVP считаем в приложении, формулы не утверждены |
| ТЗ не говорит, входит ли этап 14 в процент выполнения | Решение 8 в [TECHNICAL_DECISIONS.md](TECHNICAL_DECISIONS.md): не входит, знаменатель 13 |

Новых противоречий, требующих решения Артура, при реализации P0 не обнаружено. Открытые
вопросы вынесены пометками `TODO: PM DECISION` в коде и перечислены в
[PROGRESS.md](PROGRESS.md).

## 6. Вывод

Репозиторий на старте содержал только документацию. Аудит существующего кода превратился
в фиксацию стартовой точки: проект создан с нуля по ТЗ, приоритет P0 закрыт полностью,
сквозной сценарий проходит.
