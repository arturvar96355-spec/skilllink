# Соответствие ТЗ РТК: где проверить каждый пункт

Таблица для построчной проверки: пункт технического задания заказчика → где он
сделан на стенде https://skilllink.site, какой маршрут API, какое решение в
[журнале](TECHNICAL_DECISIONS.md) и чем проверено. Учётные записи — в
[README](../README.md), раздел «Попробовать».

Статусы: **готово** — есть и экран, и API; **API** — есть API, проверяется через
[страницу API](https://skilllink.site/api-docs); **частично** — сделана часть,
остальное в плане развития и описано честно.

## Функциональные требования

| Пункт ТЗ | Статус | Где на стенде | API | Решение |
| --- | --- | --- | --- | --- |
| Файлы к статусам: png, jpeg, pdf, zip, gzip, rar, doc, docx, xls, xlsx | готово | карточка связки → этап → «Файлы»; «Документы» → документ → «Файлы» | `POST/GET /api/workflow/stages/{id}/files`, `POST/GET /api/documents/{id}/files`, `GET/DELETE /api/files/{id}` | 145, 149 |
| Приём данных с сайта и LMS в формате JSON с добавлением в существующий или новый workflow | API | нет экрана: это межсистемный вход | `POST /api/import/external` (ключ `INTEGRATION_TOKEN`) | 145 |
| Настраиваемый workflow (14 этапов — базовый вариант) | частично | «Настройки» → «Этапы работы»: название и нормативный срок каждого этапа | `GET /api/settings/workflow`, `PATCH /api/settings/workflow/stages/{номер}` | 146, 151 |
| Роль «Руководитель»: менять, назначать и снимать ответственных за вузы | готово | войти `head@skilllink.demo` → карточка вуза → «Сменить ответственного» / «Снять»; карточка связки → «Сменить» | `PATCH /api/universities/{id}/responsible`, `PATCH /api/cooperations/{id}` | 146, 151 |
| Отчёт: вуз, ИТ-направление, ИТ-продукт, статус работы с вузом, ответственный | готово | «Отчёты» → «Отчёт по ТЗ» | `GET /api/reports/tz?format=csv\|xlsx\|json` | 145, 150 |
| Каталог: вуз, вендор, ПО, номер договора, подписание лицензии, срок лицензии (год), статус по передаче, ФИО менеджера, ответственные от вуза, комментарий | готово | «Отчёты» → «Каталог по ТЗ»; карточка связки → «Лицензия и передача ПО» | `GET /api/reports/catalog?format=…`, `PATCH /api/cooperations/{id}` | 145, 150 |
| Изменение карточек, а не только создание: вуз и образовательная программа | готово | карточка вуза / программы → «Изменить», «В архив», «Вернуть из архива»; реестр → фильтр «В архиве» | `PATCH /api/universities/{id}`, `POST …/archive`, `POST …/restore` (так же у `/api/programs/{id}`) | 152 |
| Навыки образовательной программы | готово | карточка программы → вкладка «Навыки» → «Добавить навык», «Убрать» | `PUT /api/programs/{id}/skills` | 152 |
| Срок и ответственный этапа | готово | карточка связки → этап → «Изменить срок», «Сменить ответственного» | `PATCH /api/workflow/stages/{id}` | 153 |
| Разграничение доступа по ролям | готово | представитель вуза по прямой ссылке на чужой раздел видит «Раздел недоступен»; сервер отвечает 403 | проверка прав в каждом маршруте (`assertCan`) | 153 |
| Показатели на главной ведут к данным | готово | главная → «Этапы в срок» → связки с просрочкой; остальные показатели → свои реестры | — | 153 |

## Требования к решению

| Пункт ТЗ | Статус | Где проверить | Решение |
| --- | --- | --- | --- |
| Интерактивная страница Swagger | готово | https://skilllink.site/api-docs, спецификация — [openapi.json](openapi.json) | 146 |
| Результирующий JSON-файл | готово | «Скачать JSON» у обоих отчётов; `?format=json` | 145, 150 |
| Отчёты в xls/xlsx/pdf | готово | XLSX и CSV — файлом; PDF — «Печать / PDF» на странице отчёта (печатная вёрстка) | 145, 150 |
| 50 параллельных пользователей и 10 параллельных отчётов | готово | [OPERATIONS_TESTS.md](OPERATIONS_TESTS.md), раздел 6: 0 ошибок, p95 31,9 мс и 36 мс | 146 |
| Страница не должна каждый раз обновляться или сбрасываться | готово | переходы по разделам и ссылкам внутри страниц — без перезагрузки приложения (`next/link`); тест `src/ui/internal-links.test.ts` не пропускает обычную ссылку на свою страницу | 154 |
| Документация встроена в платформу | готово | «?» в шапке или «Справка» в подвале на любом экране → `/help`: связка, этапы, контрольные точки, роли, кабинет вуза, документы, словарь терминов | 154 |
| Авторизация через Keycloak | частично | [ARCHITECTURE.md](ARCHITECTURE.md), раздел «Авторизация и Keycloak»: вход на NextAuth + bcrypt, защита от перебора, сессии; путь перехода на Keycloak через OpenID Connect без изменения API | 146 |

## Что сделано частично — и почему

- **Конструктор нового workflow с нуля.** Можно переименовать любой из 14 этапов и
  поменять его нормативный срок, в том числе для уже идущих связок. Признак
  контрольной точки у этапов 6, 7 и 11 не меняется из настроек: на нём держится
  порядок прохождения этапов (решения 5 и 28, [CONTROL_POINTS.md](CONTROL_POINTS.md)),
  и экран объясняет это словами. Новые процессы с нуля — в плане развития.
- **Keycloak.** Осознанное решение по срокам, подробно — в
  [ARCHITECTURE.md](ARCHITECTURE.md). Ролевая модель и API не зависят от способа входа.
- **Приём из LMS и с сайта.** Контракт с заказчиком ещё не согласован (так сказано в
  самом ТЗ), поэтому на другом конце нет настоящего источника. Приёмник работает и
  проверяется через страницу API.

## Учётные записи для экспертов

`expert-manager@`, `expert-admin@`, `expert-rep@skilllink.demo` — только чтение и
выгрузки: сервер отклоняет любое изменение, а экран не показывает кнопок изменения
(решение 147). Пароль — по каналу, согласованному с организаторами.
