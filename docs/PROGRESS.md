# PROGRESS.md — прогресс, риски, незавершённое

Обновлено: 25.09.2026.

## Состояние

**ТЗ визуалу (25.09.2026, решение 91, локальная ветка `visual/tz-visual`, в main не влито).**
Контраст и кегль мелкого текста подняты токенами: на главной, в рекомендациях,
связках и настройках на 1280×720 нет текста мельче 12 px и контраста ниже 4,5:1.
Заголовок вуза — короткое имя, полное строкой ниже; кегль заголовков объекта меньше.
Убраны линейка прокрутки, штрихкод и номер на бирке, неиспользуемые `NextObject` и `Sankey`.
Бирка вуза сразу открывает страницу вуза — промежуточной панели «Связи вуза» нет.
Плашка входа — стеклянная, с бегущей по кромке линией, по образцу AuthKit (решение 92); 3D-фон и переход не менялись.
Экран входа в объёме: карточка наклоняется за курсором с бликом, левая колонка — слоями (решение 93); на телефоне неподвижно.
Главная в презентационном режиме — шкала просрочек, полоски этапов, разложенный балл, дефициты, сравнение за 30 дней (решение 94); объёмные 3D-кольца и колонки, всплывающая 3D-карточка связки вместо затемнения (решение 95).
Личный кабинет — шапка с аватаром-сферой и орбитой связок, плитки по смыслу, мои связки, события (решение 96).
«Настройки» — как у Claude: колонка разделов слева, строки «название — значение» справа. Презентацию `/presentation`
делает не визуал.

**Отчёт руководителю (25.09.2026, решение 97, ветка `feat/manager-report`).** Лист A4
`/reports/portfolio`: показатели, все проблемные этапы, приоритетные действия, лучшие
программы, навыки; «Печать / PDF» — окно печати браузера, без меню и кнопок, светлый лист.
Ссылка — в шапке главной и аналитики. API и база не менялись.

**Настройки администратора (25.09.2026, решение 99).**
ТЗ, п. 5 — Артур выбрал пункты 1, 2, 3 и 6: вкладки «Пользователи» (заведение с одноразовым
временным паролем, роль с вузом, блокировка, новый пароль; защиты от самоблокировки
и потери последнего администратора) и «Журнал действий» (фильтры, действия словами,
ссылки на объекты) — только для `ADMIN`; смена своего пароля в личном кабинете для любой
роли; статус ИИ-помощника во вкладке «Интеграции». Схема базы не менялась.

**P0 закрыт. P1 закрыт по серверной части. Интерфейс собран.**

```
npm run typecheck   без ошибок
npm test            823 теста проходят
npm run build       собирается, 47 страниц и маршрутов
npm run smoke       327 проверок проходят
npm run probe       209 проверок, проблем не найдено
npm run db:verify   27 правил целостности данных соблюдены (CI и каждая перезаливка стенда)
npm run bench       самая медленная страница 26 мс на 1000 вузов (23.09, с русской сортировкой)
npm run demo:check  стенд и запасной ноутбук совпадают со сценарием показа
```

**Интерфейс (22.09.2026).** Собран по дизайн-системе Вани и Серёжи — шесть документов
в `docs/design/`, решение 41 в TECHNICAL_DECISIONS.md, устройство в `docs/FRONTEND.md`.
Экраны: вход, дашборд, реестры вузов, программ, связок, документов и IT-продуктов,
карточки вуза, программы и связки с четырнадцатью этапами и чек-листами, рекомендации
с обоснованиями, аналитика (рейтинг, дефициты, спрос), личный кабинет, настройки,
кабинет представителя вуза, глобальный поиск и лента событий под колокольчиком.
Проверено в браузере на ширине 1440 и 390.

**Интерфейс, второй заход (23.09.2026).** Сверен с дизайн-системой, семь
расхождений закрыты. Добавлены продуктовые элементы вместо общих мест: лента
четырнадцати этапов с пометкой контрольных точек, схема связки «вуз → программа
→ IT-продукт», спрос рынка рядом с навыками программы, вкладка дефицитов,
рекомендации по связке на её карточке. Плотность реестров поднята с восьми
строк на экран до двенадцати. Создание вуза, программы и связки — из интерфейса.
Оформление держит проверка `design-tokens.test.ts`: своих цветов и размеров
шрифта на страницах быть не может.

**Проверено в контейнере.** Образ собран и запущен: сквозной сценарий и пробник проходят
полностью против контейнера и против полного стека `docker compose` (база + миграции +
приложение). Сборка не требует ни базы, ни секретов.

**Проверено на объёме.** На отдельной базе с 120 вузами, 480 программами, 360 связками,
5040 этапами и 10440 задачами все страницы отвечают быстрее 40 мс, генерация 360 рекомендаций —
за 0,3 с, групповая операция по 360 связкам — за 0,2 с.

**Интерфейс, визуальный заход (23.09.2026).** Реестры университетов, программ
и связок — строками-плашками (`DataTable appearance="cards"`), главная и рекомендации —
отдельными плашками, переход «карточка раскрывается в свою страницу» на View
Transitions, чёрно-фиолетовый живой фон за курсором. Решение 64. Плотность реестров
расходится с решением 44 — на сверку с Артуром.

**Интерфейс, удобство (24.09.2026).** Обход всех экранов: поиск в боковом меню,
лента этапов без обрезанных подписей, компактный рейтинг, карточка вуза без
выхода за край, подвал одной строкой. Решение 66.

**Реестры — лентой (24.09.2026).** Компактные карточки как в рекомендациях
(`appearance="list"`), 11–14 записей на экран — расходится с решением 44/65,
нужно согласовать с Артуром. Решение 69.

**Реестры разгружены (24.09.2026).** Меньше столбцов, уточнения в подсказках, ярко —
только название, статус — точкой. Решение 68.

**Реестры, оформление (24.09.2026).** Плотность прежняя (решение 65); шапка подписью,
разделители почти невидимые, строка под курсором — капсулой с меткой, появление
волной. Решение 67.

**Интерфейс по референсам (24.09.2026, решение 79).** Граф связей вуза (как Melius),
бирки с блоком фактов и «Весь список» (как A24), воронка как Bklit, главная — бегущая
строка событий, кольца, карта России; на программе — радар спроса и покрытия;
страницы вуза и программы с линейкой прокрутки; меню с мини-сводкой и
перекатыванием букв; нижняя
панель на телефоне. Без новых зависимостей и без изменений API и базы.

**Движение и 3D (24.09.2026, решения 73–76).** Реестры вузов и программ —
3D-каруселью бирок (Motion): листаются перетаскиванием, стрелками, тачпадом;
щелчок по центральной — остальные складываются в стопку, бирка перетекает
в страницу объекта; у программ свой вид бирки и оттенок. Переключатель
«Бирки / Список». На главной — воронка связок по пяти фазам (SVG, по образцу Bklit).
Вход: звёзды во весь экран закручиваются в диск и собирают главную по блокам;
сцена рисуется в фоновом потоке (OffscreenCanvas) с запасным путём на странице.
Прыжок проверен покадрово в Chrome без окна (60 кадров/с, след не остаётся);
настоящий вход с главной глазами проверил пользователь. Новая зависимость — `motion`.

**Интерфейс на объёме (23.09.2026).** База с 1004 вузами, 6003 программами, 3003 связками
и 42042 этапами: сводка дашборда 43 мс, реестр вузов с расчётом рейтинга 294 мс, список
связок 239 мс, карточка связки со всеми этапами и чек-листами 408 мс, рейтинг программ
197 мс, глобальный поиск по шести сущностям 375 мс. Фильтр по региону на такой базе
не показывается и не запрашивается вовсе: неполный список регионов хуже отсутствующего.

**Проверено на чистом клоне.** Репозиторий развёрнут с нуля в отдельный каталог, на пустую
базу, по инструкции из README: `npm install` → `npm run db:deploy` → `npm run db:seed` →
`npm run build` → `npm start`. Сквозной сценарий против production-сборки прошёл полностью —
273 проверки.

Сделана и часть P2: журнал действий, лента событий вуза, групповые операции по IT-продукту,
сборка пакета документов из шаблонов.
Интерфейс подключён к API целиком (22.09.2026): все экраны работают с настоящими данными.

## Сделано

### Инфраструктура

- Next.js 15.5.25, TypeScript strict, Zod 4 с русской локалью, Vitest 4.
- PostgreSQL 16, Prisma 7 через драйверный адаптер `@prisma/adapter-pg`.
- Миграции через `prisma migrate`, две применённые миграции.
- `docker-compose.yml`, `.env.example` без секретов, `.env` вне репозитория.
- `npm run typecheck` выполняет `next typegen` перед `tsc`: типы маршрутов всегда свежие.

### База данных

20 таблиц по разделу 11 ТЗ плюс `contacts` и `resolution_comment` в рекомендациях —
обе добавки описаны в [DATABASE_SCHEMA.md](DATABASE_SCHEMA.md) и требуют согласования
с Тиграном.

### Модули и эндпоинты — 65 маршрутов, 81 операция

| Модуль | Эндпоинты |
| --- | --- |
| health | `GET /api/health` |
| auth | `GET /api/me`; `POST /api/me/password`; `GET /api/login-challenge`; `GET`, `POST /api/users`; `GET`, `PATCH /api/users/:id`; `POST …/password-reset`; маршруты NextAuth в `/api/auth/*` |
| universities | `GET`, `POST /api/universities`; `GET`, `PATCH /api/universities/:id`; `POST …/archive`; `POST …/restore` |
| programs | `GET`, `POST /api/programs`; `GET`, `PATCH /api/programs/:id`; `PUT …/skills`; `POST …/archive`; `POST …/restore` |
| skills | `GET /api/skills`; `GET /api/skills/demand`; `GET /api/skills/gaps` |
| products | `GET /api/products`; `GET /api/products/:id` |
| cooperation | `GET`, `POST /api/cooperations`; `GET`, `PATCH /api/cooperations/:id`; `GET …/stages` |
| workflow | `PATCH /api/workflow/stages/:id`; `GET …/history`; `PATCH /api/workflow/tasks/:id`; `GET /api/workflow/overdue`; `GET /api/workflow/blocked` |
| analytics | `GET /api/analytics/overview`; `GET /api/analytics/programs` |
| recommendations | `POST /api/recommendations/generate`; `GET /api/recommendations`; `GET`, `PATCH /api/recommendations/:id` |
| documents | `GET`, `POST /api/documents`; `GET`, `PATCH /api/documents/:id`; `PATCH …/status`; `POST …/versions`; `GET /api/document-templates`; `POST /api/cooperations/:id/documents/generate` |
| meetings | `GET`, `POST /api/meetings`; `GET`, `PATCH /api/meetings/:id` |
| portal | `GET /api/portal/overview`; `GET /api/portal/materials`; `POST /api/portal/materials/:taskId/confirm`; `PATCH /api/portal/programs/:id/metrics`; `GET`, `POST /api/portal/applications` |
| data-sources | `GET /api/data-sources`; `POST /api/data-sources/sync`; `GET /api/integrations/status` |
| audit | `GET /api/audit`; `GET /api/universities/:id/events` |
| export | `GET /api/export` — выгрузка реестров в CSV |
| import | `POST /api/import` — загрузка реестров из CSV с предпросмотром |
| products (групповые операции) | `GET`, `POST /api/products/:id/release` |
| ai-assist | `POST /api/cooperations/:id/ai-summary`; `POST /api/recommendations/:id/ai-letter`; `POST /api/ai/today` — черновики ИИ-помощника, решение 90 |

### Workflow

14 этапов создаются при создании связки, с чек-листами и нормативными сроками. Машина
состояний по решению 3, условия переходов по решению 4, история с автором и временем,
автоматический пересчёт контрольного этапа 14, просроченные и заблокированные этапы.
**25.09:** просрочен только этап в работе или заблокированный; не начатый с вышедшим
сроком — «план сдвинут», в счётчики не идёт (решение 84, ТЗ Артура). На главной
5 проблемных этапов вместо 12 — по одному на связку.
**25.09:** демо-данные — даты по сюжету, сроки этапов 11–14 от начала занятий,
правдоподобная хронология этапов (решение 85, ТЗ Артура). На главной 4 проблемных
этапа, 186 дней до начала занятий; тренд за 30 дней на демо не пустой (подпись — задача фронта).
**25.09:** одно число связок на главной — разбивка по статусам с сервера: «7 активных
связей (6 в работе, 1 черновик)», «Показаны 6 из 7», воронка «8: 7 активных,
1 завершённая» (решение 86, ТЗ Артура).
**25.09:** подписанный договор сам отмечает три пункта этапа 6 — по правилам отметки
руками и не за контрольной точкой; у чек-листа видно, какие документы подписаны
(решение 87, ТЗ Артура).
**25.09:** решения Артура (98): рейтинг от нуля — баллы вузов 36,6 / 47,7 / 50,7 / 47,6 / 85,7;
дефициты «вне профиля» в разрезе программы; рекомендации — четыре статуса; аналитик
пересобирает рекомендации, ведёт их статусы и загружает рыночные данные.
**25.09:** 152-ФЗ в коде — политика `/privacy` без входа, почта контактов в выгрузке только
ADMIN и MANAGER, обезличивание контакта администратором, сроки хранения журнала скриптом
(решение 88, docs/PRIVACY.md). Настройки сервера (журнал Caddy, пределы контейнеров,
роль базы) — включены в тот же день (решение 89): журнал запросов Caddy, без заголовка
`Server`, пределы контейнеров, приложение ходит в базу ролью без суперпользователя.
**25.09:** почта и телефон контактных лиц вузов — только ADMIN и MANAGER, представителю —
своего вуза; аналитику и наблюдателю `null` и `contactDetailsHidden: true`, ФИО и должность
видны (решение владельца, решение 106). Право `CONTACT_DETAILS`, флаг
`permissions.canSeeContactDetails` в `/api/me`; пробник проверяет карточку, поиск и выгрузку.

### Ограничение перебора пароля

- Пять неудачных попыток подряд закрывают учётную запись на 15 минут, удачный вход
  счётчик обнуляет. Значения в `src/shared/config/auth.config.ts`, помечены TEMP.
- Проверка до запроса к базе и до bcrypt: заблокированная попытка ничего не стоит.
- Ответ не отличается от «неверный пароль» — иначе перебираются существующие адреса.
- Попытки во время блокировки её не продлевают: иначе перебор стал бы способом
  заблокировать чужой вход.
- Счётчик в памяти процесса — ограничение описано в `docs/SECURITY_LIMITATIONS.md`.

### Демонстрационный набор

- Пять показателей дашборда заполнены сразу после `npm run db:seed` — раньше два
  из них выглядели сломанными на свежей установке.
- **Журнал действий заполняется посевом.** Раньше посев писал историю этапов,
  но не журнал: администратор видел пустой раздел при трёх десятках завершённых
  этапов, а показатель «операций на связку» — «Нет данных».
- **Часть завершённых этапов закрыта с опозданием** (`lateStages`). Без них
  показатель «этапы, закрытые в срок» по построению равнялся 100 %, и контроль
  сроков на демонстрации нечем было показать. Сейчас 87,9 %.
- **Рекомендации генерируются посевом** настоящим движком правил, а не сочиняются.
  Без этого дашборд открывался с пустым блоком «приоритетные действия»: рекомендации
  появлялись только после явного нажатия, и раздел выглядел неработающим.
- Все пять правил рекомендаций срабатывают на демо-данных.
- Повторный посев не удваивает записи.

### Закрытая связка не требует внимания

- Закрытые связки исключены из просроченных, заблокированных и проблемных
  на дашборде. Раньше сотрудничество, доведённое до конца или отменённое,
  продолжало требовать действий вечно — при том, что его этапы заморожены
  и изменить их нельзя.
- Генератор рекомендаций всегда работал только по действующим связкам:
  система противоречила сама себе.
- В демонстрационном наборе появилась завершённая связка — и для показа итога,
  и чтобы правилу было на чём проверяться.
- Из тех же списков убран **этап 14**: он вычисляется автоматически, руками
  не меняется, и просрочен ровно потому, что не закрыты этапы 1–13 — они
  в списке уже есть. Иначе одна задержка считалась бы дважды.

### Проверка на объёме

- `npm run volume` наполняет отдельную базу (работает только с именем, содержащим `volume`).
- `npm run bench` замеряет время ответа против запущенного сервера.
- На 1 000 вузов / 6 000 программ / 42 000 этапов самая медленная страница — 35 мс.

### Контрольные точки (решение Артура, пункт 13)

- Порядок этапов остаётся гибридным; контрольными точками сделаны этапы **6, 7, 11**.
- Контрольную точку нельзя начать и нельзя завершить, пока не закрыты предыдущие.
- Блокировка и отмена не ограничены: они ничего не утверждают о выполненной работе.
- Отменённый этап считается закрытым — иначе отмена необязательного этапа 5
  заперла бы подписание навсегда.
- В отказе приходит список конкретных мешающих этапов, а не «переход недопустим».
- Состав — одна строка `CONTROL_POINT_STAGES`, обоснование в `docs/CONTROL_POINTS.md`.

### Рейтинг вуза (пункт 7.2 ТЗ)

- Агрегируется из рейтингов программ вуза: новых факторов не вводится.
- Нормирование по всей выборке программ, а не внутри вуза — иначе вуз с одной программой
  всегда получал бы 100 баллов.
- Программа без данных не входит в среднее и не тянет балл вниз (решение 8).
- Приходит в реестре по умолчанию (решение Артура, пункт 11); `withRating=false`
  отключает расчёт. Фильтр `minRating` / `maxRating` и сортировка `sort=rating`
  закрывают требование пункта 7.2 ТЗ.
- В карточке вуза считается всегда и раскрывается сильнейшей программой.
- Требует права `ANALYTICS`: представителю вуза закрыты и показатель, и фильтр,
  и сортировка по нему — иначе баллы чужих вузов восстанавливались бы по порядку выдачи.

### Аналитика и рекомендации

Рейтинг программ по трём показателям с раскрытием вклада каждого, skill gap с порогами,
дашборд с происхождением каждого показателя, блок приоритетных действий.

Пять правил генерации рекомендаций: просроченный этап, застой связки, связка без продукта
на этапе оформления, недостающие показатели программы, критичный дефицит навыка при наличии
подходящего продукта. У каждой рекомендации — основание, связанные данные, приоритет,
уверенность и статус. Повторная генерация не плодит дубликаты и не переписывает решение
сотрудника.

**ИИ-помощник (25.09.2026, решение 90).** Черновики по фактам правил: сводка по связке,
письмо вузу по рекомендации, «что сделать сегодня». Российская модель — YandexGPT,
запасной GigaChat; по умолчанию выключен (`AI_ASSIST_PROVIDER=off`), и тот же текст
пишет шаблон. Модель не решает, что рекомендовать, и не меняет данных; персональные
данные в неё не уходят; сбой модели — шаблон, а не 500. Лимит 20 обращений в час
на пользователя, кэш 10 минут, журнал `ai.draft`. Включать ли на показе — `TODO: PM DECISION`
(Артур).

### Кабинет представителя вуза

Роль видит только свой вуз. Аналитика, рейтинги, рекомендации, справочник пользователей и
другие вузы закрыты. Внутренние комментарии к этапам, причины блокировок и заметки по связке
не отдаются. Представитель подтверждает получение материалов, вносит численность обучающихся
и количество групп, подаёт заявки на обучение — `applicationCount` пересчитывается по ним.

### Аутентификация

NextAuth.js с сессиями на JWT, пароли хешами bcrypt. Роль перечитывается из базы при каждом
запросе. Демо-режим переключения ролей без пароля сохранён для защиты проекта и выключается
переменной `DEMO_AUTH_ENABLED`; при выключенном режиме запрос без сессии получает 401.

**Пользователи и пароли (25.09.2026, решение 99).** Администратор заводит пользователей
в «Настройках» — сервер выдаёт временный пароль один раз, в базе только хеш; меняет ФИО,
должность, роль с вузом представителя, блокирует и выдаёт новый пароль. Блокировка
действует сразу, в том числе на выданные сессии. Любая роль меняет свой пароль в личном
кабинете; перебор текущего пароля ограничен тем же счётчиком, что и вход. Пробник
проходит весь путь: заведение → вход по временному паролю → смена → блокировка → разблокировка.

### Групповые операции по IT-продукту

Выпуск новой версии продукта одним действием ставит обязательную задачу во всех связках
с этим продуктом и переоткрывает закрытые этапы передачи материалов. Есть предпросмотр:
менеджер видит список затронутых связок и причину по каждой до применения. Отменённые
этапы операция не трогает — их переоткрывает только администратор поштучно.

### Сборка документов из шаблонов

Пять шаблонов с автоподстановкой реквизитов вуза, программы, продукта, ответственного
и контактного лица. Недостающий реквизит заменяется видимым прочерком и перечисляется
в ответе — документ не притворяется заполненным. Повторная сборка не плодит дубликаты.

### Спецификация OpenAPI

`docs/openapi.json` и `GET /api/openapi.json` — 64 пути, 81 операция. Собирается из тех же
Zod-схем, которыми API проверяет вход, поэтому не расходится с кодом. Полнота проверяется
тестом: маршрут без описания роняет сборку. Закрывает обещание концепции об описании
интеграционных интерфейсов по спецификации OpenAPI.

### Контейнеризация

`Dockerfile` (многоступенчатый, standalone-сборка, образ 331 МБ, запуск не от root,
HEALTHCHECK) и `docker-compose.yml` с тремя службами: база, разовая задача миграций
и приложение. Непрерывная интеграция — `.github/workflows/ci.yml`: типы, тесты,
актуальность спецификации, сборка, сквозной сценарий и пробник.

### Выгрузка в CSV

`GET /api/export` — вузы, программы, связки и дефициты навыков. Файл для Excel:
UTF-8 с BOM, разделитель — точка с запятой, значения вида `=…` обезврежены. Права
совпадают с правами соответствующего раздела: выгрузка не обходной путь к данным.

### Загрузка из CSV

`POST /api/import` — вузы и программы. По умолчанию предпросмотр: запись только при
`mode=apply`. Колонки совпадают с выгрузкой, поэтому цикл «выгрузил → поправил → загрузил»
работает. Записи опознаются по названию — повторная загрузка не плодит двойников.
Строка с ошибкой не отменяет остальные, её номер совпадает с номером строки в Excel.

### Журнал и лента событий

`GET /api/audit` — технический журнал для администратора, с 25.09.2026 — вкладка
«Настройки → Журнал действий» (решение 99). `GET /api/universities/:id/events` —
человекочитаемая лента: этапы, документы, встречи и заявки по вузу.

### Интеграции

`src/integrations`: интерфейс `MarketDataProvider` с реализациями Mock, Csv, ExternalApi,
FutureRtk; клиенты LMS и сайта. Единый HTTP-клиент с таймаутом, повторами, ограничением
частоты и журналированием без тела ответа. Интеграции по умолчанию выключены, их сбой
не ломает систему. Состояние ИИ-помощника (провайдер, модель, готовность, без ключей) —
в `GET /api/integrations/status` и во вкладке «Интеграции» (решение 99).

### Демонстрационные данные

7 вузов (один в архиве), 13 программ (10 действующих, черновик, приостановленная, архивная),
18 навыков, 36 рыночных показателей за два периода, 6 IT-продуктов (4 действующих,
планируемый, выводимый), 10 связок (черновик, 6 в работе, на паузе, завершённая,
отменённая), 140 этапов, 290 пунктов чек-листов, 9 документов, 5 встреч, 13 рекомендаций
во всех статусах, 6 пользователей (включая представителя вуза). Решение 101.

Набор специально устроен так, чтобы на демонстрации было видно всё: программы без показателей,
просроченные этапы, заблокированный этап, отменённый этап 5, связка без выбранного продукта
на этапе оформления, застоявшаяся связка, два критичных дефицита навыков, документы во всех
статусах. Срабатывают все пять правил рекомендаций.

## Найдено и исправлено при реализации

1. **`.partial()` в Zod не снимает `.default()`.** Пустой `PATCH` молча менял статус записи.
   Затрагивало вузы, программы, связки. Исправлено, закреплено тестами.
2. **`orderBy` с `nulls: 'last'` для non-nullable полей.** Список связок падал с 500.
   Добавлен хелпер `buildOrderBy`, в сквозной сценарий — проверки обоих видов сортировки.
3. **Демо-набор не показывал критичных дефицитов** — главный сигнал системы не был виден.
4. **Спред перезаписывал `id` в `universities.repo.findById`.** Представитель вуза, запросив
   чужой вуз, получил бы свой вместо 404.
5. **Представитель без назначенного вуза видел бы все вузы.** Теперь такой запрос отклоняется.
6. **Комментарий сотрудника затирал обоснование рекомендации.** Заведено отдельное поле.
7. **Правило застоя не срабатывало на начатом этапе** — обобщено на любой незакрытый этап.
8. **Демо-заявки не совпадали с `applicationCount`.** Первая же поданная заявка меняла
   показатель с 420 на 120. Seed приведён в соответствие и сверяет суммы.
9. **`DEFAULT_ROLE_ORDER` не работал:** порядок элементов `IN` в SQL не сохраняется,
   и демо-пользователем оказывался администратор вместо менеджера.
10. **Клиент Prisma создавался при импорте модуля** — это делало нетестируемым всё,
    что транзитивно его тянет. Теперь создаётся лениво.
11. **На чистом клоне падала сборка:** `src/generated` не попадает в репозиторий,
    а `db:deploy` клиент не генерирует. Добавлен `postinstall`. В рабочей копии проблема
    не всплывала — клиент там уже лежал, и поймать её удалось только разворачиванием
    репозитория с нуля.
12. **Сквозной сценарий молча пропускал проверки** на повторном прогоне без пересева:
    искал неподтверждённый материал и закрытый этап, которых после первого прогона
    не оставалось. Теперь сценарий сам готовит себе предусловия.

### Найдено пробником (`npm run probe`)

13. **Фильтр `?universityId=` у представителя вуза молча игнорировался:** на запрос программ
    чужого вуза отдавались свои. Не утечка, но фронт показал бы их под чужим заголовком.
14. То же для встреч, документов и выгрузки.
15. **С завершённого этапа снимался обязательный пункт** — этап оставался завершённым
    с незакрытым обязательным пунктом.
16. **Закрытую связку можно было править**, передав ей её же статус в теле запроса.
17. **Переоткрытие не снимало дату закрытия.**
18. **У закрытой связки двигались этапы**, менялся чек-лист и собирался пакет документов.
19. **Дефициты навыков за лимитом показа закрывались как выполненные** — система заявляла,
    что проблема решена, хотя она осталась.
20. **Двойной клик по «Завершить этап» проходил дважды** и создавал дубль в истории.
    Переходы этапов и документов переведены на условное обновление: если состояние
    изменилось между чтением и записью, приходит 409.
21. **Двойной запуск генерации рекомендаций** падал с непонятным 409 про уникальность.
22. **«Операций на связку» показывал 0** на пустом журнале — то есть «усилий не требуется»
    вместо «ещё не знаем». По решению 8 это «Нет данных».
23. **У программы не было возврата из архива**, хотя у вуза он был: убранную по ошибке
    программу нечем было вернуть.

### Найдено настоящей сборкой образа

24. **`npm install` падал без `DATABASE_URL`:** `postinstall` вызывает `prisma generate`,
    а конфигурация Prisma требовала переменную через `env()`, который бросает исключение.
    Генерации клиента подключение не нужно. Ломало и установку по README, где
    `npm install` идёт до создания `.env`.
25. **`npm run build` падал в продакшене без `AUTH_SECRET`.** Сборка не выпускает токенов,
    и требовать там боевой секрет нельзя: иначе образ не собрать ни в CI, ни где-либо,
    куда секреты попадают только на запуске.
26. **Задача миграции в compose не работала:** в рантайм-образе нет CLI Prisma.
    Собирается из сборочной ступени.
27. **Проверка живости рапортовала «здоров» на базе без таблиц.** Теперь видит
    неприменённые миграции и отдаёт 503.
28. **Контейнерная база конфликтовала с локальной** по порту 5432.

### Найдено при подготовке к облаку (22.09.2026)

| Что | Как нашлось | Что сделано |
| --- | --- | --- |
| **Кириллица сортировалась неправильно** — на macOS почти случайно, в `postgres:16-alpine` по кодам символов | Сравнение порядка из базы с `Intl.Collator('ru')` на двух постгресах | ICU-сортировка на 12 колонках, тест и раздел пробника. Изменение схемы — на согласовании у Тиграна |
| **После входа через HTTPS-прокси — переход на `localhost`** | Вход с заголовками прокси против промышленной сборки | `AUTH_URL` / `DOCKER_AUTH_URL`, решение 36 |
| **По README секрет не попадал в контейнер** | `docker compose config` с переменной из инструкции | Исправлены README и HANDOFF, тест сверяет документацию с compose |
| **`npm audit`: 6 уязвимостей, 5 high** | `npm audit` | Точечная подмена трёх вложенных версий, решение 34 |
| **Приложение поднималось раньше миграций** | Первое настоящее развёртывание в Ubuntu 24.04 | Порядок база → миграции → приложение; Caddy ждёт здорового приложения |
| **Проверка живости глотала ошибку базы** и на любую проблему советовала запустить PostgreSQL | Там он работал, а расходился пароль | Разбор ошибки драйвера: `auth-failed`, `database-missing`, `unreachable`; полная ошибка в журнал |
| **Порты базы и приложения открывались на всех интерфейсах** | Разбор compose перед выкладкой | Привязаны к `127.0.0.1`: на машине с публичным IP база с паролем по умолчанию была бы видна из интернета |
| **Пароль демо-пользователей `skilllink` записан в документации** | Разбор перед выкладкой | На стенде задаётся случайный через `SEED_DEMO_PASSWORD` |

Проверено и в порядке: тесты в часовых поясах UTC, +14 и −11; поиск без учёта
регистра по-русски в `postgres:16-alpine`; сессия внутреннего запроса `apiFetch`
за HTTPS-прокси.

### Найдено сверкой стенда со сценарием показа (23.09.2026)

Решение 45 в TECHNICAL_DECISIONS.md.

| Что | Как нашлось | Что сделано |
| --- | --- | --- |
| **Лента рекомендаций шла от неважного к важному** — первой «Связка без движения» средней важности, три критичные просрочки внизу | `npm run demo:check` против стенда | `sort=-priority` через общую константу в ленте и во вкладке карточки связки; раздел пробника |
| **Страницы реестров теряли и повторяли строки** при равных значениях поля сортировки: 6 программ из 44, 6 связок из 40, 8 вузов из 51 | Обратный эксперимент к проверке ленты | `id` последним ключом во всех списках с постраничной выдачей; пробник обходит четыре реестра |
| **Числа сценария расходятся с экраном через двое суток после заливки** — 12 проблемных этапов становятся 13, 14, 16 | Подсчёт по базе со сдвигом времени | `scripts/deploy/reseed.sh` — перезаливка без пересборки; `npm run demo:check` — сверка со сценарием |
| **Образ миграций на стенде не обновлялся с первой выкладки** | Сравнение дат образов на сервере | `remote-up.sh` собирает его при каждом развёртывании |
| **В сценарии верхнее действие называлось короче**, чем на экране | `npm run demo:check` | Сценарий приведён к экрану |
| **Финал показа открылся бы кабинетом вуза**: вход представителем на шаге 5 заменял сессию менеджера во всех вкладках, а главная у представителя перенаправляет в кабинет | Прогон шпаргалки по шагам | Представитель — в окне инкогнито со входом заранее; основное окно остаётся под менеджером |
| **После перезаливки стенд открывался пустым экраном** у всех, кто входил раньше: вход возвращал на главную, главная — на вход | Боевая сборка с выключенным демо-входом, перезаливка при открытой вкладке | Сессия снимается, вход открывается с `reauth=1`; пробник и `check.sh`; решение 48 |
| **Сквозной сценарий падал на рабочей базе** по «сработало правило»: смотрел первые 50 рекомендаций из 53 | Прогон на рабочей базе | Читает все страницы |
| **Просрочки вытеснялись из ленты уведомлений** свежими изменениями: в ленте из 50 — ноль из пяти | Пробник на рабочей базе | Просрочки попадают в показанную часть всегда; решение 50 |
| **Пробник сравнивал обход по рейтингу со всей базой**, а выдача ограничена сотней | Рабочая база переросла 100 вузов | Сверка с первыми строками выдачи |
| **Поиск «_» или «%» находил всё**: знаки LIKE не экранировались, во всех девяти списках с `q` | Проверка поиска спецсимволами | `textContains` в `shared/db`; тест и пробник |
| **Сквозной сценарий и пробник падали на базе больше сотни записей** — сравнивали первую страницу со всей базой | Рабочая база переросла 100 программ | Сверка по уникальному суффиксу и по пересечению выдач |
| **Модальные окна не держали фокус**: после открытия «Создать связку» Tab уводил на меню под окном | Проверка клавиатуры в браузере | Фокус на первое поле, по кругу внутри, возврат на кнопку после закрытия; `Modal` и `Drawer` |
| **У неподписанного документа — «Подписан: Нет данных»** | Просмотр реестра и карточки документа | «Не подписан»: отсутствие даты здесь — факт, а не нехватка данных |
| **У главной связки показа договор значился подписанным** при чек-листе подписания 0 из 3: документы и встречи набора ссылались на связки по номеру, а номера съехали | Вкладки карточки связки в браузере | Ссылки по ключу «вуз-программа»; решение 51 |
| **Порядок рекомендаций при равной важности был случайным**: верхнее действие на главной менялось от перезаливки к перезаливке | `npm run demo:check` после перезаливки | Упорядочивание черновиков по важности и остроте; решение 51 |
| **README и SECURITY_LIMITATIONS описывали стенд до выкладки**: «авторизация — заглушка», «стенд работает по HTTP», «демо-режим не выключен» — жюри увидело бы это рядом с https://skilllink.site | Сверка документов со стендом | Переписано по факту: HTTPS, cookie `Secure`/`HttpOnly`/`SameSite=Lax`, вход без пароля выключен — всё проверено на стенде |
| **Встречу нельзя было записать из интерфейса** — только посмотреть, хотя API было готово | Обход вкладок карточки связки | «Записать встречу» во вкладке «Встречи»: тема, дата и время, формат, итог, следующий шаг со сроком, ответственный |
| **Отметка развёрнутого коммита писалась до сборки**: сервер оборвал соединение посреди сборки, отметка говорила «новая версия», работала старая — обновление по расписанию не стало бы повторять | Настоящий обрыв при выкладке 23.09 | Отметка — только после подъёма стенда; как действовать при обрыве — в DEPLOY.md |
| **«Щёлкнуть по «Рейтингу» — сильнейшие сверху» было неправдой**: первый щелчок сортировал по возрастанию, сверху ДГТУ с 7,5; заголовки таблиц не сортировались с клавиатуры | Проверка фраз шпаргалки в браузере | Баллы, количества и даты обновления с первого щелчка — по убыванию; заголовок получает фокус, Enter и пробел сортируют |
| **На телефоне главная уезжала вбок на 364 px**: в узкой раскладке сетка складывается в одну колонку `1fr`, и таблица распирала и её | Замер на ширине 375 | Одиночные колонки тоже `minmax(0, 1fr)`; сторож ловит и их |
| **На проекторе 1280×720 главная уезжала вбок на 26 px, этап 14 ленты прятался под прокрутку** | Замер выхода за край на 1280 и 1024 | Колонки сетки — `minmax(0, …)`, этап от 56 px; сторож в `design-tokens.test.ts` |
| **Пример `useMutation` в FRONTEND.md** — по старой схеме, которая теряла отказ контрольной точки | Сверка документации с кодом | Пример исправлен |
| **Списки выбора обрезались на сотне**: связку с вузом № 987 из 1 004 нельзя было создать, в фильтр документов попадала одна связка из тридцати | Выпадающие списки на объёмной базе | `RemoteSelect` — первые 50 и поиск на сервере; решение 46 |
| **Escape в выпадающем списке закрывал и модальное окно** со всем введённым | Проверка клавиатуры в форме «Создать связку» | Стопка слоёв: Escape закрывает верхний; модульный тест |
| **«Просрочен на 0 дн.»** в день срока; дни до срока — по суткам UTC, а даты — по Москве | Разбор расчёта сроков | Дни по московским суткам, в день срока — «сегодня»; решение 47 |
| **Пример `daysOverdue` в контракте с неверным знаком** | Сверка контракта с ответом API | Пример исправлен, знак описан |
| **Символ с кодом 0 давал 500 на двадцати адресах API**: поиск, карточки по `id`, создание, загрузку CSV; вход отвечал «ошибка конфигурации» | Обстрел всех маршрутов мусором на локальном сервере | Остановка на входе: тело и параметры — 422 с полем, путь — 404, CSV — «не похож на текстовый»; решение 52 |
| **«Студентов: 99 999 999 999» — 500 на записи**, импорт обещал «Будет создан» и падал | Тот же обстрел | `countSchema()` с пределом колонки `Int`, тот же предел в импорте; сторож на целые без границы |
| **Импорт отдавал клиенту текст ошибки Prisma** с данными строки, журнал — вызов целиком с ФИО и контактами | Разбор ответа импорта | Наружу — только `toAppError`, в журнал — причина без данных |
| **После входа — переход на чужой сайт** по ссылке `/login?reauth=1&from=//evil.example` | Сквозная проверка кода | `safeReturnPath`: только свой сайт; решение 53 |
| **Сто одновременных попыток входа — сто догадок вместо пяти** | Сквозная проверка кода, тест на параллельные попытки | Попытка засчитывается до проверки пароля |
| **Тело запроса читалось целиком**: наблюдатель мог прислать на импорт гигабайты | Сквозная проверка кода, проверка передачей chunked | Чтение кусками с обрывом на пределе; права на импорт — до чтения |
| **Причина отклонения документа уходила представителю вуза** | Сквозная проверка кода | Скрыта, как в истории этапов; раздел пробника |
| **Панель рекомендации не закрывалась** (шаг 4 показа): загрузка отдавала данные прежнего адреса | Сквозная проверка интерфейса; в браузере — Escape меняет адрес, панель висит | Данные — только своего адреса, прежние — только спискам по их просьбе; решение 54 |
| **Этап 14 и прогресс не обновлялись** после «Начать этап» | Сквозная проверка интерфейса, новая связка в браузере | Связка перечитывается после смены статуса |
| **Завершённый этап терял результат, заблокированный — причину** правкой поля или пустым значением в теле | Сквозная проверка кода; пробник | Проверяется и записывается итог записи; решение 55 |
| **Одновременные закрытия этапов оставляли этап 14 «в работе»** при закрытых 1–13 (3 из 3 в пробнике) | Сквозная проверка кода; пробник с контрольным опытом | Изменения этапов связки — по очереди (`FOR UPDATE` на связке), повторная проверка под блокировкой |
| **Демонстрационный набор считал этап 14 своей копией правила**: у УрФУ этап 1 в работе, этап 14 «Не начат» | Сквозная проверка кода | Одно правило для системы и набора; сверка со сценарием 25 из 25 |
| **Рекомендация, закрытая системой, не возвращалась**: новая просрочка по той же связке оставалась «Выполнена» и не видна нигде | Сквозная проверка кода; пробник с контрольным опытом | Закрытая системой запись открывается при повторе; решение 56 |
| **«Связка без движения» считала от правки записи связки**, а не от работы по этапам; набор подделывал застой у связки пятидневной давности | Сквозная проверка кода, разбор ленты набора | Движение — смена статуса этапа, отметка в чек-листе, правка связки; подделка убрана |
| **В день срока главная писала «просрочен», а рекомендации молчали** — и срабатывал застой | Лента набора после исправления застоя | Просрочка с момента срока везде |
| **Встреча или документ связывали разные вузы**: представитель видел чужую программу и ФИО чужого контакта; ответственным мог быть представитель другого вуза | Сквозная проверка кода; пробник с контрольным опытом | Одна проверка привязок на оба модуля; решение 57 |
| **Счётчики «связок у продукта» и «программ с навыком» — по всем вузам** и представителю тоже | Сквозная проверка кода | `_count` с отбором по вузу пользователя |
| **Журнал не писал создание и изменение вузов и программ и пересчёт этапа 14**, хотя SECURITY_LIMITATIONS это обещал | Сквозная проверка кода | Записи добавлены, без значений полей |
| **Двойное нажатие плодило «версии 2» и пакеты документов**; утверждённый документ можно было опустошить и подписать | Сквозная проверка кода; пробник с контрольным опытом | Транзакции с условным захватом; итог записи — по правилам статуса; решение 58 |
| **Связка создавалась сразу закрытой**, несуществующий продукт в правке — «Связка не найдена» | Сквозная проверка кода | Новая — только открытая; продукт проверяется |
| **Главная выдавала демо-данные за настоящие**: у всех показателей `isMock: false`; время до занятий по плановым датам — как факт | Сквозная проверка кода | Признак по данным показателя; плановые даты — оценка; решение 59 |
| **Этап 14 считался в доле «в срок» дважды** | Сквозная проверка кода | Исключён; число показа 89,4% → 89,1%, шпаргалка и сверка обновлены |
| **«0 критических дефицитов» без рыночных данных, «Операций на связку: 0,4» → «0», «4 вузов», «204 дней»** | Сквозная проверка кода и интерфейса | Счётчики — `null`; точность по показателю; склонение по числу, одно правило |
| **Программы архивного вуза оставались в рейтинге и дефицитах**; навык с региональными замерами считался дважды; последний период — по алфавиту | Сквозная проверка кода; пробник с контрольным опытом | Одно определение действующей программы, одна строка спроса на навык, период по календарю; решение 60 |
| **Раскрытие балла не сходилось с баллом**, рейтинг брал пустые показатели первыми, «всего» — по срезу | Сквозная проверка кода | Вклады на учтённый вес; NULL в конце; счёт по базе |
| **Импорт программ стирал код, направление, длительность и показатели** у существующих программ, если колонок нет в файле; строки не проверялись правилами API; повторы давали двойников; архивные менялись | Сквозная проверка кода; пробник с контрольным опытом | Поля только из колонок файла, проверка схемами API, повтор и архив — отдельными исходами; решение 61 |
| **«Просрочено не меньше N дней» и «скоро срок» — по 24 часа, а бейджи — по календарю**; синхронизация скрыта от менеджера вопреки контракту; вкладки молча обрезали списки; сбой спроса — как «Нет данных» | Сквозная проверка кода и интерфейса | Календарь везде; кнопка по праву записи; «Показаны первые N из M»; сбой называется сбоем; решение 62 |
| **Реестр документов на проекторе сминал название в столбик**, «К чему относится» наезжал на соседний столбец: заданные ширины съедали строку, гибким доставалось по ~50 px | Обход страниц снимками на 1280×720 | Таблица не уже «заданные + 180 px на гибкий» (прокрутка вместо сминания); столбец «Автор» — в карточку; название вуза в две строки |
| **Данные рекомендации — отладочным выводом**: `deadline: 2026-07-28T14:32Z`, `status: IN_PROGRESS`, ключи в порядке jsonb; на кнопках статусы вместо действий («Принята»); три значка в каждой карточке | Обход страниц по разделу 07 дизайнера | Рекомендации — строками ленты (07, раздел 11); `describeRelatedData`: подписи, даты по Москве, свой порядок, исходник под раскрытием; кнопки — глаголами (`RECOMMENDATION_STATUS_ACTIONS`); подписи показателей программы — одни на рейтинг, правила и интерфейс |
| **Таблица рейтинга на 1280 шире экрана**, «Демонстрационные данные» в каждой строке; подсказка под полем фильтра сдвигала поле вверх; подписи ленты этапов налезали друг на друга; `dd` с браузерным отступом 40 px; иконки в плашках и 14 одинаковых карточек этапов | Обход страниц по чек-листу раздела 40 документа 07 | `mockMarks`: пометка раздела, у строки — только при смешанной выдаче; пояснения фильтров — строкой под панелью (`Toolbar note`); подпись этапа не шире колонки; сброс `dl`/`dd`; иконки без плашек; этапы — строками на одной поверхности; плавающие панели — один плотный токен |
| **Подвал показывал представителю вуза аналитику, рекомендации и настройки** — своя копия меню без учёта роли; четыре одинаковые карточки чисел в кабинете вуза и профиле | Снимки кабинета представителя | Подвал из `navigationFor` и `serviceLinksFor` (тест на роли); `KpiRow` — числа на одной поверхности, число над подписью |
| **Представитель вуза подтверждал материалы по отменённому этапу 7**: запись кабинета шла в обход правил сотрудника (закрытый этап, очередь со сменой статусов); кнопка показывалась всегда; «к подтверждению» считало и то, что подтвердить нельзя | Снимки кабинета; пробник с контрольным опытом (старый код: 200, пункт отмечен, счётчик 8 при 0) | Одна функция записи `setTaskDone` для обоих путей; сервер отдаёт `canConfirm`, счётчик — по нему; 409 в контракте |
| **Одна величина — в трёх форматах**: вес «0,4» в аналитике и «40,0%» в карточке программы; дефицит «77,0%» и «77%»; обоснование дефицита «70.33 из 100» (с точкой) при «70 из 100» в рекомендации | Обход страниц; тест с контрольным опытом | `formatShare` (доля — целым процентом), `formatDemand` и серверный `outOf100` — одно правило «N из 100» для текстов сервера и интерфейса |
| **Шаги 2 и 3.5 показа**: реестр вузов обрезал названия до «Санкт-Петербург…» (шесть узких столбцов с цифрами съедали строку); в дефицитах программы «демо» в каждой строке налезало на «Почему так», а в дефицитах вуза происхождение не показывалось вовсе | Проход сценария показа в браузере | `CellText lines={2}` для названий, столбцы с цифрами уже; таблицы дефицитов — через `mockMarks`: одна пометка над таблицей, у строки — только при смешанной выдаче; бейджи в ячейке переносятся |
| **Несуществующий адрес — белая английская страница Next** «This page could not be found»; несуществующая запись — «Не удалось загрузить» с кнопкой «Повторить», как при сбое; цвет темы браузера — старый фиолетовый | Проход по ошибочным адресам | Своя страница 404 в стиле приложения; `ErrorState` отличает «нет записи» (без повтора) от сбоя; `themeColor` — графит |
| **Карточка вуза разваливалась**: ширину блока рейтинга задавала длина пояснения в одну строку, сведения сжимались в узкий столбик, под рейтингом — пустота; «Санкт-Петербург · Санкт-Петербург» | Обход карточки вуза снимками | Шапка — сетка «сведения + рейтинг до 320 px»; числа над подписями; `formatPlace` не повторяет регион, совпадающий с городом (карточка и реестр) |
| **Личный кабинет на 1280 уезжал вбок на 20 px**: невидимая подсказка у правого края всегда лежала на странице; открытая обрезалась краем окна, в таблице — ячейкой | Обход всех страниц на 1280 и 390 (выход за край, ошибки консоли) | `Tooltip` рисует пузырь только открытым, в `body` через портал, `position: fixed` и сдвиг в пределах окна; прокрутка закрывает |
| **Нарушение CHECK-ограничения базы ушло бы клиенту как 500** «Внутренняя ошибка»: Prisma с адаптером отдаёт его кодом P2039, исходный 23514 — внутри; в тексте Postgres — вся строка с данными | Опыт на локальной базе (ограничение в откатываемой транзакции) перед задачами по CHECK-ограничениям | `422 VALIDATION_ERROR` с `details.constraint`, без содержимого строки; соглашение об именах — в DATABASE_SCHEMA |
| **Реестры — 5–7 строк на экран вместо 20–25 из решения 44**: плашки (решение 64) и названия вузов в две строки (правка 23.09); документы — по три строки на ячейку | Ответ Артура 24.09: актуальна плотность 20–25 строк | Реестры вузов, программ, связок и документов — `grid`, одна строка на ячейку, 32–33 px: 19–20 строк на 1440×900, без прокрутки на 1280; `universityShortName` в ссылках документа; решение 65 |
| **Пустая страница реестра без выхода**, пробел в поиске — ошибка вместо таблицы, «Показать ещё» после 100 событий — ошибка, время встречи — по поясу браузера | Сквозная проверка интерфейса | `usePageInRange`, пробелы в запросе = отсутствие, общий предел, поле времени по Москве |
| **Завершённая связка в демо-наборе — без даты закрытия**: приложение ставит её при закрытии, сид — нет; на карточке казанской связки не было строки «Закрыта» | `npm run db:verify` — проверка целостности данных, первый запуск на сиде и на копии стенда (24.09.2026) | Сид ставит дату закрытия по последнему закрытому этапу; правило проверяется в CI и при каждой перезаливке стенда |

## Не сделано

| Что | Почему | Приоритет |
| --- | --- | --- |
| ~~Подключение фронта~~ | **Сделано 22.09.2026**: все экраны работают с API | — |
| ~~Уведомления~~ | **Сделано 22.09.2026** лентой в интерфейсе, одобрено Артуром (решение 37) | — |
| RLS в PostgreSQL | обещано концепцией; сейчас доступ ограничивается в сервисах | P2, после защиты (решение Артура) |
| Загрузка файлов документов | решение 14: P2 | P2 |
| Уведомления на почту и в мессенджеры | в интерфейсе лента есть; внешний канал не выбран | после защиты |

## Известные ограничения, оставленные осознанно

Найдены при сплошном разборе кода 22.09.2026 и не исправлены — с объяснением,
почему именно.

| Что | Почему оставлено |
| --- | --- |
| Два источника рыночных данных за один период дадут навык в списке дефицитов дважды | В MVP источник один (`MARKET_DATA_PROVIDER=mock`), второй появится только вместе с настоящей интеграцией. Тогда и понадобится группировка по навыку — сейчас это код без применения |
| Путь Windows в `MARKET_DATA_CSV_PATH` теряет обратные слэши при чтении `.env` | Настройка относится к источнику `csv`, который в MVP не используется. Обходится прямыми слэшами — работают и в Windows |
| Правка показателей программы через кабинет вуза помечает происхождение всех трёх сразу | Показатели вносятся вместе одной формой, и различать происхождение по отдельности пока незачем. Если понадобится — это три поля вместо одного |

## Риски

| Риск | Влияние | Что делать |
| --- | --- | --- |
| ~~Нет макетов~~ **Снято 22.09.2026:** дизайн-система Вани и Серёжи в `docs/design/`, интерфейс собран по ней. `DESIGN_INTEGRATION.md` составлен по ТЗ, а не по экранам | Расхождения вскроются при подключении фронта — самое дорогое время для правок | Получить экспорт макетов в `docs/design/` и сверить поле за полем. Частично снято: по `docs/openapi.json` фронт может сгенерировать типизированный клиент |
| ~~Нет экранов кабинета вуза~~ **Снято 22.09.2026:** кабинет `/portal` | Готовый API некому показать | Нарисовать экраны по разделу 7 `DESIGN_INTEGRATION.md` |
| ~~Демо-режим включён по умолчанию~~ **Снято:** в `docker-compose.yml` по умолчанию `false`, на стенде вход только по паролю | Пока `DEMO_AUTH_ENABLED` не выключен, вход без пароля по cookie остаётся открытым | При развёртывании выставить `DEMO_AUTH_ENABLED=false` и задать `AUTH_SECRET` |
| **next-auth 5 в статусе beta** | Единственная версия, работающая с App Router; версия зафиксирована точно | Весь доступ идёт через `getCurrentUser()` — замена реализации не затрагивает модули |
| ~~Коэффициенты не утверждены~~ | **Снято 21.09.2026.** Веса рейтинга утверждены, сроки этапов и пороги рекомендаций оставлены рабочей гипотезой осознанно | см. `docs/DECISIONS.md` |
| **Нет ТЗ Tech Lead** | Часть требований восстановлена по смыслу | Получить файл или подтвердить работу без него |
| ~~Рейтинг считается в приложении, а не в СУБД~~ | **Снято 21.09.2026.** Артур одобрил; перенос в представления возможен позже без изменения контракта API | — |
| ~~Состав контрольных точек не подтверждён~~ | **Снято 22.09.2026.** Артур утвердил 6, 7, 11 без изменений | — |

## Решения Артура получены

Все 13 вопросов закрыты 21.09.2026, зафиксированы в `docs/DECISIONS.md`.
Сверка закрыта 22.09.2026: контрольные точки 6, 7, 11 утверждены без изменений,
поле рейтинга вуза остаётся `score`, в чек-листах одна правка — на этапе 8
«Проведена проверка работоспособности» стала обязательной.

Ниже — прежний список; пометки `TODO: PM DECISION` в коде сняты не везде,
потому что часть значений осознанно остаётся рабочей гипотезой.

## Требует решения Артура (`TODO: PM DECISION`)

1. Перечень статусов взаимодействия с вузом.
2. Обязателен ли IT-продукт при создании связки.
3. Окончательный состав полей заявки на обучение.
4. Веса рейтинга 0,4 / 0,4 / 0,2 и пороги дефицита навыков.
5. Нормативные сроки этапов и состав обязательных пунктов чек-листов.
6. Нужен ли строгий порядок прохождения этапов.
7. Подтвердить camelCase в JSON с фронтом.
8. Методика рейтинга вуза: сейчас среднее по рейтингам его программ (`UNIVERSITY_RATING.method`),
   альтернатива — балл сильнейшей программы. Сам рейтинг и фильтрация по нему сделаны.
9. Пороги правил рекомендаций: 7 и 21 день просрочки, 14 дней застоя, этап 4 как граница
   обязательного выбора продукта.
10. Этап 12 как цель групповой операции по выпуску версии продукта.
11. ~~Оставлять ли демо-режим переключения ролей на защите проекта.~~ Снято: на стенде вход по паролю, демо-режим выключен.
12. Состав пакета документов и тексты шаблонов — сейчас это болванки с пометкой TEMP.
13. Включать ли ИИ-помощник (YandexGPT) на стенде и на показе — решение 90. Код готов,
    по умолчанию выключен: без решения работают шаблоны.

**Пункты контрольной точки** (решение 49, 23.09.2026). Пока договор не подписан,
этап 7 начать нельзя, а его пункты («Передана лицензия») отмечались — и сотрудником,
и вузом в кабинете. Предложено подчинить отметку тому же правилу, что начало этапа;
меняет пятый шаг показа. В `main` с PR #28, шпаргалка показа обновлена.

## Требует согласования с Тиграном

1. Таблица `contacts` — расширение относительно раздела 11 ТЗ.
2. Поле `recommendations.resolution_comment` — миграция
   `20260921074512_recommendation_resolution_comment`.
2а. Поля `documents.content` и `documents.template_key` — миграция
   `20260921082617_document_template_content`.
3. Расчёт рейтинга в приложении вместо представлений СУБД.
4. Право `CREATEDB` у роли базы (нужно только `prisma migrate` локально).
5. Отсутствие уникального ограничения на тройку (вуз, программа, продукт).
6. CHECK-ограничения — миграция `20260924150000_check_constraints`, сделана
   по его заданию без него (24.09.2026): 12 правил, на сиде и данных стенда
   нарушений ноль. Список — в DATABASE_SCHEMA, раздел «CHECK-ограничения».
7. Индексы на 14 внешних ключей без индекса — миграция `20260925210100_fk_indexes`
   (25.09.2026, решение 104): только `CREATE INDEX`, замер на объёме в решении.

## Что дальше

**Расписание (уточнено 23.09.2026):** генеральный прогон — 26-го, защита —
вечером 29-го.

0. **Созвон с наставником 24.09.** К обеду 26-го он ждёт: анализ конкурентов
   и продуктовое исследование экранов, UX-аудит глазами пользователя, ревью кода
   (лишние комментарии, мёртвый код), публичный репозиторий. Взгляд наставника:
   это рабочий инструмент — на каждом экране сразу видно, где горит.
1. ~~Показать интерфейс Ване и Серёже~~ — сделано: визуал ведут они, правки
   выкладываются на стенд сами после проверок (автовыкладка фронта).
2. Утверждение текстов шаблонов документов с юридической службой.
3. База: CHECK-ограничения, проверка целостности `db:verify` и резервная копия
   стенда с проверкой восстановления — сделаны 24.09. Индексы на внешние ключи
   и замер на объёме — сделаны 25.09 (решение 104);
   ответы по базе для защиты — [DATABASE_ANSWERS.md](DATABASE_ANSWERS.md).
4. **Сплошной заморозки нет** (решение Артура 24.09: правки по аудиту и замечаниям
   наставника не успеть). Вместо неё — тихие окна, когда на стенд ничего
   не выкладывается: прогон 26.09 14:00–18:00 и защита с 29.09 12:00. В остальное
   время — PR с зелёными проверками, фронт выкладывается сам, бэк — вручную,
   после выкладки сверка со сценарием и откат при сбое.
5. **26-е — генеральный прогон** по сценарию показа и шпаргалке докладчика,
   на стенде и на запасном ноутбуке; перед ним — перезаливка демо-данных.
6. **27–28-е — правки по аудиту, прогону и обратной связи наставника.**
7. **29-е, днём** — перезаливка стенда и локальной базы, сверка со сценарием,
   вкладки открываются после неё. Вечером — защита.

**Не делаем по решению Артура:** внешние уведомления (почта, мессенджеры) и политики RLS. Оба остаются
открытыми пунктами: RLS — в `docs/SECURITY_LIMITATIONS.md`, к нему придётся
вернуться при переходе к реальным данным.
